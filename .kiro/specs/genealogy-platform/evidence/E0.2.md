# Completion Evidence — E0.2

- Status: DONE
- Completed at: 2026-08-04T15:45:00+07:00
- Scope: Discovery deliverable. Stabilise the canonical vocabulary used across the spec, and encode the policy decisions that govern visibility, living-person handling, redaction, guardianship, merge, uncertainty and direct-edit/approval. Produce the decision table `PRIVATE/UNLISTED/PUBLIC` × role × resource × living status.
- Requirements: R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R16, R17, R18, NFR1, NFR4, NFR5.

## Changes

- New: `.kiro/specs/genealogy-platform/glossary-and-policy-matrix.md` — 7 sections, ~25 canonical terms grouped by Identity/tenancy, genealogy core, privacy/consent, collaboration, sharing/media and cross-cutting. 9 policy rules (`LIVING_INFERENCE`, `REDACTION`, `GUARDIAN`, `MERGE`, `UNCERTAINTY`, `COLLABORATION_MODE`, `VISIBILITY`, `CONSENT_FORM`, `REASON_CODE_REGISTRY`). One full decision table split into three subsections (PRIVATE / UNLISTED / PUBLIC) plus cross-cutting denial rules and obligation semantics. Cross-mapping to E0.1 journeys and downstream epics.
- Updated: `.kiro/specs/genealogy-platform/tasks.md` E0.2 checkbox set to `[x]` per §6.3 of `agent-execution.md`.

## Decisions

- Inherits the platform selections from `design.md` §1 (Kong, Keycloak, OpenFGA, Temporal, Strimzi, Apicurio, Istio, Vault/KMS, Flagsmith, Argo, Grafana). No new ADR is required to compile this glossary; the policy matrix is the discovery contract that E0.5 will close with ADR entries for the open questions listed in §5 of the deliverable.
- `LIVING_INFERENCE_YEARS = 110` adopted as the default window; jurisdiction-level override is reserved for E0.4 and E0.5.
- `MINORITY_AGE = 18` adopted as the fallback; jurisdictional map is reserved for E0.4.
- `MERGE_AUTO_THRESHOLD = 0.85` and `MERGE_REVERT_WINDOW_DAYS = 30` adopted as defaults; both are flagged for E0.5 ADR.
- `CollaborationMode` defaulted to `MIXED` for every new Tree, with role × resource × branch precedence documented in §2.6.
- Deny-reason codes are listed in §2.9 and become the canonical registry consumed by `audit-service` and OpenAPI Problem Details.
- Cross-cutting denial rules in §3.4 supersede the allow matrix; this is the binding hierarchy for E3, E4, E5, E7, E10 and E11.
- Persona composition rules from E0.1 §1 are absorbed into the `Role` and `Visibility` definitions; no new persona is introduced.
- No ADR-required decision was taken in this task. All open thresholds carry `DRAFT` markers and are deferred to E0.5, as instructed by `agent-execution.md` §2 "không tự chọn công nghệ ... cần ADR".

## Validation

| Command/check | Result | Evidence |
|---|---|---|
| Read of `requirements.md` | PASS | Every R-reference in the glossary / policy maps to an existing clause (R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R16, R17, R18, NFR1, NFR4, NFR5) |
| Read of `design.md` | PASS | Cross-references anchored to §1 platform, §4 service boundaries, §5 data model, §6.2 policy model, §6.3 visibility, §8.3 proposal, §8.4 GEDCOM, §13 platform ops |
| Read of `tasks.md` | PASS | E0.2 description (glossary, policy matrix, decision table) fully satisfied; E0.3–E0.6 dependencies documented in §5 of the deliverable |
| Read of `agent-execution.md` | PASS | Section 4 process followed; no code or ADR-level decisions made; PARTIAL/BLOCKED status not required |
| Read of `personas-and-journeys.md` (E0.1) | PASS | Every journey re-mapped in §4 to the rules that govern it; no orphan policy; no orphan journey |
| Cross-term uniqueness check | PASS | No duplicate canonical names; each term has a single owner service and a single persistence location |
| Decision-table completeness check | PASS | §3.1 covers PRIVATE × all roles × all resources; §3.2 covers UNLISTED (token + member scenarios); §3.3 covers PUBLIC (anonymous + member scenarios); §3.4 covers the unconditional deny overlay |
| Reason-code coverage check | PASS | Every deny row in §3 maps to a code in §2.9; no orphan reason code |
| Inheritance/across-rule consistency | PASS | §3.4 explicitly dominates §3.1–§3.3; not contradicted anywhere |

## Security and privacy

- Tenant isolation: N/A — E0.2 is a discovery artefact, no code paths.
- Authorization/ABAC: PASS — the policy matrix in §2 is the ABAC contract that E3, E4, E5, E10 will enforce. Living redaction, minor restriction, consent gating, and visibility rules are all encoded with explicit reason codes and obligations.
- Secret/PII/DNA leakage: PASS — no real identifiers, DNA raw data, credentials or PII appear in the deliverable. All references are conceptual and tied to §2.2 redaction rules.
- Dependency/config scan: N/A — pure Markdown.
- Living-person safety: PASS — `LIVING`/`PRESUMED_LIVING` Persons are redacted in §3.1, §3.2, §3.3 with deterministic field-level rules; public projection never exposes living relationships or media.
- DNA safety: PASS — anonymous and token-only subjects are explicitly denied `DNA_*` access in §3.2 and §3.3; `dna_isolation_required` is a cross-cutting denial in §3.4.
- Minor safety: PASS — `minor_restricted` is a cross-cutting denial and a row in §3.1; Guardian workflow is bounded by §2.3 with jurisdiction hooks.

## Acceptance criteria

- [x] Chuẩn hóa Tree, Person, User, Relationship, Claim, Citation, Living, Minor, Consent và Tenant — covered in `glossary-and-policy-matrix.md` §1.1–§1.6 (Tree, Person, Relationship, Claim, Citation, LivingStatus, Minor, Consent, Tenant, User, Membership, plus cross-cutting Resource/Action/ReasonCode).
- [x] Chốt living inference, redaction, guardian, merge, uncertainty và direct-edit/approval — covered in §2.1–§2.6 (six dedicated rules) with cross-cutting denial overlay in §2.7 and §3.4.
- [x] Tạo decision table `PRIVATE/UNLISTED/PUBLIC` theo role, resource và trạng thái người sống — covered in §3.1, §3.2, §3.3 with the cross-cutting denial overlay in §3.4 and obligation semantics in §3.5.

## Residual risks

- Jurisdiction-specific values (minority age, retention, lawful basis, jurisdictional redaction hooks) are deliberately defaulted and deferred to E0.4. They are flagged in §5 of the deliverable and listed in `tasks.md` E0.4 acceptance.
- Numeric thresholds (110 years, 0.85 merge, 30-day revert, 60s consent revocation) are defaults; ADR closure in E0.5 will ratify them.
- Decision table cannot be tested by executable code in E0.2; downstream epics will implement the policy as OpenFGA tuples + ABAC obligations and validate via E2.3/E3.4/E4.5/E5.2/E10.3 tests.
