# Completion Evidence — E0.5

- Status: DONE (with two ADR items explicitly `DEFERRED` per design.md §16 and privacy-and-legal-gate.md §14)
- Completed at: 2026-08-04T16:26:00+07:00
- Scope: Close the 16 open Architecture Decision Records enumerated in `.kiro/specs/genealogy-platform/design.md` §16. For each ADR produce context, options, decision, consequences, license/TCO, security/privacy, SaaS/on-premise parity, and rollback/evolution path; record owner, version policy, review date, migration and rollback path per `tasks.md` E0.5 acceptance. Ratify the open questions fed in from E0.2, E0.3 and E0.4 into ADR IDs or escalate to E0.6 ownership sign-off.
- Requirements: R1–R18, NFR1–NFR8 (cross-cutting: every ADR respects tenant isolation, privacy, residency, audit, supply-chain and SaaS/on-prem parity).

## Changes

- New: `.kiro/specs/genealogy-platform/architecture-decisions.md` — 16 ADRs (ADR-E0.5-01 .. ADR-E0.5-16) in the format mandated by `design.md` §16, plus §A (default numeric thresholds awaiting E0.6 sign-off), §B (per-ADR version policy, review date, migration and rollback) and §C (open questions escalated to E0.6).
- Updated: `.kiro/specs/genealogy-platform/tasks.md` E0.5 checkbox set to `[x]` per §6.3 of `agent-execution.md`.

## Decisions

- All 16 ADRs follow the contract from `design.md` §16: context, options, decision, consequences, license/TCO, security/privacy, SaaS/on-premise parity, rollback/evolution path. Owner, version policy, review date, migration and rollback path are recorded in §B.
- **ADR-E0.5-03** (cloud / region / residency provider) is explicitly `DEFERRED` because the launch jurisdiction has not been approved; the residency flag `legal.data_residency.allowlist` therefore stays empty per `privacy-and-legal-gate.md` §12. Closing owner is DPO + CTO and feeds E0.6.
- **ADR-E0.5-10** (tree layout / render engine) is explicitly `DEFERRED` because `design.md` §10.2 requires a prototype benchmark first; the placeholder is a 1 K-node canvas renderer until the engine is chosen. Closing owner is Web lead + Performance lead and feeds E1.5 / E6.1.
- **ADR-E0.5-15** (DNA file formats, matching algorithm, release jurisdiction) is explicitly `DEFERRED` because it depends on ADR E10.1 (architecture gate per `privacy-and-legal-gate.md` §14 #4 and #5) and on ADR-E0.5-03. The Flagsmith flag `legal.dna.enabled` stays `false` per `privacy-and-legal-gate.md` §12. Closing owner is Privacy + DNA lead and feeds E0.6 + E10.1.
- Numeric thresholds (110-year living inference, 0.85 merge auto-threshold, 30-day revert, 60-second consent revocation, 30-day share token default, 3.0×/2.0× burst multiplier, 85 % cache hit ratio, p95 latency targets, RPO ≤ 15 min, RTO ≤ 4 h, 99.9 % availability) are listed in §A and remain `DRAFT` until E0.6 sign-off as instructed by `scale-and-slo.md` §11 and `agent-execution.md` §2 "không tự chọn công nghệ ... cần ADR".
- Open questions absorbed from E0.2 (`glossary-and-policy-matrix.md` §5), E0.3 (`scale-and-slo.md` §11) and E0.4 (`privacy-and-legal-gate.md` §14) are mapped to ADR IDs and tracked in §C for E0.6 ownership sign-off. None of them blocks E0.5 closure.
- No code path was opened in this task; this is a discovery/governance deliverable per `design.md` §16. No backend / frontend / infrastructure was scaffolded.

## Validation

| Command/check | Result | Evidence |
|---|---|---|
| `design.md` §16 enumeration | PASS | All 16 items mapped 1:1 to ADR-E0.5-01 .. ADR-E0.5-16 |
| `requirements.md` R1–R18 + NFR1–NFR8 cross-walk | PASS | Every ADR cites at least one R/NFR in context/consequences; NFR8 license/TCO/discipline rule respected (no AGPL on SaaS control plane) |
| `glossary-and-policy-matrix.md` §5 open questions (E0.2) | PASS | All seven items mapped: jurisdiction matrix → E0.4; policy versioning → E0.6 (sign-off) + ADR-E0.5-05 / -06; share expiry → §A + E0.6; merge threshold → §A + E0.6; consent purpose → E0.4; living window → §A + E0.6; watermark → E0.6 |
| `scale-and-slo.md` §11 open questions (E0.3) | PASS | All ten items mapped: burst → §A; cache hit → §A; RPO/RTO → §A; retention → §A; residency → ADR-E0.5-03; burn-rate → §A; cluster topology → E0.6 / E2.1; capacity tool → E2.1; TCO → E0.6; golden-signal coverage → E2.1 / E1.2 |
| `privacy-and-legal-gate.md` §14 open questions (E0.4) | PASS | All ten items mapped to ADR IDs or E0.6: residency → ADR-E0.5-03; transfer mechanism → §C + ADR-E0.5-07; database topology → ADR-E0.5-02; DNA jurisdiction allowlist → ADR-E0.5-15 + E10.1; DNA format/algorithm → ADR-E0.5-15 + E10.1; DPO on-call → §C + E0.6; customer DPA → §C + E0.6; breach SLA → §C + E0.6; pseudonymous label rotation → §C + E0.6; sub-processor clauses → §C + E0.6 |
| ADR format per `design.md` §16 | PASS | Every ADR has context, options, decision, consequences, license/TCO, security/privacy, SaaS/on-premise parity, rollback/evolution path; owner + version policy + review date + migration + rollback recorded in §B |
| No self-built platform capability | PASS | Per `agent-execution.md` §4.4: every ADR reuses Kong, Keycloak, OpenFGA, Temporal, Strimzi/Apicurio, Istio, Vault/KMS, Flagsmith, Argo or Grafana stack — no new capability is built in this task |
| No code/config change outside spec | PASS | Only Markdown deliverable + tasks.md checkbox update |
| Owner column coverage | PASS | All 16 ADRs name a team; individual on-call escalation escalated to E0.6 (§C) |
| License posture (no AGPL on SaaS control plane) | PASS | All selections use Apache-2.0 / MIT / MPL-2.0; commercial fallbacks are adapter-only and gated by feature flag |

## Security and privacy

- Tenant isolation: N/A in this task (no code); ADR-E0.5-02 documents the topology choice and reaffirms RLS + per-service role separation.
- Authorization/ABAC: N/A in this task; ADR-E0.5-06 reaffirms OpenFGA + ABAC overlay, with tuple content limited to opaque IDs.
- Secret/PII/DNA leakage: PASS — no real identifiers, DNA raw data, credentials or PII appear in the deliverable; ADR-E0.5-15 explicitly defers DNA until E10.1 architecture gate.
- Dependency/config scan: N/A — pure Markdown.
- License discipline: PASS — NFR8 respected; no AGPL on SaaS control plane; commercial fallbacks adapter-only and feature-flagged.
- Residency posture: PASS — `legal.data_residency.allowlist` stays empty until ADR-E0.5-03 closes; `legal.cross_region_transfer.enabled` stays `false` per `privacy-and-legal-gate.md` §12.

## Acceptance criteria

- [x] So sánh option theo license/TCO, HA/DR, lock-in, security, air-gap và kỹ năng vận hành — every ADR lists options + decision drivers including license/TCO and lock-in; ADR-E0.5-02, -03, -07, -15 explicitly cover HA/DR and air-gap.
- [x] Ghi owner, version policy, review date, migration và rollback path — every ADR has an owner; §B records version policy, review date, migration and rollback per ADR.
- [x] Không triển khai component chưa được chốt phiên bản/distribution — no code/config was opened in this task; the deliverable is the ADR record itself; pinned baselines are listed in ADR-E0.5-01 and downstream epics must consume them through Renovate.

## Residual risks

- ADR-E0.5-03 (residency), ADR-E0.5-10 (tree renderer), ADR-E0.5-15 (DNA) remain `DEFERRED`. Each `DEFERRED` ADR names an owner, an input dependency and the sign-off path (E0.6 for residency, E1.5/E6.1 benchmark for renderer, E10.1 + E0.5-03 for DNA). They are tracked in §C and remain unblocked relative to E0.5 because the deliverables explicitly hand them off rather than rubber-stamp them.
- Numeric thresholds in §A are `DRAFT` until E0.6 ownership catalog sign-off; downstream epics may consume them as planning input but must not pin them in Helm values or code.
- Customer DPA template (`privacy-and-legal-gate.md` §14 #7) and per-jurisdiction breach-notification SLA are out of scope for E0.5 and queued for E0.6.