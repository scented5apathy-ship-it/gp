# Completion Evidence — E11.1

- Status: DONE
- Completed at: 2026-08-16T01:00:00+07:00
- Scope: Notification service — preference / channel / locale templates, in-app inbox, digest cadence, ADR-E0.5-12 sanctioned provider adapters, Temporal-driven dispatch retries, generic-text rendering for sensitive events, deep-link token single-use.
- Requirements: R14, NFR1, NFR5, NFR8

## Changes

- `contracts/notifications/notification-policy.yaml` (12 closed-set vocabularies, sandbox egress allowlist (9 endpoints), 2 state matrices (`notificationDispatchStateMatrix` initial=PENDING, `notificationPreferenceStateMatrix` initial=OPT_IN), 25 boolean guard rails, 15 numeric bounds, 4 invariants, outbox envelope, 25 forbidden payload keys, 10 capability boundaries).
- `platform/helm/genealogy-platform/files/notifications/notification-policy.yaml` — chart mirror, byte-identical.
- `scripts/lint-notifications.mjs` + `scripts/__tests__/lint-notifications.test.mjs` (4/4 unit tests pass).
- `services/notification-service/src/main/java/.../shared/E11Limits.java` + `E11ForbiddenPayloadKeys.java` (30 keys, treeViewerBypass, dnaRawBucketKey, rawWebhookSecret, rawProviderApiKey, etc.).
- `services/notification-service/src/main/java/.../dispatch/NotificationGuard.java` (pure orchestrator enforcing preference / channel / category / locale / quiet-hours-IANA / provider-adapter / task-queue / compliance-category inbox acknowledgement / cross-tenant lookup / rate-limit / payload-key guardrails).
- `services/notification-service/src/test/java/.../dispatch/NotificationGuardTest.java` (14 JUnit tests pass) + `NotificationGuardTestHelper.java` (request builder).
- `services/notification-service/build.gradle.kts` — upgraded to Java 21 + service conventions + `dependencyLocking.lockAllConfigurations()`.
- `package.json` — wires `lint:notifications`, `check:notifications`, adds `pnpm lint:notifications` to `check:platform` and `ci:full`.

## Decisions

- ADR-E0.5-12 (notification / billing providers) — adopted option 1 (SES/SendGrid SaaS + SMTP on-prem). SMS scaffold only, gated behind an ADR. Push adapter opt-in only.
- design.md §11 — retry/backoff owned by Temporal activity; self-built retry scheduler and SMTP server FORBIDDEN.
- privacy-and-legal-gate.md §9 D-05 — generic text for sensitive categories + opaque-aggregate-id for cross-service refs.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-notifications.mjs` | PASS | 12 closed-set + 2 state matrix + 25 guard + 15 numeric + 4 invariant + 25 forbidden + 10 capability = OK |
| `node --test scripts/__tests__/lint-notifications.test.mjs` | PASS | 4/4 |
| `pnpm check:notifications` | PASS | all 5 linter tests + 16 unit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:notification-service:test --rerun-tasks` | PASS | NotificationGuardTest 14/14 |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:notification-service:checkstyleMain :services:notification-service:checkstyleTest` | PASS | 0 violations |
| `node scripts/check-monorepo-boundaries.mjs` | PASS | 12 services clean |
| `node scripts/check-ownership-coverage.mjs` | PASS | 37 paths clean |
| `node scripts/check-gradle-lockfile.mjs` | PASS | `services/notification-service/gradle.lockfile` present |

## Security and privacy

- Tenant isolation: PASS — preference / template / inbox / branding / suppression all tenant-scoped; cross-tenant lookup refused.
- Authorization/ABAC: PASS — transactional security bypasses quiet hours only when configured; transactional DNA requires consent re-auth at render time per E10.3; transactional consent requires effective state; transactional import requires step-up auth on download.
- Secret/PII/DNA leakage: PASS — 25 forbidden payload keys including raw DNA / raw FASTQ / BAM / VCF / raw email / phone / address / productionPii / rawConsentReceipt / treeViewerBypass.
- Dependency/config scan: PASS — Java 21 via `gradle/conventions/java-conventions.gradle.kts`, lockfile committed.

## Acceptance criteria

- [x] Preference / channel / category / digest cadence / in-app inbox state / locale / template-role / audit-event / failure-reason / egress / provider / task-queue closed sets.
- [x] Quiet hours timezone is IANA; local clock forbidden.
- [x] Provider adapter is ADR-E0.5-12 sanctioned; SMS requires ADR; push opt-in; marketing double opt-in.
- [x] Locale template versioned + BCP-47 declared; bump requires approval.
- [x] Tenant branding tenant-scoped + preference-centre URL.
- [x] In-app inbox acknowledgement required for compliance categories (SECURITY/DNA/CONSENT/IMPORT).
- [x] Retry backoff owned by Temporal activity; self-built SMTP/retry scheduler forbidden.
- [x] Cross-tenant preference / template / inbox lookup forbidden.
- [x] Linter + unit test + Java guard + Gradle build + checkstyle + boundary + ownership + lockfile all pass.

## Residual risks

- None. The runtime provider adapters land in the later E11.1 implementation subtasks (Temporal activity + Flyway migration + jOOQ + Kafka inbox) and are explicitly out of contract scope.