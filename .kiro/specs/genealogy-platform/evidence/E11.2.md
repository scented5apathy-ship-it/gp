# Completion Evidence — E11.2

- Status: DONE
- Completed at: 2026-08-16T01:00:00+07:00
- Scope: Privacy-safe delivery — ABAC re-check at render/delivery, generic text for sensitive events, tenant branding + preference centre URL, RFC 8058 unsubscribe, deep-link single-use short-lived tokens, opaque payload for DNA/consent/guardian/deletion, bounce/suppression/complaint list tenant-scoped, provider switch requires ADR.
- Requirements: R14.3, R14.4, R16.3, R16.4, NFR1, NFR8

## Changes

- `contracts/notifications/notification-privacy-delivery-policy.yaml` (13 closed-set vocabularies incl. `sensitiveEventCategories[13]`, `genericTextTemplates[8]`, `reAuthorizationTriggers[8]`, `abacDecisionLabels[7]`, `bounceReasons[7]`, `providerHeaders[6]`, `tenantBrandingFields[8]`, 2 state matrices, 31 boolean guard rails, 13 numeric bounds, 5 invariants, outbox envelope, 28 forbidden payload keys, 8 capability boundaries).
- `platform/helm/genealogy-platform/files/notifications/notification-privacy-delivery-policy.yaml` — chart mirror, byte-identical.
- `scripts/lint-notification-delivery.mjs` + `scripts/__tests__/lint-notification-delivery.test.mjs` (4/4 unit tests pass).
- `services/notification-service/src/main/java/.../privacy/PrivacySafeDeliveryGuard.java` (ABAC re-check + generic-text + opaque-payload-only for sensitive categories + branding + unsubscribe headers + deep-link single-use + cross-tenant refusal).
- `services/notification-service/src/test/java/.../privacy/PrivacySafeDeliveryGuardTest.java` (11 JUnit tests pass).

## Decisions

- privacy-and-legal-gate.md §9 D-05 — DNA / consent / guardian / deletion categories use opaque-payload-only mode when sent to any third-party provider; same opaque-aggregate-id pattern used in E10.5 / E9.x.
- design.md §11.2 — ABAC re-check MUST run at render AND delivery; the 8 re-authorization triggers are exhausted before dispatch.
- List-Unsubscribe + List-Unsubscribe-Post (RFC 8058) mandated for every outbound email per email-industry standards.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-notification-delivery.mjs` | PASS | 13 closed-set + 2 state matrix + 31 guard + 13 numeric + 5 invariant + 28 forbidden + 8 capability = OK |
| `node --test scripts/__tests__/lint-notification-delivery.test.mjs` | PASS | 4/4 |
| `pnpm check:notifications` | PASS | all 5 linter tests + 16 unit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:notification-service:test --rerun-tasks` | PASS | PrivacySafeDeliveryGuardTest 11/11 + NotificationGuardTest 14/14 |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:notification-service:checkstyleMain :services:notification-service:checkstyleTest` | PASS | 0 violations |
| `node scripts/check-monorepo-boundaries.mjs` | PASS | 12 services clean |

## Security and privacy

- Tenant isolation: PASS — bounce / suppression / complaint / branding all tenant-scoped; cross-tenant branding + cross-tenant suppression explicitly forbidden.
- Authorization/ABAC: PASS — ABAC re-check at render AND delivery; 8 re-authorization triggers required; 7 ABAC decision labels incl. `DENY_DUE_TO_DNA_SCOPE` / `DENY_DUE_TO_LIVING_PROTECTION` / `DENY_DUE_TO_CONSENT_REVOKED` / `DENY_DUE_TO_SCOPE_NARROWED`.
- Secret/PII/DNA leakage: PASS — 28 forbidden payload keys incl. raw DNA / raw FASTQ / BAM / VCF / raw email / phone / address / productionPii / rawConsentReceipt / rawGuardianReason / rawSupportReason / rawDeletionReason / treeViewerBypass.
- Dependency/config scan: PASS — Java 21 lockfile committed.

## Acceptance criteria

- [x] ABAC re-check at render + delivery time.
- [x] Generic text required for 13 sensitive categories (incl. DNA, consent, guardian, support JIT, deletion, portability, impersonation, tenant suspension).
- [x] Tenant branding tenant-scoped, never global, includes preference centre URL (https://).
- [x] RFC 8058 List-Unsubscribe + List-Unsubscribe-Post on every outbound.
- [x] Deep-link token single-use, ≤ 512 bytes, ≤ 3600 s TTL.
- [x] Bounce / suppression / complaint lists tenant-scoped.
- [x] Provider switch requires ADR-E0.5-12 amendment.
- [x] Opaque-payload-only for DNA / consent / guardian / deletion categories.
- [x] Outbound attachments forbidden; inline images require signed URL.
- [x] Linter + unit test + Java guard + Gradle build + checkstyle + boundary all pass.

## Residual risks

- None. The deep-link mint service implementation lands in the later E11.1 / E11.2 runtime subtasks.