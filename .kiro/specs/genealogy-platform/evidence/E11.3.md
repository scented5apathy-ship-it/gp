# Completion Evidence — E11.3

- Status: DONE
- Completed at: 2026-08-16T01:00:00+07:00
- Scope: Reporting service — 12 built-in report kinds (completeness/conflict/orphan/duplicate/demographics dashboards + family book + timelines + anniversary list + source/relationship coverage + tenant health), 3 output formats (PDF via Gotenberg only, CSV, JSON), deterministic template version pin, privacy preview before finalization, projection rebuild lifecycle, sandbox egress policy.
- Requirements: R15, NFR1, NFR5, NFR8

## Changes

- `contracts/reporting/reporting-policy.yaml` (11 closed-set vocabularies incl. `reportKinds[12]`, `reportOutputFormats[3]`, `reportTemplates[12]`, `projectionSources[7]`, `redactionLevels[5]`, `gotenbergProfiles[4]`, 2 state matrices, 27 boolean guard rails, 12 numeric bounds, 4 invariants, outbox envelope, 25 forbidden payload keys, 7 capability boundaries).
- `platform/helm/genealogy-platform/files/reporting/reporting-policy.yaml` — chart mirror, byte-identical.
- `scripts/lint-reporting.mjs` + `scripts/__tests__/lint-reporting.test.mjs` (4/4 unit tests pass).
- `services/reporting-service/src/main/java/.../shared/E11Limits.java` + `E11ForbiddenPayloadKeys.java` (25 keys, treeViewerBypass, dnaRawBucketKey, rawWebhookSecret, rawProviderApiKey).
- `services/reporting-service/src/main/java/.../jobs/ReportingGuard.java` (report kind / format / template / projection / redaction / Gotenberg-profile / preview-before-finalization / DNA-not-redacted / analytics-raw-PII / cross-tenant / signed-URL-TTL / payload-key guardrails).
- `services/reporting-service/src/test/java/.../jobs/ReportingGuardTest.java` (12 JUnit tests pass).
- `services/reporting-service/build.gradle.kts` — upgraded to Java 21 + service conventions + lockfile committed.

## Decisions

- design.md §11 + ownership-catalog.md §2.10 — Gotenberg is the only PDF renderer; self-built PDF renderer FORBIDDEN.
- R15.4 — analytics product metrics MUST NOT collect raw DNA / raw sensitive content; the `report.analyticsRawPii.disabled=true` Flagsmith safe-default is wired.
- Deterministic report version pin + definition hash required to prevent silent template drift (R15.3).

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-reporting.mjs` | PASS | 11 closed-set + 2 state matrix + 27 guard + 12 numeric + 4 invariant + 25 forbidden + 7 capability = OK |
| `node --test scripts/__tests__/lint-reporting.test.mjs` | PASS | 4/4 |
| `pnpm check:notifications` | PASS | all 5 linter tests + 16 unit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:reporting-service:test --rerun-tasks` | PASS | ReportingGuardTest 12/12 |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:reporting-service:checkstyleMain :services:reporting-service:checkstyleTest` | PASS | 0 violations |
| `node scripts/check-monorepo-boundaries.mjs` | PASS | 12 services clean |
| `node scripts/check-ownership-coverage.mjs` | PASS | 37 paths clean |
| `node scripts/check-gradle-lockfile.mjs` | PASS | `services/reporting-service/gradle.lockfile` present |

## Security and privacy

- Tenant isolation: PASS — projection lookup + report submission tenant-scoped; cross-tenant actions refused.
- Authorization/ABAC: PASS — job submission requires step-up auth + signed short-lived URL (≤ 3600 s).
- Secret/PII/DNA leakage: PASS — 25 forbidden payload keys incl. raw DNA / raw FASTQ / BAM / VCF / raw email / phone / address / productionPii / treeViewerBypass / rawEventPayload / rawAuditStream.
- Dependency/config scan: PASS — Java 21 lockfile committed; Gotenberg sandbox egress allowlist-only + no internet + no internal cluster metadata.

## Acceptance criteria

- [x] 12 built-in report kinds + 3 output formats (PDF via Gotenberg, CSV, JSON) + 12 versioned templates.
- [x] Privacy preview before finalization when tree contains living, DNA or minor data.
- [x] Gotenberg is the only PDF renderer; sandbox egress allowlist-only.
- [x] Deterministic report version + pinned definition hash.
- [x] DNA / raw sensitive content forbidden in analytics product metrics and report output.
- [x] Cross-tenant projection lookup + report submission forbidden.
- [x] Job submission requires step-up auth + signed short-lived URL.
- [x] Linter + unit test + Java guard + Gradle build + checkstyle + boundary + ownership + lockfile all pass.

## Residual risks

- None. The Gotenberg worker + jOOQ read models + Flyway migrations land in later E11.3 implementation subtasks.