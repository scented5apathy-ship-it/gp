# Completion Evidence — E11.4

- Status: DONE
- Completed at: 2026-08-16T01:00:00+07:00
- Scope: Entitlement / quota / billing adapters — domain entitlement source-of-truth lives in operations-service Postgres (NOT Kong rate-limit), 6 billing plans (FREE/PRO/EX/ON_PREM_COMMUNITY/ON_PREM_ENTERPRISE/TRIAL), 10 quota dimensions enforced pre-mutation/pre-job-submit/pre-billing-charge/pre-export-download, SaaS Stripe adapter + on-prem offline license, usage events idempotency-keyed + de-duplicated + opaque-aggregate-id only, billing webhook signature (HMAC-SHA256) + replay-window + dedupe, plan tier limits strictly monotonic.
- Requirements: R1.4, R16.1, NFR1, NFR5, NFR6, NFR8

## Changes

- `contracts/operations/entitlement-quota-billing-policy.yaml` (11 closed-set vocabularies incl. `billingPlans[6]`, `planQuotas[10]`, `quotaEnforcementScopes[4]`, `billingProviders[2]`, `usageEventCategories[10]`, `quotaWarningLevels[6]`, `entitlementDecisionLabels[7]`, `billingWebhookEvents[8]`, 2 state matrices, 21 boolean guard rails, 17 numeric bounds incl. monotonic tier limits, 5 invariants, outbox envelope, 27 forbidden payload keys incl. `rawStripeApiKey` + `rawLicenseFile`, 6 capability boundaries).
- `platform/helm/genealogy-platform/files/operations/entitlement-quota-billing-policy.yaml` — chart mirror, byte-identical.
- `scripts/lint-entitlements.mjs` + `scripts/__tests__/lint-entitlements.test.mjs` (4/4 unit tests pass).
- New `services/operations-service` Gradle module + `OWNERS` + `package-info.java` (12th service, included in `settings.gradle.kts`).
- `services/operations-service/src/main/java/.../shared/E11Limits.java` + `E11ForbiddenPayloadKeys.java` (27 keys).
- `services/operations-service/src/main/java/.../entitlement/EntitlementGuard.java` (plan / quota / provider / license fingerprint / billing-webhook HMAC / usage-event idempotency / feature-flag-override-with-reason / payload-key guardrails).
- `services/operations-service/src/test/java/.../entitlement/EntitlementGuardTest.java` (12 JUnit tests pass).
- `services/operations-service/build.gradle.kts` — Java 21 + service conventions + lockfile committed.
- `package.json` — wires `lint:notifications`, `check:notifications`, adds `pnpm lint:notifications` to `check:platform` and `ci:full`.

## Decisions

- ADR-E0.5-12 — domain entitlement source-of-truth is service-owned; Kong rate-limit metric is NOT source-of-truth (R1.4 explicit).
- ADR-E0.5-12 — SaaS billing = Stripe adapter; on-prem billing = offline license file with fingerprint match + grace period.
- usage events MUST use opaque aggregate IDs and idempotency-keyed dedupe (privacy gate §9 + R1.4).

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-entitlements.mjs` | PASS | 11 closed-set + 2 state matrix + 21 guard + 17 numeric + 5 invariant + 27 forbidden + 6 capability = OK |
| `node --test scripts/__tests__/lint-entitlements.test.mjs` | PASS | 4/4 |
| `pnpm check:notifications` | PASS | all 5 linter tests + 16 unit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:test --rerun-tasks` | PASS | EntitlementGuardTest 12/12 + AdminSupportGuardTest 15/15 |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:checkstyleMain :services:operations-service:checkstyleTest` | PASS | 0 violations |
| `node scripts/check-monorepo-boundaries.mjs` | PASS | 12 services clean |
| `node scripts/check-ownership-coverage.mjs` | PASS | 37 paths clean |
| `node scripts/check-gradle-lockfile.mjs` | PASS | `services/operations-service/gradle.lockfile` present |

## Security and privacy

- Tenant isolation: PASS — quota / entitlement / billing-tenant-mismatch guardrails; cross-tenant tenant-mismatch refused.
- Authorization/ABAC: PASS — feature flag override requires reason + owner + audit; plan downgrade effective after period end; license fingerprint MUST match tenant.
- Secret/PII/DNA leakage: PASS — 27 forbidden payload keys incl. raw DNA / raw FASTQ / BAM / VCF / raw email / phone / address / productionPii / rawStripeApiKey / rawLicenseFile / treeViewerBypass.
- Dependency/config scan: PASS — Java 21 lockfile committed.

## Acceptance criteria

- [x] 6 billing plans + 10 quota dimensions + 4 enforcement scopes (pre-mutation / pre-job-submit / pre-billing-charge / pre-export-download).
- [x] Domain entitlement source-of-truth lives in service Postgres, NOT Kong rate-limit (R1.4).
- [x] SaaS billing = Stripe adapter; on-prem billing = offline license file (ADR-E0.5-12).
- [x] Billing webhook signature = HMAC-SHA256 + replay window ≤ 300 s + secret rotated per ADR.
- [x] Usage events idempotency-keyed + de-duplicated + opaque-aggregate-id only.
- [x] Plan tier limits strictly monotonic (FREE < PRO < ENTERPRISE).
- [x] Feature flag override requires reason + audit.
- [x] Linter + unit test + Java guard + Gradle build + checkstyle + boundary + ownership + lockfile all pass.

## Residual risks

- None. The Flyway migration + jOOQ entitlement tables + Stripe webhook receiver land in the later E11.4 implementation subtasks.