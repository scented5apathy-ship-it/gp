# Completion Evidence — E11.5

- Status: DONE
- Completed at: 2026-08-16T01:00:00+07:00
- Scope: Admin / support / operations — JIT support access (step-up auth + scoped expiry + banner + audit), impersonation disabled by default and NEVER covers DNA / consent / export raw bundle / tenant deletion, DLQ replay requires snapshot + lineage hash, projection rebuild tenant-scoped (cross-tenant forbidden), feature flag override requires reason + owner + audit and NEVER touches DNA_FORBIDDEN / CONSENT_FORBIDDEN / TENANT_ISOLATION_FORBIDDEN / AUDIT_FORBIDDEN categories, tenant switch requires re-auth, audit export = one-shot signed URL that respects retention.
- Requirements: R16, NFR1, NFR5, NFR8

## Changes

- `contracts/operations/admin-support-operations-policy.yaml` (12 closed-set vocabularies incl. `adminRoles[9]`, `supportAccessModes[6]`, `adminOperations[23]`, `impermissibleScopes[7]` (DNA / consent / export raw / tenant deletion force / admin self-grant / impersonation grant-other), `dlqReplayModes[4]`, `projectionRebuildSources[9]`, `featureFlagCategories[10]` with 4 forbidden categories, 3 state matrices (`jitAccessStateMatrix`, `dlqReplayStateMatrix`, `projectionRebuildAdminStateMatrix`), 31 boolean guard rails, 14 numeric bounds incl. `impersonationMaxDurationSeconds == 0`, 5 invariants, outbox envelope, 25 forbidden payload keys, 9 capability boundaries).
- `platform/helm/genealogy-platform/files/operations/admin-support-operations-policy.yaml` — chart mirror, byte-identical.
- `scripts/lint-operations.mjs` + `scripts/__tests__/lint-operations.test.mjs` (4/4 unit tests pass).
- `services/operations-service/src/main/java/.../admin/AdminSupportGuard.java` (JIT invariants + impersonation NEVER covers DNA / consent / export raw / tenant deletion + DLQ snapshot requirement + cross-tenant rebuild refused + forbidden flag categories + tenant-switch re-auth + audit-export retention guardrails).
- `services/operations-service/src/test/java/.../admin/AdminSupportGuardTest.java` (15 JUnit tests pass).
- `services/operations-service/OWNERS` (per-service CODEOWNERS) + `package-info.java` + Gradle module included in `settings.gradle.kts`.

## Decisions

- privacy-and-legal-gate.md §9 TM-06 — JIT step-up auth + DNA namespace separate in OpenFGA + impersonation flag never covers DNA.
- R16.4 — impersonation disabled by default; `impersonationMaxDurationSeconds == 0` is enforced as an invariant (any non-zero value is a bug).
- ADR-E0.5-08 — DLQ replay requires a snapshot with lineage hash to prevent silent event reordering.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-operations.mjs` | PASS | 12 closed-set + 3 state matrix + 31 guard + 14 numeric + 5 invariant + 25 forbidden + 9 capability = OK |
| `node --test scripts/__tests__/lint-operations.test.mjs` | PASS | 4/4 |
| `pnpm check:notifications` | PASS | all 5 linter tests + 16 unit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:test --rerun-tasks` | PASS | AdminSupportGuardTest 15/15 + EntitlementGuardTest 12/12 |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:checkstyleMain :services:operations-service:checkstyleTest` | PASS | 0 violations |
| `node scripts/check-monorepo-boundaries.mjs` | PASS | 12 services clean |
| `node scripts/check-ownership-coverage.mjs` | PASS | 37 paths clean |

## Security and privacy

- Tenant isolation: PASS — projection rebuild tenant-scoped; cross-tenant rebuild refused; tenant switch requires re-auth.
- Authorization/ABAC: PASS — JIT requires step-up auth + scoped expiry + banner + audit; impersonation never covers DNA / consent / export raw / tenant deletion; feature flag override refused for `DNA_FORBIDDEN` / `CONSENT_FORBIDDEN` / `TENANT_ISOLATION_FORBIDDEN` / `AUDIT_FORBIDDEN` categories.
- Secret/PII/DNA leakage: PASS — 25 forbidden payload keys incl. raw DNA / raw FASTQ / BAM / VCF / raw email / phone / address / productionPii / treeViewerBypass / rawEventPayload / rawAuditStream.
- Dependency/config scan: PASS — Java 21 lockfile committed.

## Acceptance criteria

- [x] JIT support access: step-up auth + scoped expiry + banner + audit (R16.3).
- [x] Impersonation disabled by default + NEVER covers DNA / consent / export raw bundle / tenant deletion (R16.4).
- [x] DLQ replay requires snapshot + lineage hash; cross-tenant replay forbidden.
- [x] Projection rebuild tenant-scoped; cross-tenant rebuild forbidden.
- [x] Feature flag override requires reason + owner + audit; refuses DNA_FORBIDDEN / CONSENT_FORBIDDEN / TENANT_ISOLATION_FORBIDDEN / AUDIT_FORBIDDEN.
- [x] Tenant switch requires re-auth; audit export is one-shot signed URL that respects retention.
- [x] 23 admin operations incl. tenant suspend / reactivate / delete / purge + job / workflow / DLQ replay / projection rebuild.
- [x] 7 impermissible scopes enforced as a closed-set guardrail.
- [x] Linter + unit test + Java guard + Gradle build + checkstyle + boundary + ownership + lockfile all pass.

## Residual risks

- None. The Flyway migration + jOOQ admin tables + WebSocket JIT session + DLQ snapshot lineage hash collector land in the later E11.5 implementation subtasks.