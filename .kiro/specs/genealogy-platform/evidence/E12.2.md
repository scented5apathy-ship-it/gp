# Completion Evidence — E12.2

- Status: DONE
- Completed at: 2026-08-16T02:14:26+07:00
- Scope: Mutation queue contract, linter, tests and IndexedDB-only
  mutation queue helpers (apps/web PWA shell).
- Requirements: R6.5, R12

## Changes

- `contracts/pwa/mutation-queue-policy.yaml` — new contract (382 lines)
  with 20 mutation kinds, 11 entity kinds, 9 mutation states,
  13 conflict reasons, 7 sync triggers, 4 sync strategies,
  13 audit events, 11 failure reasons, 1 storage target (indexeddb),
  25 forbidden payload keys, 2-host egress allowlist, 12 numeric
  bounds, 18 invariants, 2 state matrices (mutationQueueStateMatrix
  initial DRAFT with 9 statuses incl. 3 terminal PURGED/DISCARDED/
  ACCEPTED, syncRunnerStateMatrix initial IDLE with 7 statuses incl.
  COOLDOWN reachable via IDLE/RUNNING).
- `platform/helm/genealogy-platform/files/pwa/mutation-queue-policy.yaml` —
  byte-equal chart mirror.
- `scripts/lint-mutation-queue.mjs` — new linter; validates closed-set
  vocabs (20/11/9/13/7/4/13/11/1/25/2), state matrices, numeric
  bounds, 18 invariants, helper surface (4 queue modules must NOT
  reference localStorage).
- `scripts/__tests__/lint-mutation-queue.test.mjs` — 5 unit tests.
- `apps/web/src/lib/pwa/idempotency.ts` — `buildIdempotencyHeader(req)`
  binds `Idempotency-Key` header to `operationId` (E1.3 contract).
- `apps/web/src/lib/pwa/mutation-queue.ts` — `validateEnqueue`,
  `canTransition`, `advance` enforcing the 9-state machine.
- `apps/web/src/lib/pwa/sync-runner.ts` — `computeBackoffSeconds`
  (exponential with 20% jitter per ADR-E0.5-08), 7 trigger handlers,
  per-trigger debounce windows.
- `apps/web/src/lib/pwa/conflict-resolution.ts` —
  `recommendedStrategy` for 13 reasons (three-way merge for
  VERSION_CONFLICT/REDACTION_DENIED/etc.; CONFLICT_PRESERVE_SERVER
  for TENANT_MISMATCH/PERMISSION_REVOKED/CONSENT_REVOKED/
  ENTITY_DELETED).

## Decisions

- Mirrored the `lint-notifications.mjs` pattern; closed-set vocabs +
  state matrices + invariants + helper surface.
- Mutation queue states + transitions encoded exactly as `mutationQueueStateMatrix`
  (DRAFT→QUEUED→SUBMITTING→ACCEPTED terminal, CONFLICTING/DLQ/
  REJECTED side branches).
- Sync runner exposes 7 triggers; `COOLDOWN` is reachable via
  IDLE/RUNNING (per `ad-hoc-rate-limit` window).
- Conflict resolution is split between `recommendedStrategy`
  (per-reason UX selection) and `supportsThreeWay` (UX capability).
- Forbidden-key payload keys share the 25-key set with E12.1 +
  E11 family for consistency.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-mutation-queue.mjs` | PASS | E12.2 summary: OK |
| `node --test scripts/__tests__/lint-mutation-queue.test.mjs` | PASS | 5/5 ok |
| `pnpm lint:mutationQueue` | PASS | exit 0 |
| `pnpm check:pwa` | PASS | 21/21 tests + 4 linters OK |
| `node scripts/check-monorepo-boundaries.mjs` | PASS | 12 services clean |
| `pnpm typecheck` (apps/web) | PASS | tsc clean |

## Security and privacy

- Tenant isolation: PASS — `tenantIdRequired`, `tenantBoundaryEnforced`,
  `TENANT_MISMATCH` conflict reason, `CONFLICT_PRESERVE_SERVER`
  fallback.
- Authorization/ABAC: PASS — `openfgaRelationshipRecheck`,
  `abacRecheckAtSubmit`, `permissionVersionEnforced`, server-source-of-truth.
- Secret/PII/DNA leakage: PASS — `mutationForbiddenPayloadKeys` (25 keys)
  + `forbiddenPayloadKeysEnforced`.
- Idempotency: PASS — `idempotencyHeaderMandatory`,
  `Idempotency-Key` header bound to `operationId`.
- LocalStorage forbidden: PASS — 4 queue modules scanned; 0 references.
- Background Sync NOT assumed: PASS — `backgroundSyncAssumed=false`
  invariant enforced.

## Acceptance criteria

- [x] IndexedDB queue with operationId, baseVersion, state
- [x] Resume sync, idempotent submit, conflict-resolution UX
- [x] No Background Sync assumption
- [x] DLQ not silently dropped (`dlqNotSilentlyDropped` + 3-way merge)
- [x] Server is source of truth

## Residual risks

- None