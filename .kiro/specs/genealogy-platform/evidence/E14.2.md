# Completion Evidence — E14.2

- Status: DONE
- Completed at: 2026-08-16T06:34:23+07:00
- Scope: Disaster recovery drill contract + Helm mirror +
  deep linter + Java guard + operator runbook + drill
  evidence anchors for 8 mandatory drill kinds.
- Requirements: NFR3 (DR SHALL be diễn tập định kỳ), NFR6
  (on-premise parity), NFR8 (re-use platform stack —
  Litmus / chaos-mesh / Argo CD / Kubernetes for region
  failover).

## Changes

- `contracts/disaster-recovery/drill-policy.yaml` — DR drill
  contract with 8 mandatory drill kinds
  (`cluster_loss`, `region_loss`, `dependency_outage`,
  `control_plane_failure`, `data_corruption`, `rpo_breach`,
  `rto_breach`, `on_premises_failover`), 5 allowed DR
  regions, 7 reconcile targets, 8 blast radii, 4 severities,
  closed-set `replayLogCaptureMode = redacted_metrics_only`,
  8 drill scenarios with RPO / RTO budget, mandatory
  artefacts, drill state matrix (9 statuses incl. 3
  terminal), 12 numeric bounds, 18 invariants, 7 capability
  boundaries, 32 forbidden keywords, 5 runtime helpers.
- `platform/helm/genealogy-platform/files/disaster-recovery/
  drill-policy.yaml` — byte-identical Helm mirror
  (15 152 bytes).
- `scripts/lint-drill-config.mjs` — deep validator:
  closed-set drill kinds (8), regions (5), reconcile
  targets (7), blast radii (8), replay mode (1 = closed),
  severities (4), drill scenarios (8), invariants (18),
  capability boundaries (7), forbidden keywords (32),
  runtime helpers (5 + on-disk existence), numeric bounds
  (12), state matrix (9 statuses incl. 3 terminal),
  byte-identity contract == chart, RPO / RTO budget per
  drill kind. 23 assertions PASS.
- `scripts/__tests__/lint-drill-config.test.mjs` — 5 unit
  tests (valid baseline, chart drift, missing drill kind,
  replay mode divergence, invariant drift).
- `services/operations-service/src/main/java/.../drill/
  E14DrillLimits.java` — closed-set catalogue + numeric
  bounds for the Java side.
- `services/operations-service/src/main/java/.../drill/
  DrillGuard.java` — pure orchestrator validating drill
  kind, regions, blast radius, reconcile targets, replay
  mode, required artefacts, severity, cadence, RPO / RTO
  budget per kind, drill state transitions.
- `services/operations-service/src/test/java/.../drill/
  DrillGuardTest.java` — 12 JUnit tests covering every
  drill kind, unknown kind, replay-mode forbidden,
  region-loss without secondary, on-premise failover
  without on-prem region, production-wide without flag,
  production-wide with flag accepted, reconcile targets
  below minimum, cadence out of range, RPO over budget,
  state transitions, terminal status.
- `runbook/disaster-recovery.md` — operator runbook for
  drill kinds, reconcile targets, replay capture, RPO /
  RTO budget, required artefacts, failure handling.
- `.kiro/specs/genealogy-platform/evidence/dr/
  cluster-loss.md`, `region-loss.md` — drill evidence
  anchors under the required path.

## Decisions

- ADR-E0.5-04 (Kong runtime) — control plane failure drill
  exercises Kong outage path; Kong holds runtime edge
  policy but does not own domain data.
- ADR-E0.5-07 (Temporal) — `temporal_workflow` reconcile
  target reuses the existing outbox / temporal plumbing;
  no custom reconciler introduced.
- ADR-E0.5-09 (Argo CD / Rollouts) — region failover uses
  Argo CD promotion; `production_wide` blast radius
  requires an explicit feature flag and product approval
  (matches E2.9 canary abort rules).
- NFR3 baseline — RPO ≤ 900 s for tight components,
  ≤ 3 600 s for kafka / on-prem, ≤ 86 400 s for control
  plane; RTO ≤ 14 400 s globally.

## Validation

| Command / check                                          | Result | Evidence |
| -------------------------------------------------------- | ------ | -------- |
| `node scripts/lint-drill-config.mjs`                     | PASS   | 23 assertions, E14.2 summary: OK |
| `node --test scripts/__tests__/lint-drill-config.test.mjs` | PASS | 5 / 5 tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:test --tests "com.genealogy.platform.services.operations.drill.*"` | PASS | 12 / 12 JUnit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:checkstyleMain :services:operations-service:checkstyleTest` | PASS | 0 violations |

## Security and privacy

- Tenant isolation: PASS — drill output captured via
  `redacted_metrics_only`; linter + guard reject
  `full_payload`, `aggregated_metrics_only`, `disabled` or
  any other mode.
- Authorization / ABAC: PASS — `production_wide` blast
  radius requires the explicit `productionWideFlagApproved`
  flag; region-loss drill requires at least one
  `gp-region-secondary-*` allowed region; on-premises
  failover requires an `onprem-customer-*` region.
- Secret / PII / DNA leakage: PASS — linter rejects every
  forbidden keyword (`raw_dna_bytes`, `production_pii`,
  `shared_admin_password`, `inline_jwt`, `bypass_authorization`,
  `skip_consent`, `skip_dna_isolation`, `skip_reconcile`,
  `skip_remediation`, `skip_signoff`, `ad_hoc_region_failover`,
  `manual_drill_only`, …).
- Dependency / config scan: PASS — every drill tooling is
  bound to the platform stack (Litmus / chaos-mesh / Argo
  CD / Kubernetes / outbox relay / Kafka / Temporal /
  otlp_audit); no custom DR coordinator introduced.

## Acceptance criteria

- [x] 8 mandatory drill kinds declared.
- [x] 5 allowed DR regions; manual / ad-hoc region list
      forbidden.
- [x] Reconcile targets cover outbox_relay, kafka_consumer,
      temporal_workflow, search_projection,
      public_projection, audit_pipeline, flagsmith_cache.
- [x] `replayLogCaptureMode=redacted_metrics_only` only;
      anything else is forbidden.
- [x] Required artefacts per drill = drillLog +
      reconcileReport + postmortem + remediation + signoff.
- [x] Severity ladder SEV1..SEV4 closed-set.
- [x] `production_wide` blast radius requires explicit flag
      + product approval; otherwise forbidden.
- [x] RPO / RTO budget per drill kind enforces closed
      caps.
- [x] Region-loss drill MUST declare a secondary region;
      on-premises failover MUST declare an on-prem region.
- [x] Drill cadence: 90 days SaaS / 180 days on-prem max.
- [x] Drill state matrix 9 statuses incl. 3 terminal,
      reachable from PLANNED via BFS.
- [x] Byte-identity between contract and Helm chart mirror
      (15 152 bytes).
- [x] 23 linter assertions + 5 linter unit tests + 12 Java
      tests all pass.
- [x] Checkstyle clean.

## Residual risks

- The remaining 6 of 8 drill evidence anchors (dependency
  outage, control plane failure, data corruption, RPO
  breach, RTO breach, on-premises failover) are not yet
  produced; only `cluster-loss.md` and `region-loss.md` are
  written in this epic. Each will be filled out during
  the actual quarterly drill under the cadence declared
  in the contract. The contract requires the evidence
  path to be on disk for the linter-required subset,
  which is satisfied.
- The `on_premises_failover` drill assumes the customer
  shares responsibility with the SRE team; the exact SLA
  is owned by the E14.5 operator documentation contract.