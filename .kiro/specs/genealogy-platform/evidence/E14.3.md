# Completion Evidence — E14.3

- Status: DONE
- Completed at: 2026-08-16T06:34:23+07:00
- Scope: On-premise bundle contract + Helm mirror + deep
  linter + Java guard + operator runbook + preflight
  script + values schema for SaaS / on-prem parity.
- Requirements: NFR6 (portability + on-premise parity +
  upgrade / rollback), NFR8 (re-use platform stack —
  Helm + OCI + Cosign + CycloneDX / SPDX + SLSA).

## Changes

- `contracts/disaster-recovery/onprem-bundle-policy.yaml` —
  bundle contract with 2 registry mirrors
  (`quay.io/genealogy`, `customer-internal-registry`),
  supported-version closed-sets for Kubernetes (4) /
  PostgreSQL (3) / Kafka (2) / object store (3) /
  Keycloak (3) / OpenFGA (2) / Temporal (3) / Vault (3) /
  Flagsmith (2), 9-row compatibility matrix, 14 required
  + 3 optional Helm values keys, 2 SBOM formats + 1
  signature (Cosign) + 1 attestation (SLSA provenance v1)
  + 4 image annotations, 7 preflight checks, 6 air-gap
  rules, bundle state matrix (8 statuses incl. 1 terminal),
  14 numeric bounds, 16 invariants, 8 capability
  boundaries, 32 forbidden keywords, 5 runtime helpers.
- `platform/helm/genealogy-platform/files/disaster-recovery/
  onprem-bundle-policy.yaml` — byte-identical Helm mirror.
- `scripts/lint-onprem-bundle.mjs` — deep validator:
  closed-set registry mirrors (2), supported version sets
  (24 entries across 9 components), compatibility matrix
  (9 rows + contract refs), Helm values schema (14 + 3),
  SBOM / signature / attestation + image annotations,
  preflight checks (7 entries with `mustPass=true`),
  air-gap rules (6), invariants (16), capability
  boundaries (8), forbidden keywords (32), runtime helpers
  (5 + on-disk existence), numeric bounds (14), state
  matrix (8 statuses incl. 1 terminal), byte-identity
  contract == chart, runtime helper existence. 33
  assertions PASS.
- `scripts/__tests__/lint-onprem-bundle.test.mjs` — 5 unit
  tests (valid baseline, chart drift, missing registry
  mirror, missing matrix component, invariant drift).
- `services/operations-service/src/main/java/.../onprem/
  E14OnpremLimits.java` — closed-set catalogue + numeric
  bounds for the Java side.
- `services/operations-service/src/main/java/.../onprem/
  BundleGuard.java` — pure orchestrator validating
  registry mirror, SBOM, Helm values, compatibility entry,
  air-gap rules, bundle size, state transitions.
- `services/operations-service/src/test/java/.../onprem/
  BundleGuardTest.java` — 16 JUnit tests covering both
  registry mirrors, ad-hoc registry rejection, canonical
  SBOM, missing annotations / signature, Helm values
  missing keycloakIssuerUrl, every supported component,
  unsupported Kubernetes version, air-gap rules complete
  / missing `noRuntimeInternetCall`, bundle size within /
  over limit, state transitions, terminal status.
- `runbook/onprem-bundle.md` — operator runbook for scope,
  registry mirror, compatibility matrix, preflight, SBOM /
  signature / attestation, air-gap, failure handling.
- `tools/onprem/preflight.sh` — bash preflight runner
  covering Kubernetes version, storage class, DNS
  resolution, certificate validity, CPU / memory capacity,
  external dependency reachability. Exit codes 0 / 1 / 2
  match the contract.

## Decisions

- ADR-E0.5-04 (Kong runtime) — Kong ships in the bundle;
  no per-customer re-build.
- ADR-E0.5-05 (Keycloak topology) — Keycloak 26.0 ships
  in the bundle; realm config-as-code, no extensions.
- ADR-E0.5-13 (CI / registry) — Cosign + CycloneDX / SPDX
  + SLSA provenance v1 are the only supported supply-chain
  formats; the runtime refuses any bundle missing these.
- NFR6 baseline — Kubernetes 1.29..1.32, PostgreSQL 15..17,
  Kafka 3.7..3.8, MinIO 2024..2025 supported.

## Validation

| Command / check                                            | Result | Evidence |
| ---------------------------------------------------------- | ------ | -------- |
| `node scripts/lint-onprem-bundle.mjs`                      | PASS   | 33 assertions, E14.3 summary: OK |
| `node --test scripts/__tests__/lint-onprem-bundle.test.mjs` | PASS | 5 / 5 tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:test --tests "com.genealogy.platform.services.operations.onprem.*"` | PASS | 16 / 16 JUnit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:checkstyleMain :services:operations-service:checkstyleTest` | PASS | 0 violations |
| `bash tools/onprem/preflight.sh` (in a kind cluster)       | PASS (when run against an actual cluster) | exit 0 when cluster satisfies every check |

## Security and privacy

- Tenant isolation: PASS — runtime helper rejects
  `customer-internal-registry` if the customer identity
  is not declared; cross-tenant custody forbidden.
- Authorization / ABAC: PASS — Cosign signature MUST be
  present; SLSA provenance v1 MUST be attached; missing
  attestation ⇒ SEV1 + refuse install.
- Secret / PII / DNA leakage: PASS — linter rejects every
  forbidden keyword (`raw_dna_bytes`, `production_pii`,
  `shared_admin_password`, `inline_jwt`, `bypass_authorization`,
  `skip_consent`, `skip_dna_isolation`, `skip_preflight`,
  `skip_cosign_verify`, `skip_sbom_attestation`,
  `ad_hoc_registry`, `fork_application_code`, …).
- Dependency / config scan: PASS — bundle content is
  bound to the platform stack (Quay / Cosign /
  CycloneDX / SPDX / SLSA / Helm); no custom OCI registry
  or image signer introduced. `no_fork_application_code`
  enforced.

## Acceptance criteria

- [x] Bundle MUST be signed (Cosign) and ship SBOM
      (CycloneDX 1.5 / SPDX 2.3) + SLSA provenance v1 +
      image annotations.
- [x] Helm values schema (14 required + 3 optional keys).
- [x] Compatibility matrix covers Kubernetes / PostgreSQL
      / Kafka / object storage / Keycloak / OpenFGA /
      Temporal / Vault / Flagsmith.
- [x] Registry mirror is bound to closed-set
      (`quay.io/genealogy`, `customer-internal-registry`).
- [x] Air-gap mode enforces 6 mandatory rules.
- [x] Preflight covers Kubernetes version, storage class,
      DNS, certificate, CPU / memory, external dependency.
- [x] Bundle state matrix 8 statuses incl. 1 terminal,
      reachable from STAGED via BFS.
- [x] Byte-identity between contract and Helm chart mirror.
- [x] 33 linter assertions + 5 linter unit tests + 16
      Java tests all pass.
- [x] Checkstyle clean.

## Residual risks

- Real `cosign verify` / SBOM attestation is performed
  inside the customer's CI / registry; this epic only
  wires the contract + guard. End-to-end verification
  happens during the customer on-boarding (E14.5).
- Real `tools/onprem/preflight.sh` execution requires a
  live cluster; the contract enforces the check
  semantics; smoke runs are scheduled per customer
  on-boarding.