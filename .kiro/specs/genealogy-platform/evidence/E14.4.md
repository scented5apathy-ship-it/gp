# Completion Evidence — E14.4

- Status: DONE
- Completed at: 2026-08-16T06:34:23+07:00
- Scope: Upgrade / rollback contract + Helm mirror + deep
  linter + Java guard + operator runbook + simulation
  script for Flyway expand-contract + Argo Rollouts abort.
- Requirements: NFR6 (upgrade SHALL có migration versioned,
  compatibility matrix, preflight + rollback plan), NFR8
  (re-use platform stack — Flyway + Argo Rollouts +
  Apicurio + Flagsmith).

## Changes

- `contracts/disaster-recovery/recovery-rollback-policy.yaml` —
  upgrade / rollback contract with 5 supported previous
  versions (`2025.10` / `2025.12` / `2026.02` / `2026.04` /
  `2026.06`), 6 Flyway migration kinds (expand-contract
  only), 4 API / event / schema compatibility kinds (BACKWARD
  / BACKWARD_TRANSITIVE / FULL / NONE_BREAKING_SUPERSEDED_BY_ADR),
  4 Argo Rollouts abort rule kinds (mirror E13.4), 6
  pre-upgrade + 7 post-upgrade checks, 6 rollback
  constraints, upgrade state matrix (10 statuses incl. 3
  terminal), 11 numeric bounds, 18 invariants, 7 capability
  boundaries, 31 forbidden keywords, 5 runtime helpers.
- `platform/helm/genealogy-platform/files/disaster-recovery/
  recovery-rollback-policy.yaml` — byte-identical Helm mirror.
- `scripts/lint-recovery-rollback-config.mjs` — deep
  validator: closed-set supported previous versions (5),
  migration kinds (6), compat kinds (4), abort rule kinds
  (4), pre-upgrade checks (6), post-upgrade checks (7),
  rollback constraints (6), invariants (18), capability
  boundaries (7), forbidden keywords (31), runtime helpers
  (5 + on-disk existence), numeric bounds (11), state
  matrix (10 statuses incl. 3 terminal), byte-identity
  contract == chart. 22 assertions PASS.
- `scripts/__tests__/lint-recovery-rollback-config.test.mjs` —
  5 unit tests (valid baseline, chart drift, missing
  migration kind, missing pre-check, invariant drift).
- `services/operations-service/src/main/java/.../recovery/
  E14RecoveryLimits.java` — closed-set catalogue + numeric
  bounds for the Java side.
- `services/operations-service/src/main/java/.../recovery/
  RecoveryGuard.java` — pure orchestrator validating
  version pairs, migration kinds (with destructive
  detection), compatibility kinds, pre / post checks,
  abort rule kinds, rollback constraints, state
  transitions.
- `services/operations-service/src/test/java/.../recovery/
  RecoveryGuardTest.java` — 21 JUnit tests covering every
  dimension above.
- `runbook/upgrade-rollback.md` — operator runbook for
  supported versions, migration contract, compatibility,
  pre / post checks, rollback constraints, Argo Rollouts
  abort, upgrade test coverage, failure handling.
- `tools/upgrade/simulate-upgrade.sh` — bash simulator that
  validates source / target versions, Flyway migrations
  for destructive operations, Argo Rollouts abort rules,
  schema / event compatibility.

## Decisions

- ADR-E0.5-08 (Kafka/Apicurio compatibility) — schema
  compatibility MUST be BACKWARD / BACKWARD_TRANSITIVE /
  FULL / NONE_BREAKING_SUPERSEDED_BY_ADR.
- ADR-E0.5-09 (Argo CD / Rollouts) — upgrade abort wires
  to the E13.4 canary abort rules; the linter enforces the
  wiring.
- ADR-E0.5-12 (Vault / KMS) — rollback runs the Flagsmith
  feature flag kill switch BEFORE swapping image; this
  matches the secrets-rotation expectation in E2.6.
- NFR6 baseline — Flyway expand-contract mandatory; every
  upgrade tested against the last 3 supported versions.

## Validation

| Command / check                                                    | Result | Evidence |
| ------------------------------------------------------------------ | ------ | -------- |
| `node scripts/lint-recovery-rollback-config.mjs`                   | PASS   | 22 assertions, E14.4 summary: OK |
| `node --test scripts/__tests__/lint-recovery-rollback-config.test.mjs` | PASS | 5 / 5 tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:test --tests "com.genealogy.platform.services.operations.recovery.*"` | PASS | 21 / 21 JUnit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:checkstyleMain :services:operations-service:checkstyleTest` | PASS | 0 violations |

## Security and privacy

- Tenant isolation: PASS — `noCrossTenantRollback` invariant
  enforced; rollback is tenant-scoped, max 1 active rollback
  per tenant.
- Authorization / ABAC: PASS — rollback requires an approval
  ticket AND feature flag kill switch; missing either
  ⇒ `FORBIDDEN`.
- Secret / PII / DNA leakage: PASS — linter rejects every
  forbidden keyword (`raw_dna_bytes`, `production_pii`,
  `shared_admin_password`, `inline_jwt`, `bypass_authorization`,
  `skip_consent`, `skip_dna_isolation`, `skip_precheck`,
  `skip_postcheck`, `skip_rollback_plan`,
  `skip_feature_flag_kill_switch`, `destructive_migration`,
  `breaking_change_without_adr`, …).
- Dependency / config scan: PASS — every migration /
  compat / abort / kill switch is bound to the platform
  stack (Flyway / Apicurio / Argo Rollouts / Flagsmith);
  no custom migration engine or progressive-delivery
  controller introduced.

## Acceptance criteria

- [x] Flyway expand-contract mandatory; destructive ops
      forbidden in release window.
- [x] API / event / schema compatibility BACKWARD /
      BACKWARD_TRANSITIVE / FULL / NONE_BREAKING.
- [x] 5 supported previous versions; min 3, max 5.
- [x] Argo Rollouts abort wired to E13.4 canary abort
      rules.
- [x] 6 pre-upgrade + 7 post-upgrade checks declared.
- [x] Rollback constraints enforced: max 1 per tenant,
      no cross-tenant, approval ticket, feature flag kill
      switch.
- [x] Upgrade state matrix 10 statuses incl. 3 terminal,
      reachable from PLANNED via BFS.
- [x] Byte-identity between contract and Helm chart mirror.
- [x] 22 linter assertions + 5 linter unit tests + 21
      Java tests all pass.
- [x] Checkstyle clean.

## Residual risks

- `tools/upgrade/simulate-upgrade.sh` greps for
  `DROP TABLE|COLUMN|INDEX` patterns. Customers with
  case-sensitive DDL (e.g. quoted identifiers) may need
  to amend the regex; this is documented in the runbook.
- Real upgrade dry-runs against production-like datasets
  are scheduled per release cadence; this epic only wires
  the contract + simulator. End-to-end verification is
  part of the customer on-boarding (E14.5).