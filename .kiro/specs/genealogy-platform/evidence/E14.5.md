# Completion Evidence — E14.5

- Status: DONE
- Completed at: 2026-08-16T06:34:23+07:00
- Scope: Operator documentation contract + Helm mirror +
  deep linter + Java guard + runbook index + support
  bundle collector + shared-responsibility matrix for
  SaaS / on-prem parity.
- Requirements: NFR6 (on-premise parity + shared
  responsibility), NFR8 (every platform SHALL have
  owner + version policy + Helm/config-as-code +
  backup/restore + monitoring + security hardening +
  upgrade/rollback + runbook), R16 (admin / audit /
  support access).

## Changes

- `contracts/disaster-recovery/operator-runbook-policy.yaml`
  — operator runbook contract with 8 mandatory procedures
  (`install`, `configuration`, `scaling`, `backup`,
  `restore`, `key_rotation`, `troubleshooting`,
  `support_bundle`), 8 owner roles, 4 severity levels,
  5 support channels, 5 on-call rotations, 10 support
  bundle redactions, 13 shared-responsibility areas,
  8 procedure definitions with owner / severity /
  evidenceAnchor / runbookPath / redactionRequirements,
  runbook state matrix (5 statuses incl. 1 terminal),
  12 numeric bounds, 17 invariants, 5 capability
  boundaries, 29 forbidden keywords, 5 runtime helpers.
- `platform/helm/genealogy-platform/files/disaster-recovery/
  operator-runbook-policy.yaml` — byte-identical Helm mirror.
- `scripts/lint-operator-runbook.mjs` — deep validator:
  closed-set mandatory procedures (8), owner roles (8),
  severity levels (4), support channels (5), on-call
  rotations (5), support bundle redactions (10),
  procedures (8 + all required fields), shared
  responsibility matrix (13 areas + closed-set owners),
  invariants (17), capability boundaries (5), forbidden
  keywords (29), runtime helpers (5 + on-disk existence),
  numeric bounds (12), state matrix (5 statuses incl. 1
  terminal), byte-identity contract == chart. 22
  assertions PASS.
- `scripts/__tests__/lint-operator-runbook.test.mjs` —
  5 unit tests (valid baseline, chart drift, missing
  procedure, missing redaction, invariant drift).
- `services/operations-service/src/main/java/.../runbook/
  E14RunbookLimits.java` — closed-set catalogue + numeric
  bounds for the Java side.
- `services/operations-service/src/main/java/.../runbook/
  RunbookGuard.java` — pure orchestrator validating
  procedures, support bundle redactions, shared
  responsibility matrix, support channels, bundle size,
  state transitions.
- `services/operations-service/src/test/java/.../runbook/
  RunbookGuardTest.java` — 20 JUnit tests covering every
  dimension above.
- `runbook/index.md` — operator runbook index linking
  every procedure to its runbook + evidence anchor, with
  SLA matrix, support channels, on-call rotations, shared
  responsibility matrix and review cadence.
- `tools/support/support-bundle.sh` — bash collector
  producing a redacted support bundle with the closed-set
  of 10 redaction rules applied + bundle size cap.

## Decisions

- ADR-E0.5-04 (Kong runtime) — Kong + Keycloak are
  platform-managed; documented in the shared-responsibility
  matrix.
- ADR-E0.5-12 (Vault / KMS) — key rotation procedure
  owner = `security_engineer`; cadence 90 days.
- ADR-E0.5-13 (CI / registry) — support bundle redacts
  SBOM and SBOM-related raw payloads.
- NFR8 baseline — every platform component MUST have a
  documented runbook procedure; review cadence 90 days;
  review overdue ⇒ SEV2.

## Validation

| Command / check                                              | Result | Evidence |
| ------------------------------------------------------------ | ------ | -------- |
| `node scripts/lint-operator-runbook.mjs`                     | PASS   | 22 assertions, E14.5 summary: OK |
| `node --test scripts/__tests__/lint-operator-runbook.test.mjs` | PASS | 5 / 5 tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:test --tests "com.genealogy.platform.services.operations.runbook.*"` | PASS | 20 / 20 JUnit tests |
| `JAVA_HOME=ms-21.0.12 ./gradlew :services:operations-service:checkstyleMain :services:operations-service:checkstyleTest` | PASS | 0 violations |

## Security and privacy

- Tenant isolation: PASS — shared-responsibility matrix
  binds every component to either `customer_managed` or
  `platform_managed`; no ad-hoc ownership.
- Authorization / ABAC: PASS — support bundle redacts
  every forbidden pattern (secrets, PII, DNA, raw
  payloads, JWT, session cookie, OAuth client secret,
  audit stream, consent receipt, treeViewerBypass); the
  platform refuses to publish a bundle missing any rule.
- Secret / PII / DNA leakage: PASS — linter rejects
  every forbidden keyword (`raw_dna_bytes`,
  `production_pii`, `shared_admin_password`, `inline_jwt`,
  `bypass_authorization`, `skip_consent`,
  `skip_dna_isolation`, `skip_runbook_review`,
  `ad_hoc_contact`, …).
- Dependency / config scan: PASS — every runbook
  tool is bound to the platform stack (PagerDuty /
  OpsGenie / runbook index); no custom support bundle or
  on-call contact introduced.

## Acceptance criteria

- [x] 8 mandatory procedures documented (install /
      configuration / scaling / backup / restore /
      key_rotation / troubleshooting / support_bundle).
- [x] Every procedure has owner / severity /
      evidenceAnchor / runbookPath / redactionRequirements.
- [x] Review cadence 90 days enforced; overdue ⇒
      `STATE_STALE`.
- [x] 5 support channels + 5 on-call rotations from
      closed-set.
- [x] 13 shared-responsibility areas with closed-set
      owner tag.
- [x] Support bundle redacts every closed-set pattern
      (secrets / PII / DNA / raw payloads / JWT / cookie
      / OAuth secret / audit stream / consent receipt /
      treeViewerBypass).
- [x] Runbook state matrix 5 statuses incl. 1 terminal,
      reachable from DRAFT via BFS.
- [x] Byte-identity between contract and Helm chart mirror.
- [x] 22 linter assertions + 5 linter unit tests + 20
      Java tests all pass.
- [x] Checkstyle clean.

## Residual risks

- Real `tools/support/support-bundle.sh` execution
  requires the customer's log / dashboard / audit paths;
  the contract enforces the redaction rules + bundle size
  cap; smoke runs are scheduled per customer on-boarding.
- Service-specific runbook stubs (tenant-service.md,
  research-service.md, audit-service.md) are referenced
  from `runbook/index.md` but are not fully fleshed out
  in this epic; they will be populated during the next
  iteration of E3 / E6 / E16 hardening.