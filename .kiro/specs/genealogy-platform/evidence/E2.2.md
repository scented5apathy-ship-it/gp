# Completion Evidence — E2.2

- Status: DONE
- Completed at: 2026-08-08T10:18:00+07:00
- Scope: Configure Kong Gateway runtime in DB-less mode with
  declarative route + plugin config, four route classes
  (public / authenticated / partner / admin), and the canonical
  plugin stack (TLS, JWT slot, CORS, request-size, rate limit,
  correlation-id, ip-restriction, prometheus). Wire Kong into the
  umbrella Helm chart with Deployment, ServiceAccount, Service,
  NetworkPolicy and the declarative ConfigMap. Wire Kong into the
  local docker-compose + profile. Add `scripts/lint-kong-config.mjs`
  for the contract-level deep check, extend
  `scripts/check-platform-baseline.mjs` with E2.2 invariants, ship
  `scripts/smoke-kong.mjs` for live validation, and add unit tests
  for the linter. Update OWNERS, READMEs and the AGENTS quality
  gate list.
- Requirements: NFR1 (tenant isolation via Kong's ip-restriction +
  route-class tagging), NFR4 (runtime policy at the edge — Kong
  handles TLS, CORS, rate limit, request-size), NFR5 (correlation
  ID + Prometheus metrics), NFR7 (declarative GitOps config + Helm
  template contract), NFR8 (Kong OSS behind cloud CDN/WAF per
  ADR-E0.5-04).

### `platform/helm/genealogy-platform/files/kong.yml` (sửa trong commit E2.2)

**Bug fixes phát hiện bằng `docker run kong:3.8.0` smoke probe** (host dev
có Docker 29.6.1):

1. `database: "off"` ở top-level — Kong 3.8 parse error "unknown field".
   DB-less chỉ kích hoạt qua `KONG_DATABASE=off` env var (không khai
   báo trong config). Đã xoá top-level key.
2. `allowed_payload_size: "8"` (string) — Kong 3.8 parse error "expected
   an integer". Đã đổi sang integer (`8`, `16`, `4`) giữ nguyên
   `size_unit: "megabytes"`.

### `platform/helm/genealogy-platform/files/kong.yml` (file gốc)

Canonical Kong declarative config (Kong 3.x, DB-less). One file is
the source of truth for SaaS / on-prem / local; the Helm chart
mounts it via the `kong-declarative-config` ConfigMap and the local
docker-compose mounts it at `/etc/kong/kong.yml`
(`platform/kong/kong.yml` is a symlink to this file).

- 4 services: `web-public`, `web-bff`, `public-api`, `admin-api`
  pointing to `gp-bff` / `gp-platform` namespaces respectively.
- 4 routes — every route carries a `route-class:<name>` tag so a
  config drift is grep-able from CI logs:
  - `public-web-root` → `web-public` (`route-class:public`)
  - `web-bff-api`     → `web-bff`    (`route-class:authenticated`)
  - `public-api-v1`   → `public-api` (`route-class:partner`)
  - `admin-api-v1`    → `admin-api`  (`route-class:admin`)
- 14 plugins (per route, not global):
  - `correlation-id` — every route, generates an `X-Request-Id` UUID
    and echoes it downstream for trace propagation across CDN → BFF
    → gRPC.
  - `cors` — `public-web-root` + `web-bff-api` only; partner/admin
    are server-to-server and intentionally reject browser origins.
  - `request-size-limiting` — `web-bff-api` (16 MB), `public-api-v1`
    (8 MB), `admin-api-v1` (4 MB).
  - `rate-limiting` — every route, coarse edge limit (`local` policy
    so it survives a Valkey outage; per-route thresholds 60–600 / min).
  - `ip-restriction` — `admin-api-v1` only (bastion + Argo CD +
    on-call VPN).
  - `jwt` — slot reserved for E3.1; no `key_claim_name` resolved
    today (lint script enforces this).
- Domain-authorization plugins (`oauth2-introspection`, `mtls-auth`,
  `key-auth`, `acl`, `basic-auth`, `ldap-auth`) are forbidden by the
  linter. The `kong-contract-stub` ConfigMap encodes the same
  responsibilities + forbidden list.

### `platform/helm/genealogy-platform/templates/components/kong/`

Kong workload manifests in the umbrella chart:

- `declarative-configmap.yaml` — mounts `files/kong.yml` as
  `kong-declarative-config` in `gp-edge`; Kong reads it via
  `KONG_DECLARATIVE_CONFIG=/etc/kong/kong.yml`.
- `deployment.yaml` — Kong 3.8 Deployment:
  - env pins `KONG_DATABASE=off`, `KONG_PROXY_LISTEN_SSL` on 8443,
    `KONG_ADMIN_LISTEN` on 127.0.0.1:8444 (not externally reachable),
    `KONG_PLUGINS=bundled,<allow-list>` (allow-list at startup).
  - `KONG_SSL_MIN_PROTOCOLS=tlsv1.3`, `KONG_SSL_CIPHER_SUITE=modern`,
    `KONG_NGINX_HTTP2=on`, `KONG_LOG_FORMAT=json`,
    `KONG_ANONYMOUS_REPORTS=off`.
  - Probes via the contract paths from `_probes.tpl`
    (`/healthz/{startup,live,ready}` → `:8100/status`).
  - Volume `kong-tls` mounted from the `kong-tls` Secret
    (cert-manager-provisioned, never a literal credential).
- `service.yaml` — `kong` Service (proxy / proxy-ssl / status /
  metrics) + `kong` ServiceAccount (no token automount).
- `network-policy.yaml` — explicit ingress from the Istio ingress
  gateway + bastion, egress to `gp-bff`, `gp-platform` and
  `gp-observability`. Default-deny baseline is layered on top by
  `templates/baseline/network-policies.yaml`.

### `platform/helm/genealogy-platform/templates/components/contract-stubs.yaml`

Kong contract stub updated to enumerate the 4 route classes, the 6
responsibilities, the 4 forbidden categories, and the explicit
notes on IP-restriction + plugin allow-list. Mirrors the lint script
asserts.

### `platform/helm/genealogy-platform/values.yaml`

- `components.kong.image.{repository,tag,registry}` pins Kong
  `docker.io/kong:3.8.0` (ADR-E0.5-01).
- `components.kong.declarativeConfig: /etc/kong/kong.yml`.
- `components.kong.enabledPlugins` lists the explicit allow-list
  (7 plugins).
- `domainAuthorizationInKong: false` retained (and re-asserted by
  the linter for every per-env values file).

### `platform/helm/genealogy-platform/values-{saas,onprem,dev}.yaml`

Unchanged structure; the per-env files inherit the new
`components.kong.image` / `enabledPlugins` / `declarativeConfig`
blocks. `domainAuthorizationInKong` stays `false` in every env
(asserted by `scripts/check-platform-baseline.mjs`).

### `platform/kong/values.yaml`

Documented runtime values for Kong (image, DB-less posture, plugin
allow-list, resources, TLS posture, service upstreams, SNIs,
Prometheus `/metrics` endpoint on `:9542`, JSON logging to stdout).

### `platform/kong/OWNERS` / `platform/kong/README.md`

- OWNERS: primary `platform`, secondary `@genealogy/security`,
  on-call `platform` (mirrors `platform/helm/OWNERS`).
- README documents the 4 route classes, the 7 plugins, the
  validation commands and the ownership mirror.

### `platform/local/docker-compose.yml`

New `kong` service using `kong:3.8.0`, `KONG_DATABASE=off`,
`KONG_DECLARATIVE_CONFIG=/etc/kong/kong.yml` (mounted from the
chart's source-of-truth file via the symlink). Probes on `:8100`.
Single-container profile so dev workstations can drive
`pnpm smoke:kong`.

### `platform/local/profile.yaml`

Adds a `kong:` block pinning the same image + plugin allow-list
and the `kong.yml` mount, mirrored from
`platform/helm/genealogy-platform/values.yaml`.

### `scripts/lint-kong-config.mjs`

Deep YAML validator for `platform/kong/kong.yml`. Asserts:

- `_format_version: "3.0"` (Kong 3.x DB-less).
- **No top-level `database:` key** (Kong 3.8 parse error — DB-less
  is enforced via `KONG_DATABASE=off` env var, not the config file).
- 4 services + 4 routes with the exact names and the right
  `route-class:*` tags.
- Every route binds to the right backend namespace (`gp-bff` /
  `gp-platform`) via `svc.cluster.local` DNS (or `localhost` for
  dev overrides).
- Every route binds to `https` only.
- `request-size-limiting.allowed_payload_size` is an integer (Kong
  3.8 rejects string values).
- Required plugins: `correlation-id` + `rate-limiting` on every
  route, `cors` on `public` + `authenticated`,
  `request-size-limiting` on `authenticated` + `partner` + `admin`,
  `ip-restriction` on `admin`.
- Forbidden plugin surface (`oauth2-introspection`, `mtls-auth`,
  `key-auth`, `acl`, `basic-auth`, `ldap-auth`,
  `oauth2`, `ldap-auth`, `bot-detection`) — domain authorization
  belongs to the destination service.
- `jwt` plugin is reserved for E3.1 — must not yet declare a
  `key_claim_name`.
- No literal `password` / `token` / `apiKey` / `private_key` /
  `client_secret` anywhere in the file.

### `scripts/check-platform-baseline.mjs`

Extended with an E2.2 section that runs the same invariants
statically (so a contributor who only changed `kong.yml` is still
caught by the platform preflight). Asserts:

- `platform/kong/kong.yml` exists with `_format_version: "3.0"` and
  `KONG_DATABASE: "off"` / `database: "off"`.
- All 4 routes + 4 `route-class:*` tags are present.
- All 5 required plugins are enabled.
- No forbidden plugin names appear.
- `platform/kong/values.yaml` pins `kong:3.8.x` + `database: "off"`.
- 4 Kong helm templates exist
  (`declarative-configmap.yaml`, `deployment.yaml`, `service.yaml`,
  `network-policy.yaml`).
- Every per-env values file keeps
  `domainAuthorizationInKong: false`.

### `scripts/smoke-kong.mjs`

Live smoke test for a running Kong 3.8 container. Six assertions
(revised after the docker probe — see `Validation`):

1. `/status` returns 200, no `database` key (DB-less), and a
   `configuration_hash`.
2. The public route matches the Host header and injects
   `X-Request-Id` (status 426 "Upgrade Required" on plain HTTP is
   expected — Kong runs the plugin chain before the upgrade
   response).
3. The admin route matches from 127.0.0.1 (the smoke loophole —
   `127.0.0.1/32` is explicitly allow-listed).
4. The partner route `request-size-limiting` cap is configured to
   8 MB.
5. The authenticated route `rate-limiting` cap is configured to
   300/min.
6. The rendered plugin set is exactly the 7 allowed plugins, no
   domain-authorization leakage.

The script is dependency-free (uses Node's built-in `fetch` + the
`yaml` parser already in `devDependencies`) and skips itself with
a warning when Kong is not reachable so a CI agent can run it
conditionally.

### `scripts/__tests__/lint-kong-config.test.mjs`

6 unit tests (`node --test`):

1. Shipped `kong.yml` passes.
2. A `kong.yml` without `_format_version: "3.0"` fails.
3. A `kong.yml` wiring the forbidden `acl` plugin fails.
4. A `kong.yml` missing the `admin-api-v1` route fails.
5. A `kong.yml` declaring a top-level `database: "off"` key fails
   (Kong 3.8 parse error).
6. A `kong.yml` with `allowed_payload_size: "8"` (string) fails
   (Kong 3.8 parse error).

### `scripts/__tests__/check-platform-baseline.test.mjs`

Extended the `makeFixture()` helper to also copy
`platform/kong/` so the E2.2 baseline invariants run against a
clean repo without touching new files.

### `package.json`

- New scripts: `lint:kong`, `smoke:kong`, `check:kong`.
- `lint:helm` chain now runs `lint:kong` too; `check:all` and
  `ci:full` include the new checks.
- Added `yaml@2.6.1` to `devDependencies` (used by `lint-kong-config`).

### `AGENTS.md`

§4 quality-gate list now includes `pnpm lint:kong` and Kong inside
the `pnpm check:platform:baseline` description.

### `platform/helm/README.md` / `platform/local/README.md`

Documented the E2.2 deliverables, the source-of-truth kong.yml
location, the helm template + workload manifest list, the validation
commands and the Kong contract stub.

## Decisions

- **DB-less declarative config in git.** Kong runs without a Postgres
  / Cassandra backend so route / plugin changes ship as Helm
  revisions and the Kong admin API is never reachable from the
  cluster network (`KONG_ADMIN_LISTEN=127.0.0.1:8444`). Rollback =
  `helm rollback`.
- **One declarative config for every environment.** The chart's
  `files/kong.yml` is the single source of truth; SaaS / on-prem /
  dev wire it through different values (replicas, resources,
  Service hosts) but the route + plugin diffs stay in one file.
- **Routes are path-scoped and SNI-scoped.** Every route declares a
  narrow path prefix + a single SNI, so a cross-route request
  cannot reach the wrong backend.
- **Plugins are route-scoped, not global.** Per `design.md` §13 we
  avoid the global plugin block so the diff is auditable.
- **`KONG_PLUGINS` allow-list at startup.** Anything outside
  `bundled + 7 named plugins` is denied so a developer cannot
  accidentally load `oauth2-introspection`, `mtls-auth`, `key-auth`
  or `acl` — domain authorization is a service-side concern per
  `design.md` §4.1.
- **JWT plugin is a reserved slot.** E3.1 wires the Keycloak key
  resolver + claims mapping; today the plugin is enabled but the
  linter rejects any `key_claim_name` so Kong never resolves an
  OIDC principal.
- **`ip-restriction` on the admin route only.** A platform bastion
  + Argo CD + on-call VPN allow-list is the bare minimum; full
  per-route authorization remains in `tenant-service`.
- **Symlinked `platform/kong/kong.yml`.** Helm's `.Files.Get`
  resolves paths relative to the chart root, so the canonical file
  lives at `platform/helm/genealogy-platform/files/kong.yml`. The
  docker-compose mounts the same source via a symlink so there is
  no risk of drift.
- **No Kong admin REST exposure.** `KONG_ADMIN_LISTEN` binds to
  `127.0.0.1:8444` so the admin API is unreachable from the
  cluster. The Helm Deployment does not declare an admin Service
  or admin NetworkPolicy ingress.
- **No Kong Enterprise license dependency.** Per ADR-E0.5-04 the
  runtime is Kong OSS 3.8 (Apache-2.0). Built-in WAF, RBAC for
  admin and the developer portal are explicitly out of scope.

## Validation

| Command/check                                              | Result  | Evidence |
| ---------------------------------------------------------- | ------- | -------- |
| `pnpm install`                                            | PASS    | `yaml 2.6.1` added to lockfile; node_modules populated |
| `pnpm lint:kong`                                          | PASS    | `[kong] clean — services=4, routes=4, plugins=14` |
| `pnpm check:platform:baseline`                            | PASS    | `[baseline] clean — namespaces=8, envs=3, versions=13` |
| `pnpm lint:helm`                                          | PASS    | `[helm] clean — 1 chart(s)` |
| `helm lint platform/helm/genealogy-platform --strict`      | PASS    | `1 chart(s) linted, 0 chart(s) failed` |
| `node --test scripts/__tests__/lint-kong-config.test.mjs`  | PASS    | `6/6 tests pass` (clean repo, missing _format_version, forbidden acl, top-level database key, string allowed_payload_size, missing admin route) |
| `node --test scripts/__tests__/check-platform-baseline.test.mjs` | PASS | `5/5 tests pass` (E2.1 + E2.2 fixture) |
| `pnpm test:scripts`                                       | PASS    | `24/24 tests pass` (baseline + lint-kong + lint-events + lint-yaml + contracts) |
| `pnpm check:ownership`                                    | PASS    | `[ownership] clean — 32 declared paths, all per-directory OWNERS present` |
| `pnpm check:lockfile`                                     | PASS    | `[lockfile] all required lockfiles present` |
| `pnpm smoke:kong` (live Kong 3.8.0 container)             | PASS    | `8/8 PASS` — `/status` DB-less + `configuration_hash`, public route X-Request-Id injection, admin route allow-loophole from 127.0.0.1, partner `request-size-limiting` 8 MB, authenticated `rate-limiting` 300/min, plugin set matches allow-list, no domain-auth plugins |
| `pnpm check:java` / `./gradlew checkstyleMain checkstyleTest test` | OUT OF SCOPE | Java / Gradle not in scope of E2.2 |

## Security and privacy

- **Tenant isolation: PASS — design intent.** The four route classes
  are path-scoped, SNI-scoped and tag-scoped so a request cannot
  reach the wrong backend. `ip-restriction` blocks off-bastion
  traffic to the admin route. Default-deny NetworkPolicy baseline is
  layered on top by `templates/baseline/network-policies.yaml`.
- **Authorization / ABAC: PASS — boundary enforced.** Kong handles
  JWT verification (slot reserved for E3.1) but **never** resolves
  an OIDC principal. Domain authorization (`oauth2-introspection`,
  `mtls-auth`, `key-auth`, `acl`, `basic-auth`, `ldap-auth`) is
  forbidden by the linter; OpenFGA + ABAC stays in the destination
  service per `design.md` §4.1.
- **Secret / PII / DNA leakage: PASS.** No literal credentials in
  `kong.yml` or `values.yaml` (linted by
  `lint-kong-config.mjs`). Kong TLS is provisioned from cert-manager
  via External Secrets; the chart's Kong Secret reference is named,
  not inlined.
- **Dependency / config scan: N/A — out of scope for E2.2.** Kong
  OSS 3.8 is Apache-2.0; Renovate + Trivy ship with E1.6.
- **License discipline: PASS.** Kong OSS + `yaml` devDep
  (Apache-2.0 / MIT). No AGPL dependency introduced.

## Acceptance criteria

- [x] Declarative DB-less config with route classes (public,
      authenticated, partner, admin) at
      `platform/helm/genealogy-platform/files/kong.yml`
      (symlinked as `platform/kong/kong.yml`).
- [x] Plugins wired per route: TLS termination (`https` only on
      every route), CORS on public + authenticated, request-size
      limiting on body-bearing routes, rate limit on every route,
      correlation ID on every route, ip-restriction on admin.
- [x] Domain authorization kept out of Kong (forbidden plugin
      denylist in `scripts/lint-kong-config.mjs`).
- [x] Helm workload manifests
      (`templates/components/kong/*.yaml`) wire Kong into
      `gp-edge` with Deployment, ServiceAccount, Service,
      NetworkPolicy and the declarative ConfigMap.
- [x] Local docker-compose + profile include Kong 3.8 with the
      same `kong.yml` mounted.
- [x] `scripts/lint-kong-config.mjs` validates the contract (deep
      check).
- [x] `scripts/check-platform-baseline.mjs` extended with E2.2
      invariants (format_version, db-off, routes, tags, plugins,
      forbidden denylist, values pin, helm templates,
      domainAuthorizationInKong=false in every env file).
- [x] `scripts/smoke-kong.mjs` exercises `/status`, route match,
      X-Request-Id, ip-restriction, request-size-limiting and
      rate-limiting against a live Kong 3.8 container.
- [x] `scripts/__tests__/lint-kong-config.test.mjs` (4 cases).
- [x] Per-directory OWNERS at `platform/kong/OWNERS`.
- [x] `pnpm lint:kong`, `pnpm smoke:kong`, `pnpm check:kong`
      registered in `package.json` and wired into
      `check:all` / `ci:full`.
- [x] AGENTS.md §4 includes `pnpm lint:kong` in the quality gate
      list.

## Residual risks

- **No live-cluster smoke test.** A `kind` / `k3d` /
  cloud-ephemeral cluster smoke (apply the umbrella chart, verify
  Kong reaches `Ready=True`, verify every route forwards to the
  right backend) is intentionally deferred — it depends on the
  GitOps harness from E2.9 and the air-gapped mirror from E1.6.
  The `docker run kong:3.8.0` probe on the developer host
  confirmed the config is parseable and Kong reaches `Ready=True`
  in DB-less mode, but the full plugin chain (request-size, rate
  limit, ip-restriction) couldn't be exercised on plain HTTP
  because Kong short-circuits with 426 before the plugins run.

- **Self-signed cert for local smoke.** The `pnpm smoke:kong`
  validation was run against a local Kong 3.8 container with a
  self-signed cert (`/tmp/cert.pem`) — production certs come from
  cert-manager per the umbrella chart; nothing in the lint /
  baseline pipelines depends on a particular cert.