# Completion Evidence — E4.4

- Status: DONE
- Completed at: 2026-08-10T17:55:00+07:00
- Scope: Relationship graph and invariants (R3 / R4 / R4.4 / R8 / R10 / R18 / NFR1 / NFR4 / NFR7)
- Requirements: R3 (tenant), R4 (person + relationship), R4.4 (certainty + provenance), R8 (collaboration), R10 (living privacy), R18 (accessibility); NFR1 (defence-in-depth), NFR4 (privacy-first), NFR7 (audit), NFR8 (observability)

## Changes

| Layer | Path | Purpose |
| --- | --- | --- |
| Contract | `contracts/genealogy/relationship-graph-policy.yaml` | Source-of-truth for closed-sets, audit hooks, conflict policy |
| Chart mirror | `platform/helm/genealogy-platform/files/relationship-graph-policy.yaml` | Byte-identical (verified via `diff -q`) |
| Domain Java | `RelationshipKind.java`, `PartnerSubKind.java`, `ParticipantRole.java`, `ProvenanceStatus.java`, `RelationshipConflictCode.java`, `RelationshipId.java`, `ParticipantRef.java`, `TemporalValidity.java`, `Relationship.java`, `RelationshipInvariants.java` | Aggregate + value objects + invariant service |
| Flyway | `services/genealogy-service/src/main/resources/db/migration/V5__relationship.sql` | `tree_service.relationship` + `tree_service.relationship_participant` with CHECK + RLS |
| Linter | `scripts/lint-relationship-config.mjs` | Validates policy + mirror |
| Linter test | `scripts/__tests__/lint-relationship-config.test.mjs` | 7/7 PASS |
| Domain tests | `RelationshipTest.java`, `RelationshipInvariantsTest.java` | 31 tests (20 + 11) PASS |
| Wiring | `package.json` | `lint:relationship` + `check:relationship`; chained into `check:platform` + `ci:full` |

## Decisions

- Mirrored `design.md` §5.2: `RELATIONSHIP + participant/role` instead of denormalised `father_id` / `mother_id` columns.
- Closed-sets: 12 `relationshipKinds` (incl. CUSTOM + GODPARENT for cultural lineage terms), 8 `partnerSubKinds`, 7 `participantRoles`, 4 `certainties` (shared with E4.3), 4 `provenanceStatuses` (USER_ENTERED / IMPORTED / VERIFIED_BY_SOURCE / CORRECTION).
- `cyclePolicy = deny` and `selfLinkPolicy = deny` (hard deny — renderer cannot recover from a malformed graph).
- `chronologicalConflictPolicy = warn-only` (overlap is soft-warning; never hard-deny — legitimate biology like twins must work).
- `partnerOverlapPolicy = allow-with-validity` (sequential / polygamous unions supported via `validFrom` / `validUntil`).
- `unknownParticipantLabel = UNKNOWN` (synthetic participant for missing parents; personId-null + unknown-true marker).
- Audit hooks: `consent` class on every relationship mutation (privacy surface changes — R10).
- Provider-neutral: authority references stay opaque; Person / Date / Place reuse from E4.2 / E4.3.
- Assumptions: None (all closed-set values are derivable from the contract).

## Validation

| Command | Result |
| --- | --- |
| `node scripts/lint-relationship-config.mjs` | PASS |
| `node --test scripts/__tests__/lint-relationship-config.test.mjs` | PASS — 7/7 |
| `node scripts/check-monorepo-boundaries.mjs` | PASS — 11 services clean |
| `node scripts/check-ownership-coverage.mjs` | PASS — 37 paths |
| `node scripts/lint-events.mjs` | PASS — 25 schemas, no regression |
| `node scripts/lint-{person,tree,dateplace}-config.mjs` | PASS — no regression |
| `./gradlew :services:genealogy-service:test` | PASS — 130 tests (99 cũ + 20 Relationship + 11 RelationshipInvariants) |
| `./gradlew :services:genealogy-service:checkstyleMain checkstyleTest` | PASS — 0 violations |
| Prettier --check (new files) | PASS |
| `diff -q contract == chart-mirror` | PASS — byte-identical |

## Security and privacy

- Tenant isolation: PASS — `tree_service.relationship` has `tenant_id` + FORCE RLS; `tree_service.relationship_participant` inherits via parent EXISTS subquery.
- Authorization/ABAC: PASS — `Relationship.createdBy` opaque; `Person ↔ Relationship` link via opaque `personId` only; OpenFGA allow + ABAC obligations (R10 living-link redaction) layered on top by E3.4.
- Secret/PII/DNA leakage: PASS — forbidden-token scan clean; `customLabel` capped at 256 chars; no literal DNA / PII.
- Cycle / self-link prevention: PASS — `RelationshipInvariants.checkCycle` DFS from any new PARENT through CHILD edges; rejects any closed loop with `Severity.DENY` + `RelationshipConflictCode.CYCLE`.
- Living-link redaction: PASS — `spec.livingLinkRedactsByDefault = true` (renderer + projection reads this flag).
- Audit: PASS — audit hooks anchored in contract for `relationship.created` / `.updated` / `.ended` / `.disputed`; relay implementation is E4.7.

## Acceptance criteria

- [x] Biological / adoptive / foster / guardian / step / partner / custom kinds supported (12 kinds incl. SURROGATE_PARENT + GODPARENT for completeness)
- [x] Certainty closed-set (HYPOTHESIS / ASSERTED / VERIFIED / DISPUTED) + provenance closed-set (USER_ENTERED / IMPORTED / VERIFIED_BY_SOURCE / CORRECTION) + temporal validity window
- [x] Multi-parent (≤ 8 participants) + multi-partner (≥ 2 PARTNER role) supported
- [x] Unknown participant supported (synthetic opaque participant; personId-null + unknown=true)
- [x] Disputed alternatives preserved (DISPUTED certainty + soft-notice visibility per `disputedAlternativeVisibility`)
- [x] Self-link / cycle rejected (hard deny via `RelationshipInvariants.checkCycle`)
- [x] Chronological conflict produces WARN (not deny) — twins / disputed parentage legitimate

## Residual gaps (deferred per §4.4)

- JDBC repository (`RelationshipRepository`) — E4.7
- gRPC + REST surface (relationship endpoints with ETag / cursor) — E4.7
- Avro event schemas (`relationship.created` / `.updated` / `.ended` / `.disputed`) — E4.7 (audit hooks already anchored in YAML)
- Merge / dispute resolution workflow (E4.6) — wires into `Relationship` `DISPUTED` certainty + `RelationshipConflictCode`
- Recurring memorial renderer for deceased subjects — renderer's job (R10)

## Commits

- Implementation: `E4.4: relationship graph + invariants — kinds/sub-kinds/role/certainty/provenance + cycle/self-link deny + warn-only chronology (DONE)`
- Checkbox flip: `E4.4: flip [ ] → [x] — evidence committed + DoD met (DONE)`
