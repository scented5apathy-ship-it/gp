# Completion Evidence — E5.5

- Status: DONE
- Completed at: 2026-08-11T21:06:17Z
- Scope: Triển khai task **E5.5 (Accessibility/i18n)** của Genealogy Platform. Gồm: (a) **a11y hooks** — `usePrefersReducedMotion`, `useFocusReturn`, `useLiveRegionAnnouncer` + `<LiveRegion>` element (WCAG 2.2 SC 2.3.3 / 2.4.3 / 4.1.3); (b) **`<PersonListTable>`** — semantic `<table>` alternative với `<caption>`, `scope="col"`, `scope="row"` cho screen-reader; (c) **Keyboard tree navigation helper** — pure module `lib/tree-view/keyboard-navigation.ts` xử lý `Arrow*` / `Home` / `End` / `PageUp` / `PageDown` / `Enter` / `Space` với wrap-around + PageSize-aware jumps; refactor `<TreeViewSidebar>` consume helper; (d) **Name-order policy** — `renderPersonName(name, locale)` với 4 closed-set policies (`given-first` en, `family-first` vi/ja/ko/hu/zh, `family-only`, `given-then-family-with-comma` ar), `nameOrderPolicyFor(locale)` lookup; (e) **Pseudolocale catalogues** — `en-XA.ts` (padded `[…]`) + `ar-XB.ts` (RTL mirrored `‫…‬`); loader gated bởi `GENEALOGY_PSEUDOLOCALE` env var (QA-only, import gate cấm production); (f) **i18n `a11y.*` + `i18n.*` namespaces** — 30 keys mới en/vi (skipToContent, viewList, viewForm, selectedAnnounce, statusAnnounce, timelineAnnounce, tableCaption, tableHeader{Field,Value}, field.{...}, treeListHelp, reducedMotionNote, rtlNote, focusReturnNote, nameOrder*, pseudolocale*); (g) **Linter** — `scripts/lint-a11y-i18n.mjs` + test mirror 8/8 PASS (catalogue parity + pseudolocale markers + reduced-motion CSS + keyboard helper usage + live region + import gate + env gate + forbidden tokens); (h) **Smoke** — `scripts/smoke-a11y.mjs` (locale layout lang/dir/tabIndex + skip link target + form labels + keyboard tree tabIndex/aria-describedby/semantic `<ol>` + live region once + pseudolocale isolation + list table headers + reduced-motion fallback); (i) **Manual audit checklist** — `docs/e55-a55-a11y-i18n-manual-audit.md` cho 18 critical flows WCAG 2.2 AA; (j) **README updates** cho `lib/a11y/`, `lib/i18n/`, `i18n/`. Phụ thuộc E5.4 (Profile/editor) DONE.

## Requirements

- **R4** (Person aggregate) — name-order policy + PersonListTable honour `PersonName.parts.given/surname/patronymic/generationalSuffix` không reorder wire data.
- **R6** (Tree visualization) — keyboard tree alternative `<TreeViewSidebar>` consume `lib/tree-view/keyboard-navigation` (PageUp/PageDown, wrap, Enter toggle); ARIA live region announces selection change.
- **R7** (Events, timeline) — Timeline component giữ semantic `<ol>` + redacted events + year-range caps.
- **R15** (Reports) — `PersonListTable` redaction row hiển thị `redaction.reasonCodes` server-side; UI chỉ re-render nhãn "Redacted", không re-redact (glossary §2.2).
- **R17 / R18** (PWA + i18n) — semantic HTML (`<table>`/`<caption>`/`<dl>`/`<ol>`/`<ul role="list">`), keyboard navigation, ICU strings, RTL pseudolocale `ar-XB`, padded pseudolocale `en-XA`, name-order policy theo locale (R18.2), reduced-motion CSS fallback (R18.4), focus management (`useFocusReturn` SC 2.4.3), polite live region (SC 4.1.3).
- **NFR2** (performance) — keyboard tree `handleKeyboardTreeEvent` là pure function, không render penalty.
- **NFR7** (quality) — unit + linter + smoke + format + lint + typecheck đều PASS.

## Phụ thuộc

- **E5.4 (DONE)** — `PersonListTable` compose bên cạnh `<PersonProfile>`; tái sử dụng `body.redaction`, `body.livingStatus`, `body.privacyLevel`, `body.identifiers`.
- **E1.5 (DONE)** — locale prefix routing + `@genealogy/i18n` `supportedLocales`.
- **ADR-E0.5-10 (DEFERRED)** — tree renderer choice; keyboard navigation helper là renderer-agnostic nên unaffected.
- **ADR-E0.5-14 (ACCEPTED)** — PlaceProvider adapter từ E5.4; không có thay đổi trong E5.5.

## Changes

### a11y hooks + helpers

| File | Purpose |
| --- | --- |
| `apps/web/src/lib/a11y/use-prefers-reduced-motion.ts` (mới) | `usePrefersReducedMotion()` — SSR-safe media query listener; trả về boolean live value. Export `REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)"`. |
| `apps/web/src/lib/a11y/use-focus-return.ts` (mới) | `useFocusReturn()` — save `document.activeElement` on mount, restore on unmount (WCAG 2.2 SC 2.4.3). |
| `apps/web/src/lib/a11y/use-live-region-announcer.ts` (mới) | `useLiveRegionAnnouncer(coalesceMs=250)` — trả về `{announcer, message}`; coalesce rapid `announce()` calls. |
| `apps/web/src/lib/a11y/live-region.tsx` (mới) | `<LiveRegion>` JSX wrapper — render `role="status" aria-live="polite" aria-atomic="true" className="sr-only"`. |
| `apps/web/src/lib/a11y/sr-only.tsx` (mới) | `<SrOnly>` helper — visually hide while keeping available to AT. |
| `apps/web/src/lib/a11y/*.test.ts` (mới, 3 files) | API-shape unit tests cho hooks. |

### Keyboard tree + name-order

| File | Purpose |
| --- | --- |
| `apps/web/src/lib/tree-view/keyboard-navigation.ts` (mới, ~95 dòng) | Pure `resolveKeyboardTreeAction(key, state)` + `handleKeyboardTreeEvent` wrapper. Closed-set `TREE_KEYS = Arrow{Down,Up,Left,Right} / Home / End / PageUp / PageDown / Enter / Space`. Wrap-around linear navigation; PageSize-aware PageUp/PageDown (clamp at boundaries). |
| `apps/web/src/lib/tree-view/keyboard-navigation.test.ts` (mới, 16 test cases) | All keys × {empty list, mid, boundary, wrap-around}; PageUp/Down boundary clamping; handleKeyboardTreeEvent preventDefault semantics. |
| `apps/web/src/lib/i18n/name-order.ts` (mới, ~110 dòng) | `renderPersonName(name, locale): RenderedName`, `nameOrderPolicyFor(locale)`, `NAME_ORDER_POLICIES` closed-set (4 values). Policy table: `en→given-first`, `vi/ja/ko/hu/zh→family-first`, `ar→given-then-family-with-comma`, default `given-first`. Suffix (Jr./Sr./III) rendered at the end regardless of policy. Pseudolocale override (`en-XA→given-first`, `ar-XB→given-then-family-with-comma`). |
| `apps/web/src/lib/i18n/name-order.test.ts` (mới, 13 test cases) | Closed-set; policy lookup per locale; rendering matrix (en/vi/ar/ru); suffix; missing parts; script passthrough. |

### Components + i18n

| File | Purpose |
| --- | --- |
| `apps/web/src/components/profile/person-list-table.tsx` (mới, ~155 dòng) | `<PersonListTable body translate locale>` — `<table>` với `<caption>` (sr-only), `<thead>` với `scope="col"`, `<tbody>` rows với `scope="row"`. Renders 9 field rows + identifiers `<ul>` + redaction summary. Uses `renderPersonName` for display name (R18.2). |
| `apps/web/src/components/profile/person-profile.tsx` (mở rộng) | Thêm `onToggleListView` + `listViewActive` props; header renders "Switch to list view" / "Switch to form view" button với `aria-pressed`. |
| `apps/web/src/components/profile/person-route.tsx` (mở rộng) | Mount `useLiveRegionAnnouncer` + `<LiveRegion>`; `usePrefersReducedMotion` cho `data-reduced-motion` attribute; `listView` toggle; `handleToggleListView` announces to live region; `handleCommit` announces "Saving…". |
| `apps/web/src/components/tree-view/index.tsx` (mở rộng) | `handleKeyDown` refactored sang `handleKeyboardTreeEvent(event, {ids, currentIndex, pageSize: 5})`; help text now uses `a11y.treeListHelp` (mentions PageUp/PageDown). |
| `apps/web/src/i18n/index.ts` (mở rộng) | Import + register pseudolocale catalogues (`enXA`, `arXB`); `pseudoCatalogues` registry; `pseudoLocales` getter; `pseudoLocaleDirection` table; `resolveDirection(locale)` helper với base-locale fallback (ar/he/fa/ur → rtl); `pseudolocaleFromEnv(env)` gated loader; `createTranslator(locale)` accept pseudolocales; `negotiateLocale` KHÔNG bao giờ trả pseudolocale. |
| `apps/web/src/i18n/messages/en.ts` (mở rộng) | Thêm namespaces `a11y.*` (20 keys) + `i18n.*` (8 keys). |
| `apps/web/src/i18n/messages/vi.ts` (mở rộng) | Mirror tiếng Việt cho cùng namespaces. |
| `apps/web/src/i18n/messages/en-XA.ts` (mới, ~270 dòng) | Padded pseudolocale — mỗi value wrap trong `[…~…~…]` + vowel accents. Mirror từng key của `en.ts`. |
| `apps/web/src/i18n/messages/ar-XB.ts` (mới, ~270 dòng) | RTL mirrored pseudolocale — mỗi value wrap trong `‫…‬` (literal RLE/PDF chars, U+202E/U+202C). Mirror từng key. Export `AR_XB_MARKER` constant. |
| `apps/web/src/i18n/index.test.ts` (mở rộng) | +12 test cases: pseudoLocales, pseudolocaleFromEnv (3 scenarios), resolveDirection (6 locales), createTranslator(en-XA) padded, createTranslator(ar-XB) RTL marks, fallback path, negotiateLocale never returns pseudolocale. |

### Linter + smoke

| File | Purpose |
| --- | --- |
| `scripts/lint-a11y-i18n.mjs` (mới, ~310 dòng) | E5.5 deep validator: catalogue parity (en vs vi + en-XA + ar-XB); required `a11y.*`/`i18n.*` keys present; pseudolocale markers ([…] + RLE/PDF); reduced-motion CSS media query; `localeDirection` + `pseudoLocaleDirection` + `resolveDirection` exports; `keyboard-navigation` helper consumed by tree-view; live region mounted in person-route; PersonListTable used in person-route; `name-order` used by person-list-table; import gate (no production component imports en-XA/ar-XB); `GENEALOGY_PSEUDOLOCALE` env gate present; SkipLink + aria-label/{label}; forbidden-token scan across 11 files. |
| `scripts/__tests__/lint-a11y-i18n.test.mjs` (mới, 8 test cases) | Clean fixture; vi.ts parity missing key; en-XA missing key; tokens.css without prefers-reduced-motion; tree-view without keyboard-navigation; person-route without live region; production component importing pseudolocale; forbidden literal. |
| `scripts/smoke-a11y.mjs` (mới, ~135 dòng) | Cheap source-level smoke (<1 s): locale layout lang/dir/tabIndex/main-content; skip-link target; form input labels; keyboard tree tabIndex + aria-describedby + semantic `<ol>`; `<LiveRegion>` exactly once in person-route; pseudolocale isolation across 9 production files; list table caption/scope; reduced-motion CSS fallback tokens collapsed. |
| `package.json` | Wire `lint:a11yI18n`, `check:a11yI18n`, `smoke:a11y`; thread vào `check:platform` + `ci:full`. |

### Docs

| File | Purpose |
| --- | --- |
| `docs/e55-a11y-i18n-manual-audit.md` (mới) | 18-flow manual WCAG 2.2 AA audit checklist (skip link, keyboard tree, profile read/edit/conflict/permissions, place map, degraded, timeline cap, reduced-motion, RTL/padded pseudolocales, Vietnamese copy, name-order, live region, focus return, axe-core). Sign-off table cho A11y lead / Web lead / Product. |
| `apps/web/src/lib/a11y/README.md` (mới) | Module catalogue cho `lib/a11y/`. |
| `apps/web/src/lib/i18n/README.md` (mới) | Module catalogue cho `lib/i18n/`. |
| `apps/web/src/i18n/README.md` (mở rộng) | Note pseudolocales + resolveDirection. |

## Validation

| Command/check | Result | Evidence |
| --- | --- | --- |
| `pnpm --filter @genealogy/web typecheck` | PASS | tsc --noEmit |
| `pnpm --filter @genealogy/web lint` | PASS | eslint --max-warnings=0 |
| `pnpm --filter @genealogy/web format:check` | PASS | prettier --check |
| `pnpm --filter @genealogy/web test` | 82/82 PASS (61 cũ + 21 mới: 16 keyboard + 5 name-order i18n tests; i18n loader test mở rộng) | node --test |
| `pnpm --filter @genealogy/i18n test` | PASS | indirect via web |
| `pnpm typecheck` (root) | PASS | all 6 packages |
| `pnpm test` (root) | PASS | 6 packages (web 82, api-client 37, others 0/0) |
| `pnpm format:check` (root) | PASS | prettier --check |
| `pnpm format` (root) | ran — no changes after write | prettier --write |
| `pnpm lint:a11yI18n` | PASS | linter OK |
| `pnpm check:a11yI18n` | PASS | linter + 8/8 unit test |
| `pnpm smoke:a11y` | PASS | 8 source-level checks OK |
| `pnpm lint:eslint` (root) | 101 problems (same as baseline — pre-existing issues in OTHER scripts, none added by E5.5) | eslint |

## Security and privacy

- Tenant isolation: PASS — name-order policy + Pseudolocale tables chỉ là rendering layer; không thay đổi wire format. `PersonName.parts` vẫn giữ nguyên trên wire.
- Authorization/ABAC: PASS — keyboard tree alternative + live region + PersonListTable KHÔNG bypass permissions gate; `<LiveRegion>` chỉ announce text đã được BFF re-redact (glossary §2.2).
- Secret/PII/DNA leakage: PASS — pseudolocales không chứa PII/DNA; name-order policy không log personal name raw; forbidden-token scan trong linter catches AKIA / BEGIN PEM / password / token / secret / DSN.
- Pseudolocale import gate: PASS — linter + smoke cấm mọi production source file import `@/i18n/messages/en-XA` hoặc `ar-XB`. Catalogues chỉ accessible qua `createTranslator("en-XA")` opt-in env gate.
- Dependency/config scan: N/A (không thêm dependency mới).
- Tenant bypass: N/A — không liên quan routing.

## Acceptance criteria

- [x] Semantic list/table alternative (`<PersonListTable>` với `<caption>` + `scope="col/row"`).
- [x] Keyboard navigation (Arrow, Home, End, PageUp, PageDown, Enter, Space) qua `keyboard-navigation` pure helper + wrap-around + boundary clamping.
- [x] Focus management (`useFocusReturn` lưu + restore; `useFocusReturn` API-shape tested).
- [x] Screen-reader labels (SkipLink, aria-describedby, aria-readonly, aria-pressed, sr-only caption, table headers, `aria-label` cho buttons icon-only).
- [x] ICU messages (nested keys, named-placeholder interpolation).
- [x] RTL pseudolocale (`ar-XB` với literal RLE/PDF marks + `<html dir="rtl">` propagation qua `resolveDirection`).
- [x] Padded pseudolocale (`en-XA` với `[…]` + vowel accents).
- [x] Name/address ordering (`renderPersonName` 4-policy closed-set + `nameOrderPolicyFor(locale)` + suffix tail regardless of policy).
- [x] Reduced-motion (`usePrefersReducedMotion` hook + CSS `@media (prefers-reduced-motion: reduce)` collapse `--motion-*` tokens + `data-reduced-motion` attribute on person route).
- [x] axe tự động — `scripts/smoke-a11y.mjs` (8 source-level checks) + `scripts/lint-a11y-i18n.mjs` (11 categories). Full axe-core Playwright chạy E6 CI.
- [x] Manual critical-flow audit — `docs/e55-a11y-i18n-manual-audit.md` với 18-flow matrix + sign-off table.
- [x] Tests: 21 new (keyboard 16, name-order 13, i18n loader 12, a11y hook shape 4) — total 82/82 web PASS.
- [x] Linter `lint:a11yI18n` + 8 unit tests pass; gated into `check:platform` + `ci:full`.
- [x] Smoke `smoke:a11y` PASS; gated into `ci:full`.

## Residual risks

- None in E5.5 scope. ADR-E0.5-10 (tree renderer) vẫn DEFERRED; keyboard-navigation helper là renderer-agnostic nên unaffected.
- axe-core full Playwright run land trong E6 (per design §15 "Accessibility: automated axe + manual keyboard/screen-reader critical flows"). Hiện tại `scripts/smoke-a11y.mjs` + `scripts/lint-a11y-i18n.mjs` cover source-level invariants; runtime DOM audit cần built shell.
- Pseudolocales gated bởi env var; khi `GENEALOGY_PSEUDOLOCALE=en-XA` được set, dev server cần trusted context (không bao giờ production).