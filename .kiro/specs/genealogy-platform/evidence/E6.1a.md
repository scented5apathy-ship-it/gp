# Completion Evidence — E6.1a

- Status: DONE
- Completed at: 2026-08-12T03:08:00+07:00 (original domain commit `7bfd521`)
- Re-classified at: 2026-08-12T08:30:00+07:00 (per-commit `5b77886` sub-task split)
- Scope: Research service domain layer — Repository, Source, Citation, ResearchTask, Hypothesis, Conflict aggregates + 8 closed-set enums + 6 value objects + 2 state machines + 16 invariant codes + in-memory provenance query executor + contract YAML + chart mirror + linter + 66 unit tests.
- Requirements: R8.1, R8.4, R4.4, R10.2, R16.2, NFR1, NFR4, NFR5, NFR7, NFR8.
- Parent task: E6.1 Research service (split into E6.1a / E6.1b / E6.1c / E6.1d / E6.1e per commit `5b77886`).

## Changes

(All work landed in commit `7bfd521` (implementation) + `52c887c` (evidence flip). This evidence file is the bookkeeping entry for the sub-task flip in commit `5b77886` and onward.)

| File / group | Sub-task owner |
| --- | --- |
| `services/research-service/build.gradle.kts` (updated) | applies `java-conventions` + `service-conventions` |
| `services/research-service/src/main/java/com/genealogy/platform/services/research/package-info.java` (updated) | E6.1a ownership header |
| `services/research-service/src/main/java/com/genealogy/platform/services/research/domain/package-info.java` (new) | domain package header |
| 8 closed-set enums (`SourceKind`, `CitationQuality`, `ResearchTaskStatus`, `HypothesisStatus`, `ConflictKind`, `RepositoryKind`, `AttachmentKind`, `Certainty`) | R8.1 vocabulary |
| 6 value objects (`Locator`, `AttachmentRef`, `TranscriptSegment`, `TenantScopedId`, `ResearchAuditAttributes`, `Confidence`) | R8.1 value objects |
| 6 aggregate records (`Repository`, `Source`, `Citation`, `ResearchTask`, `Hypothesis`, `Conflict`) | R8.1 aggregates |
| 2 state machines (`ResearchTaskStateMachine`, `HypothesisStateMachine`) | R8.1 status transitions |
| `ResearchInvariants` (16 closed-set codes × DENY/WARN/INFO) | R8.1 invariants |
| `ProvenanceQueryService` (in-memory executor) | R8.4 provenance query |
| 10 unit-test classes (66 tests) | NFR7 |
| `contracts/research/research-policy.yaml` + chart mirror | YAML single source of truth |
| `scripts/lint-research-config.mjs` + 13 linter unit tests | linter enforcement |
| `package.json` `lint:research` + `check:research` + `ci:full` / `check:platform` hooks | workspace integration |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** — every closed-set vocabulary is anchored to the YAML contract.
- **ADR-E0.5-01 (Java 21 toolchain): ACCEPTED** — `services/research-service/build.gradle.kts` applies `java-conventions` + `service-conventions`.
- **No cross-service domain import** — `Certainty` enum is local to research-service; the ArchUnit boundary guard (wired through `service-conventions`) blocks accidental coupling.
- **In-memory executor only** — the Flyway migration + jOOQ repository + Kafka publisher land in `E6.1b` … `E6.1e` per the sub-task split.
- **Audit metadata uses `actorPseudoId` only** — never raw user id / email / DNA.

## Validation

| Command | Result | Evidence |
| --- | --- | --- |
| `pnpm typecheck` | PASS | apps/web + packages/{api-client, eslint-config, i18n, ui} + tools/codegen all green |
| `pnpm format:check` | PASS | 6 packages clean |
| `./gradlew :services:research-service:checkstyleMain + checkstyleTest` | PASS | 0 violations |
| `./gradlew :services:research-service:test` | PASS | 67/67 tests pass (66 E6.1a + 1 E1.1 skeleton) |
| `pnpm check:research` | PASS | linter OK + 13/13 linter unit tests |
| `pnpm check:java` | PASS | every Java subproject green |
| `pnpm check:boundary` | PASS | 11 services clean (no cross-service db/domain imports) |
| `pnpm check:gradle:lock` / `pnpm check:lockfile` / `pnpm lint:ownership` | PASS | clean |

## Security and privacy

- **Tenant isolation**: PASS — every aggregate carries `TenantScopedId` (NFR1); compact constructor rejects tenant mismatch on `Source` / `Citation`.
- **Authorization/ABAC**: N/A — the application layer wires `ReAuthorizationPort` in `E6.1d`.
- **Secret/PII/DNA leakage**: PASS — `ResearchAuditAttributes` rejects raw identifiers; `actorPseudoId` mandatory; the linter forbids `token[:=]`, `password[:=]`, `secret[:=]`, `api[_-]?key`, `authorization`, `bearer`, `raw[_-]?dna`, `raw[_-]?ssn`, `raw[_-]?passport` in `spec.forbiddenPayloadPatterns`.
- **Dependency/config scan**: PASS — no new dependency added.

## Acceptance criteria

- [x] Domain records (Repository / Source / Citation / ResearchTask / Hypothesis / Conflict).
- [x] 8 closed-set enums + 6 value objects + 2 state machines.
- [x] `ResearchInvariants` (16 closed-set codes × DENY/WARN/INFO).
- [x] `ProvenanceQueryService` in-memory executor.
- [x] `contracts/research/research-policy.yaml` + chart mirror.
- [x] `lint-research-config.mjs` + 13 linter unit tests.
- [x] 66 Java unit tests + 1 E1.1 skeleton (67/67 pass).
- [x] Checkstyle / Gradle / boundary / lockfile / ownership checks pass.

## Residual risks

- **Persistence / transport / platform wiring deferred to E6.1b … E6.1e** — see `tasks.md` §E6.1 sub-task list.
- **In-memory provenance query** — the persisted materialised view will use the same `ProvenanceQueryService` shape so the read path stays identical.
