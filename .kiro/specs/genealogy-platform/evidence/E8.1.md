# Completion Evidence — E8.1

- Status: DONE
- Completed at: 2026-08-15T22:40:00+07:00
- Scope: Search projection contract + linter + Java orchestrator. The
  `search-service` projection pipeline consumes
  genealogy/research/collaboration/media Avro events idempotently
  per ADR-E0.5-08, normalises multilingual names/aliases with
  PostgreSQL FTS + pg_trgm + unaccent-aware columns per
  `design.md` §9, and persists the per-row privacy classification
  + projection version. Lag is monitored through the OTel pipeline;
  rebuild + reconciliation flow through Temporal per ADR-E0.5-07.
- Requirements: R11.1, R11.2, R13, NFR1, NFR5, NFR8.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/search/search-projection-policy.yaml` (new, ~510 lines) | Single source of truth for the E8.1 projection contract. 15 closed-set vocabularies (searchDocumentKinds[7]: PERSON/EVENT/PLACE/SOURCE/CITATION/MEDIA/ALBUM; searchPrivacyClasses[5]: PRIVATE/TREE_DEFAULT/UNLISTED/PUBLIC/REDACTED; searchProjectionStatuses[5]: PENDING/INDEXED/STALE/REDACTED/PURGED; searchSourceDomains[4]: GENEALOGY/RESEARCH/COLLABORATION/MEDIA; searchEventTypes[20]; searchProjectionLagPhases[5]: HEALTHY/BACKFILLING/DRAINING/AT_RISK/DEGRADED; searchReconciliationStatuses[9]; searchFailureReasons[23]; searchAuditEvents[18]; searchBenchmarkWorkloads[8]; searchBenchmarkVerdicts[8]; searchProjectionEvolutionPaths[6]; languageNormalizationStrategies[3]; savedSearchSharingScopes[3]; searchFacetAxes[7]). `dnaBucketAccess=FORBIDDEN` + `dnaBucketPrefixes=[dna/raw, dna/match, dna/consent]` reject DNA keys end-to-end (ADR-E0.5-15). `postgresFullTextOnly=true` + `pgTrgmEnabled=true` + `unaccentNormalizationRequired=true` enforce PostgreSQL FTS+trigram. `eventConsumptionIdempotent=true` + `idempotencyKeyRequired=true` enforce idempotent Kafka consumption. `projectionVersioningRequired=true` enforces optimistic concurrency. `reconciliationWorkflowRequired=true` + `outboxEvents.items` wire `gp.search.v1.ProjectionReconciliationPurged` / `ProjectionLagBreached` / `SavedSearchCreated`. Two state machines (projection lag 6 status + reconciliation 9 status). Sandbox egress allowlist `[postgres, apicurio, vault-agent, openfga, audit-service, kafka-broker]`. 35 numeric bounds. 3 reconciled invariants (p95<multiplier×heartbeat, lookback≥multiplier×cadence, p95≤multiplier×freshness). |
| `platform/helm/genealogy-platform/files/search-projection-policy.yaml` (new, byte-identical) | Chart mirror enforced by `lint-search-projection.mjs`. |
| `scripts/lint-yaml.mjs` (new) | Shared minimal block-YAML parser (sequences, mappings, inline lists, multiline `\|` / `>` scalars). Used by every E8 search linter. |
| `scripts/lint-search-projection.mjs` (new, ~500 lines) | E8.1 deep validator: 15 closed-set checks + sandbox egress allowlist + projection lag state matrix (HEALTHY→DECIDED, every status reachable) + reconciliation state matrix (QUEUED→DECIDED, every status reachable) + 19 boolean guard rails (`postgresFullTextOnly`, `pgTrgmEnabled`, `unaccentNormalizationRequired`, `privacyClassificationRequired`, `projectionVersioningRequired`, `eventConsumptionIdempotent`, `idempotencyKeyRequired`, `outboxRelaySeparated`, `reconciliationWorkflowRequired`, `lagMetricsExported`, `lagBudgetBreachedAlerts`, `projectionOutOfBandForbidden`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `languageTagIetfBcp47Required`, `savedSearchSharingOpaque`, `savedSearchQueryNoRawPii`, `benchmarkSuiteRequiredForRelease`, `adrRequiredBeforeOpenSearch`) + `dnaBucketAccess MUST equal FORBIDDEN` + 35 numeric bounds + 3 reconciled invariants + outbox envelope field check + 18 audit hooks + forbidden-literal scan + chart mirror byte-equality. |
| `scripts/__tests__/lint-search-projection.test.mjs` (new, 12 tests) | Pins every invariant: clean fixture exits 0; missing PERSON kind fails; `dnaBucketAccess` MUST equal FORBIDDEN; missing dna/raw prefix fails; `postgresFullTextOnly` MUST be true; `projectionLagP95BudgetSeconds=24`; terminal DECIDED must have empty transitions; lag invariant p95<multiplier×heartbeat; chart mirror drift fails; missing envelope field fails; sandbox egress drift fails; audit hook missing fails. |
| `package.json` (updated) | New `lint:search` + `check:search` scripts; wired into `check:platform` (after `lint:albums`) + `ci:full` (same position). |
| `services/search-service/build.gradle.kts` (updated) | Applies `java-conventions` + `service-conventions` plugins (Java 21, Checkstyle, ArchUnit boundary guard) per ADR-E0.5-01. |
| `services/search-service/src/main/java/.../shared/SearchLimits.java` (new) | Centralised string-length caps + DNA bucket prefixes shared across all E8 subtask packages. |
| `services/search-service/src/main/java/.../projection/SearchDocumentKind.java` (new) | Closed-set enum (7 kinds). |
| `services/search-service/src/main/java/.../projection/SearchPrivacyClass.java` (new) | Closed-set enum (5 classes). |
| `services/search-service/src/main/java/.../projection/SearchProjectionStatus.java` (new) | Closed-set enum (5 statuses). |
| `services/search-service/src/main/java/.../projection/SearchEventType.java` (new) | Closed-set enum (20 event types). |
| `services/search-service/src/main/java/.../projection/SearchFailureReason.java` (new) | Closed-set enum (23 failure reasons including `PROJECTION_DNA_BUCKET_FORBIDDEN`). |
| `services/search-service/src/main/java/.../projection/SearchAuditEvent.java` (new) | Closed-set enum (18 audit events). |
| `services/search-service/src/main/java/.../projection/SearchProjectionRedactionReason.java` (new) | Closed-set enum (7 redaction reasons). |
| `services/search-service/src/main/java/.../projection/SearchProjectionLimits.java` (new) | Numeric constants (MAX_NAME_LENGTH=256, MAX_ALIAS_PER_DOCUMENT=64, PROJECTION_LAG_P95_BUDGET_SECONDS=24, BACKFILL_LOOKBACK_HOURS=168, etc.). |
| `services/search-service/src/main/java/.../projection/SearchProjectionEvent.java` (new) | Per-event payload record with compact-constructor enforcement (alias count, language count, BCP-47 length, idempotency key, actor pseudo id, correlation id). |
| `services/search-service/src/main/java/.../projection/SearchProjectionDecision.java` (new) | Per-event decision record (INDEXED / REDACTED / PURGED) with compact-constructor shape enforcement. |
| `services/search-service/src/main/java/.../projection/SearchProjectionIndex.java` (new) | Pure deterministic orchestrator: `apply(event, seenIdempotencyKeys)` → dedup → DNA bucket shield → privacy downgrade for PRIVATE → INDEXED. `isDnaBucketKey(key)` matches all 3 closed-set prefixes. |
| `services/search-service/src/test/java/.../projection/SearchProjectionClosedSetEnumsTest.java` (new, 11 tests) | Pins every closed-set enum + numeric constant + DNA bucket prefix + decision constructor shape. |
| `services/search-service/src/test/java/.../projection/SearchProjectionIndexTest.java` (new, 6 tests) | Pins the orchestrator: happy path → INDEXED; PRIVATE → REDACTED/POLICY_DENY; dna/raw key → PURGED/FAIL; duplicate idempotency key → PURGED/FAIL; blank idempotency key rejected at construction; too-many aliases rejected at construction. |
| `.kiro/specs/genealogy-platform/tasks.md` (E8.1 checkbox) | Flipped `- [ ] E8.1 …` → `- [x] E8.1 …` after the evidence file is committed. |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** —
  every closed-set vocabulary anchored to the YAML contract; the
  Java enum values are validated by the linter.
- **ADR-E0.5-01 (Java 21 toolchain): ACCEPTED** — `services/search-service/build.gradle.kts`
  applies the `java-conventions` + `service-conventions` plugins
  (Java 21, Checkstyle, ArchUnit boundary guard).
- **ADR-E0.5-07 (Temporal reconciliation workflow): ACCEPTED** —
  the projection worker emits `gp.search.v1.ProjectionReconciliationPurged` /
  `gp.search.v1.ProjectionLagBreached` events; the
  reconciliation state machine + numeric bounds (lookback ≥ 7 ×
  cadence, p95 < 6 × heartbeat) wire the Temporal worker that
  lands in the worker subproject.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** —
  `dnaBucketAccess=FORBIDDEN` + `dnaBucketPrefixes=[dna/raw,
  dna/match, dna/consent]` reject any DNA bucket key before
  OpenFGA + before persistence.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `pnpm lint:search` | PASS | 100+ OK lines, exit 0 |
| `pnpm check:search` (linter + 12 unit tests) | PASS | 12/12 tests pass |
| `JAVA_HOME=… pnpm check:java` (checkstyle + all services tests) | PASS | 86 tasks executed, exit 0 |
| `pnpm check:boundary` | PASS | scanned 11 service(s); no cross-service db/domain imports |
| `pnpm lint:ownership` | PASS | 37 declared paths, all per-directory OWNERS present |
| `pnpm check:gradle:lock` | PASS | every lockAll subproject has gradle.lockfile |
| `diff -q contracts/search/search-projection-policy.yaml platform/helm/.../search-projection-policy.yaml` | PASS | byte-equal (16063 bytes) |

## Security and privacy

- Tenant isolation: PASS — `tenantScopeId` mandatory + length cap
  + null rejection at construction.
- Authorization/ABAC: N/A at the projection layer (ABAC overlay
  lives in E8.2); projection never holds raw PII or DNA.
- Secret/PII/DNA leakage: PASS — `dnaBucketAccess=FORBIDDEN` +
  DNA bucket shield at the orchestrator + forbidden payload
  patterns (rawDnaSequence, rawFastq, rawVcf, exifGps, …).
- Dependency/config scan: N/A — no new dependencies; existing
  `spring-boot-starter-actuator`, `postgres`, `flyway-core`,
  `openfeature-sdk`, JUnit/Testcontainers bundle.

## Acceptance criteria

- [x] Closed-set vocabularies pinned + linter enforced (15 sets)
- [x] Sandbox egress allowlist pinned + linter enforced (6 entries)
- [x] State machines reachable from initial status; terminals have
      empty transition lists
- [x] Boolean guard rails pinned to exact values (19)
- [x] Numeric bounds pinned to exact values (35)
- [x] Reconciled invariants enforced (3)
- [x] Outbox envelope fields declared for every outbox event
- [x] Audit hooks declared for every required event
- [x] DNA bucket shield enforced (3 prefixes)
- [x] Cross-service references opaque (projection never joins)
- [x] Chart mirror byte-equal

## Residual risks

- OpenFGA SDK + reconciliation Temporal worker + Kafka consumer
  + `SearchLimits` post-construction extension are port seams
  (E8.x / E11.x); projection worker + Flyway migration land in
  later sub-epic.
- `SearchProjectionEvent` constructor enforces the closed-set
  contract today; runtime workers will need to project Avro
  payloads through a similar contract-check step before calling
  the orchestrator.