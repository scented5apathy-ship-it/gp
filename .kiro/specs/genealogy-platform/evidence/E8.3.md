# Completion Evidence — E8.3

- Status: DONE
- Completed at: 2026-08-15T22:40:00+07:00
- Scope: Public projection contract + linter + Java redactor. Only
  PUBLIC rows enter the public index AFTER living / minor / DNA /
  consent redaction per R11.4 + R13.7 + `design.md` §9. UNLISTED
  rows return `noindex` and require an opaque token with hash +
  expiry + rate limit (enforced at Kong and validated in-app).
  Purge propagates to projection + cache + sitemap when visibility
  or policy changes (per ADR-E0.5-07 + ADR-E0.5-08).
- Requirements: R11.4, R13.7, NFR1, NFR8.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/search/public-projection-policy.yaml` (new, ~410 lines) | Single source of truth for E8.3. 9 closed-set vocabularies (publicProjectionVisibilityScopes[2]: PUBLIC/UNLISTED; publicProjectionLifecycleStatuses[5]; publicProjectionRedactionReasons[10]: LIVING/MINOR/DNA_ATTACHED/CONSENT_MISSING/CONSENT_REVOKED/VISIBILITY_NOT_PUBLIC/TENANT_REDACTION_FORBIDDEN/POLICY_DENY/SOFT_DELETED/LEGAL_HOLD; publicProjectionTokenKinds[3]; publicProjectionPurgeTriggers[9]; publicProjectionSitemapStatuses[5]; publicProjectionIndexStrategies[4]; publicProjectionFailureReasons[21]; publicProjectionAuditEvents[18]). 21 boolean guard rails (`publicOnlyAfterRedaction`, `livingRedactionRequired`, `minorRedactionRequired`, `consentRedactionRequired`, `consentWithdrawalInvalidatesResults`, `unlistedReturnsNoindex`, `unlistedTokenHashRequired`, `unlistedTokenExpiryRequired`, `unlistedTokenRateLimitRequired`, `unlistedTokenVerifiedAtEdge`, `unlistedTokenReverifiedInApp`, `purgePropagatesToCache`, `purgePropagatesToSitemap`, `purgePropagatesToProjection`, `robotstxtUpdatedOnVisibilityChange`, `canonicalHostEnforced`, `languageHreflangRequired`, `publicProjectionOutOfBandForbidden`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `outboxRelaySeparated`). 21 numeric bounds. 3 reconciled invariants (sitemap rebuildP95 < 2 × heartbeat; perMinute ≥ 6 × burst; batch = 2 × outbox). 2 state machines (lifecycle + sitemap). |
| `platform/helm/genealogy-platform/files/public-projection-policy.yaml` (new, byte-identical) | Chart mirror enforced by `lint-public-projection.mjs`. |
| `scripts/lint-public-projection.mjs` (new) | E8.3 deep validator reusing `lint-yaml.mjs`. Enforces 9 closed-set + 21 boolean + 21 numeric + 3 invariants + 2 state machines + outbox envelope + chart mirror. |
| `scripts/__tests__/lint-public-projection.test.mjs` (new, 8 tests) | Pins every invariant: clean fixture; missing PUBLIC; `dnaBucketAccess=FORBIDDEN`; terminal PURGED empty transitions; sitemap heartbeat invariant; projection batch invariant; chart mirror drift; `unlistedReturnsNoindex=true`. |
| `package.json` (updated) | New `lint:search:public` + `check:search:public`; wired into `check:platform` + `ci:full`. |
| `services/search-service/src/main/java/.../publicindex/PublicProjectionVisibility.java` (new) | Closed-set enum (PUBLIC / UNLISTED). |
| `services/search-service/src/main/java/.../publicindex/PublicProjectionRedactionReason.java` (new) | Closed-set enum (10 redaction reasons). |
| `services/search-service/src/main/java/.../publicindex/PublicProjectionFailureReason.java` (new) | Closed-set enum (21 failure reasons). |
| `services/search-service/src/main/java/.../publicindex/PublicProjectionLifecycleStatus.java` (new) | Closed-set enum (5 statuses). |
| `services/search-service/src/main/java/.../publicindex/PublicProjectionDecision.java` (new) | Decision record with shape enforcement (INDEXED requires publicRowId + canonicalUrl; REDACTED requires redactionReason; PURGED requires failureReason; PENDING forbids both reasons). |
| `services/search-service/src/main/java/.../publicindex/PublicProjectionRow.java` (new) | Input record. |
| `services/search-service/src/main/java/.../publicindex/PublicProjectionIndex.java` (new) | Pure deterministic redactor: DNA bucket shield → visibility check → living → minor → DNA attached → consent missing → INDEXED with deterministic canonical URL. |
| `services/search-service/src/test/java/.../publicindex/PublicProjectionClosedSetEnumsTest.java` (new, 7 tests) | Pins every closed-set enum + decision constructor shape. |
| `services/search-service/src/test/java/.../publicindex/PublicProjectionIndexTest.java` (new, 8 tests) | Pins the redactor: PUBLIC happy path; UNLISTED → VISIBILITY_NOT_PUBLIC; living → LIVING; minor → MINOR; DNA attached → DNA_ATTACHED; consent missing → CONSENT_MISSING; DNA bucket key → PURGED/DNA_BUCKET_FORBIDDEN; blank canonical host rejected. |
| `.kiro/specs/genealogy-platform/tasks.md` (E8.3 checkbox) | Flipped `- [ ] E8.3 …` → `- [x] E8.3 …`. |

## Decisions

- **ADR-E0.5-07 (Temporal reconciliation + purge): ACCEPTED** —
  `purgePropagatesToCache=true` + `purgePropagatesToSitemap=true` +
  `purgePropagatesToProjection=true` + outbox events
  `gp.search.v1.PublicProjectionPurged` /
  `gp.search.v1.PublicProjectionVisibilityChanged` wire the
  worker that lands in E8.x/E11.x.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket
  shield enforced BEFORE visibility check (DNA keys never enter
  the index even if visibility is PUBLIC).
- **ADR-E0.5-09 (Permission-version binding): PARTIAL** — public
  projection has no permission-version concept (it's anonymous);
  `consentWithdrawalInvalidatesResults=true` covers the
  consent withdrawal invalidation case (R13.3).

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `pnpm lint:search:public` | PASS | 100+ OK lines, exit 0 |
| `pnpm check:search:public` (linter + 8 unit tests) | PASS | 8/8 tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks, exit 0 |
| `pnpm check:boundary` | PASS | clean |
| `pnpm lint:ownership` | PASS | clean |
| `pnpm check:gradle:lock` | PASS | clean |
| Chart mirror byte-equal | PASS | 11441 bytes |

## Security and privacy

- Tenant isolation: PASS — `tenantScopeId` mandatory + length cap;
  `canonicalHostEnforced=true` ensures the public URL is
  stable.
- Authorization/ABAC: N/A at the public layer (anonymous); ABAC
  outcomes drive the REDACTED decision (LIVING / MINOR /
  DNA_ATTACHED / CONSENT_MISSING).
- Secret/PII/DNA leakage: PASS — DNA bucket shield + forbidden
  payload patterns (rawDnaSequence, rawFastq, rawVcf, exifGps,
  rawEmail, rawPhone, rawAddress, biometricTemplate,
  rawFacialEmbedding, rawLivingStatus, rawMinorStatus,
  rawConsentDocument).
- Dependency/config scan: PASS — no new dependencies.

## Acceptance criteria

- [x] Only PUBLIC rows ever enter the public index
- [x] Living / minor / DNA / consent redaction enforced
- [x] UNLISTED returns noindex; token hash + expiry + rate limit
- [x] Purge propagates to projection + cache + sitemap
- [x] robots.txt + canonical host + language hreflang enforced
- [x] DNA bucket shield enforced
- [x] Cross-service references opaque
- [x] Chart mirror byte-equal

## Residual risks

- Token verification (Kong + in-app) + sitemap generator +
  robots.txt writer + consent invalidation worker are port seams
  (E8.x / E11.x).