# Completion Evidence — E8.4

- Status: DONE
- Completed at: 2026-08-15T22:40:00+07:00
- Scope: Search benchmark + evolution gate contract + linter + Java
  gate. The gate measures p95 / p99 / freshness / fuzzy recall /
  fuzzy precision on the closed-set workloads and emits a closed-set
  verdict that maps to a closed-set evolution path. PostgreSQL is the
  default and only dependency; OpenSearch may only land via an
  explicit ADR supersession (per ADR-E0.5-09 + `design.md` §9).
- Requirements: R11, NFR1, NFR2, NFR8.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/search/benchmark-evolution-gate-policy.yaml` (new, ~410 lines) | Single source of truth for E8.4. 11 closed-set vocabularies (benchmarkWorkloads[8]: EXACT_PERSON/EXACT_ALBUM/FUZZY_PERSON_TRIGRAM/FUZZY_PLACE_TRIGRAM/FACET_TREE_FAMILY/FACET_DECADE_LIVING_STATUS/CURSOR_PAGINATION/SAVED_SEARCH_ALERT; benchmarkDatasetShapes[8]; benchmarkVerdicts[12]: PASS/PASS_WITH_NOTES/FAIL_P95/FAIL_P99/FAIL_LAG/FAIL_FRESHNESS/FAIL_FUZZY_RECALL/FAIL_FUZZY_PRECISION/FAIL_FACET_CARDINALITY/FAIL_INDEX/FAIL_SAFETY/BLOCKED_ADR_REQUIRED; benchmarkEvolutionPaths[8]: POSTGRES_HOLD/POSTGRES_REINDEX/POSTGRES_PARTITION/POSTGRES_GIN_REWRITE/POSTGRES_GIST_REWRITE/POSTGRES_BRIN_PARTITION/ADAPTIVE_INDEX_REQUIRED/OPENSEARCH_REQUIRED; benchmarkSloMetrics[10]; benchmarkQueryLanguages[5]: en/vi/ja/fr/es; benchmarkRolloutStages[5]: NIGHTLY/PRE_MERGE/RELEASE_CANDIDATE/POST_RELEASE/AD_HOC; benchmarkFailureReasons[22]; benchmarkAuditEvents[14]). 16 boolean guard rails (`postgresqlOnlyByDefault`, `opensearchRequiresAdr`, `benchmarkSuiteRequiredForRelease`, `fuzzyRecallFloorEnforced`, `fuzzyPrecisionFloorEnforced`, `freshnessBudgetEnforced`, `facetCardinalityFloorEnforced`, `p95BudgetEnforced`, `p99BudgetEnforced`, `safetyBudgetEnforced`, `deterministicRunnerRequired`, `contractHashBoundToRun`, `datasetShapeSealed`, `benchmarkOutOfBandForbidden`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`). 24 numeric bounds. 4 reconciled invariants (p99 = 2 × p95; timeout < 6 × heartbeat; maxDatasets = 1 × maxWorkloads × 2). rolloutToEvolutionPath mapping for every rollout stage. 1 state machine (benchmark gate 10 status). |
| `platform/helm/genealogy-platform/files/benchmark-evolution-gate-policy.yaml` (new, byte-identical) | Chart mirror enforced by `lint-benchmark-evolution-gate.mjs`. |
| `scripts/lint-benchmark-evolution-gate.mjs` (new) | E8.4 deep validator reusing `lint-yaml.mjs`. Enforces 11 closed-set + 16 boolean + 24 numeric + 4 invariants + rollout→path mapping + state machine + outbox envelope + chart mirror. |
| `scripts/__tests__/lint-benchmark-evolution-gate.test.mjs` (new, 8 tests) | Pins every invariant: clean fixture; missing PASS verdict; `opensearchRequiresAdr=true`; p95/p99 ratio; terminal PROMOTED empty transitions; rollout→path mapping drift; chart mirror drift; `dnaBucketAccess=FORBIDDEN`. |
| `package.json` (updated) | New `lint:search:benchmark` + `check:search:benchmark`; wired into `check:platform` + `ci:full`. |
| `services/search-service/src/main/java/.../benchmark/BenchmarkVerdict.java` (new) | Closed-set enum (12 verdicts). |
| `services/search-service/src/main/java/.../benchmark/BenchmarkEvolutionPath.java` (new) | Closed-set enum (8 evolution paths). |
| `services/search-service/src/main/java/.../benchmark/BenchmarkRolloutStage.java` (new) | Closed-set enum (5 rollout stages). |
| `services/search-service/src/main/java/.../benchmark/BenchmarkFailureReason.java` (new) | Closed-set enum (22 failure reasons). |
| `services/search-service/src/main/java/.../benchmark/BenchmarkLimits.java` (new) | Numeric constants (MAX_DATASETS=16, MAX_QUERIES_PER_DATASET=1024, MAX_WORKLOADS=8, P95_BUDGET_MILLISECONDS=1000, P99_BUDGET_MILLISECONDS=2000, FUZZY_RECALL_FLOOR=0.85, FUZZY_PRECISION_FLOOR=0.90, RUNTIME_TIMEOUT_SECONDS=120, HEARTBEAT_SECONDS=30). |
| `services/search-service/src/main/java/.../benchmark/BenchmarkSample.java` (new) | Per-sample record with shape enforcement (p99 ≥ p95; fuzzy in [0,1]; safety/dna non-negative). |
| `services/search-service/src/main/java/.../benchmark/BenchmarkDecision.java` (new) | Decision record with shape enforcement (PASS/PASS_WITH_NOTES/FAIL_* require evolutionPath; BLOCKED_ADR_REQUIRED requires BENCHMARK_ADR_REQUIRED failure reason). |
| `services/search-service/src/main/java/.../benchmark/SearchBenchmarkGate.java` (new) | Pure deterministic gate: `evaluate(stage, samples)` → DNA bucket leak short-circuits to FAIL_SAFETY; rolls worst verdict up; picks evolution path from rollout-to-path mapping (NIGHTLY allows OPENSEARCH_REQUIRED + ADAPTIVE_INDEX_REQUIRED; RELEASE_CANDIDATE only POSTGRES_HOLD; etc.). |
| `services/search-service/src/test/java/.../benchmark/SearchBenchmarkClosedSetEnumsTest.java` (new, 9 tests) | Pins every closed-set enum + numeric constants + decision/sample constructor shape. |
| `services/search-service/src/test/java/.../benchmark/SearchBenchmarkGateTest.java` (new, 8 tests) | Pins the gate: all passing → PASS/POSTGRES_HOLD; p95 breach → FAIL_P95; fuzzy recall breach → FAIL_FUZZY_RECALL; DNA bucket leak → FAIL_SAFETY; empty samples → BLOCKED_ADR_REQUIRED; freshness breach → FAIL_FRESHNESS; facet cardinality below floor → FAIL_FACET_CARDINALITY; null stage rejected. |
| `.kiro/specs/genealogy-platform/tasks.md` (E8.4 checkbox) | Flipped `- [ ] E8.4 …` → `- [x] E8.4 …`. |

## Decisions

- **ADR-E0.5-09 (OpenSearch evolution gate): ACCEPTED** —
  `opensearchRequiresAdr=true` + `postgresqlOnlyByDefault=true` +
  `BLOCKED_ADR_REQUIRED` verdict enforce the ADR supersession
  requirement per `design.md` §9.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket leak
  short-circuits to `FAIL_SAFETY` / `BENCHMARK_SAFETY_BUDGET_EXCEEDED`.
- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** —
  every closed-set vocabulary anchored to the YAML contract.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `pnpm lint:search:benchmark` | PASS | 100+ OK lines, exit 0 |
| `pnpm check:search:benchmark` (linter + 8 unit tests) | PASS | 8/8 tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks, exit 0 |
| `pnpm check:boundary` | PASS | clean |
| `pnpm lint:ownership` | PASS | clean |
| `pnpm check:gradle:lock` | PASS | clean |
| Chart mirror byte-equal | PASS | 11846 bytes |

## Security and privacy

- Tenant isolation: PASS — gate operates on aggregate samples;
  no per-row PII enters the decision.
- Authorization/ABAC: N/A at the gate level.
- Secret/PII/DNA leakage: PASS — `productionPii` + DNA bucket
  patterns + rawEmail/rawPhone/rawAddress forbidden payload
  patterns + `BENCHMARK_DNA_BUCKET_LEAK=0` budget.
- Dependency/config scan: PASS — no new dependencies.

## Acceptance criteria

- [x] PostgreSQL only by default; OpenSearch requires ADR
- [x] Benchmark suite required for release
- [x] Fuzzy recall + precision floors enforced
- [x] Freshness + p95 + p99 + facet cardinality budgets enforced
- [x] Rollout-to-evolution-path mapping declared for every stage
- [x] DNA bucket leak short-circuits to FAIL_SAFETY
- [x] Deterministic runner + contract hash binding
- [x] Chart mirror byte-equal

## Residual risks

- k6/Gatling benchmark runner + dataset loader + recommendation
  TTL store are port seams (E8.x / E11.x). The `p99Budget=2000ms`
  may need rebalancing once real dataset rows are wired in.