# Completion Evidence — E9.1

- Status: DONE
- Completed at: 2026-08-15T23:45:00+07:00
- Scope: Temporal transfer framework contract + linter + Java orchestrator. The `importexport-service` Temporal worker drives every import / export / public-API / webhook run via durable workflows with progress, signals, cancellation, checkpoint and compensation. Activities are idempotent with heartbeat; domain service is the source of truth. NO generic job-state / retry database is introduced (Temporal is the only scheduler per ADR-E0.5-07).
- Requirements: R12.2, R12.6, R16.6, NFR1, NFR3, NFR8.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/importexport/temporal-transfer-framework-policy.yaml` (new, 14820 bytes) | Single source of truth for E9.1. 12 closed-set vocabularies: transferWorkflowKinds[11] (IMPORT_GEDCOM, IMPORT_PREVIEW, IMPORT_DEDUP, IMPORT_CONFIRM, IMPORT_CHUNK, IMPORT_RECONCILE, EXPORT_BRANCH, EXPORT_FULL, EXPORT_REDACTED_PREVIEW, PUBLIC_API_REQUEST, WEBHOOK_DISPATCH), transferSignals[7], transferQueries[6], transferPhases[10], transferStatuses[10], transferCheckpointStates[6], transferActivityKinds[19], transferCompensationActions[7], transferFailureReasons[23], transferAuditEvents[18], transferActivityStatuses[8], transferIdempotencyOutcomes[5]. `dnaBucketAccess=FORBIDDEN` + `dnaBucketPrefixes=[dna/raw, dna/match, dna/consent]` (ADR-E0.5-15). 27 boolean guards including `temporalIsOnlyScheduler`, `customJobStateDatabaseForbidden`, `customRetryDatabaseForbidden`, `workflowDeterministicRequired`, `activityIdempotencyRequired`, `activityHeartbeatRequired`, `checkpointVersioned`, `compensationReversible`, `compensationIdempotent`, `outboxRelaySeparated`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `rawPayloadInWorkflowInputForbidden`, `secretInWorkflowInputForbidden`, `auditHookOnEveryPhaseAdvance`, `auditHookOnEveryCompensation`, `adrRequiredBeforeExternalScheduler`. 24 numeric bounds (`workflowMaxSignalsPerSecond=10`, `workflowMaxActivityAttempts=8`, `workflowMaxHistoryLengthEvents=50000`, `workflowMaxExecutionDurationSeconds=86400`, `workflowMaxCheckpointBytes=1048576`, `activityScheduleToCloseTimeoutSeconds=600`, `activityStartToCloseTimeoutSeconds=310`, `activityHeartbeatIntervalSeconds=30`, `activityHeartbeatTimeoutMultiplier=3`, `activityRetryMaximumAttempts=8`, `transferSecretMaximumBytes=0`). 3 state matrices (transferRunStateMatrix initial=QUEUED, transferCheckpointStateMatrix initial=NONE, transferActivityStateMatrix initial=SCHEDULED). 1 invariant (startToClose ≥ 10 × heartbeat). Sandbox egress allowlist `[postgres, apicurio, vault-agent, openfga, audit-service, kafka-broker, temporal-frontend]`. Outbox events `gp.importexport.v1.TransferQueued` / `TransferFinalized`. |
| `platform/helm/genealogy-platform/files/temporal-transfer-framework-policy.yaml` (byte-equal) | Chart mirror. |
| `scripts/lint-yaml.mjs` (extended) | Exported `assertClosedSet(name, expected, actual, label, ok, fail)` + `assertStateMatrix(label, matrix, expectedStatuses, initialStatus, ok, fail)` so every E9 linter shares the same closed-set / reachability / terminal-empty-transitions assertion. |
| `scripts/lint-temporal-transfer-framework.mjs` (new, ~480 lines) | E9.1 deep validator: 12 closed-set checks + sandbox egress allowlist + 3 state matrices + 27 boolean guards + `dnaBucketAccess MUST equal FORBIDDEN` + 24 numeric bounds + 1 invariant + outbox envelope + 18 audit hooks + forbidden payload patterns + capability boundaries + chart mirror byte-equality. |
| `scripts/__tests__/lint-temporal-transfer-framework.test.mjs` (new, 4 tests) | Pins: canonical contract exits 0; missing workflow kind fails; forbidden payload line printed; chart mirror byte-equal confirmation. |
| `package.json` (extended) | New `lint:temporalTransfer` / `check:temporalTransfer` + umbrella `lint:importexport` / `check:importexport` scripts. Wired into `check:platform` (after `lint:search:benchmark`) + `ci:full` (same position). |
| `services/importexport-service/build.gradle.kts` (updated) | Applies `java-conventions` + `service-conventions` plugins (Java 21, Checkstyle, ArchUnit boundary guard). |
| `services/importexport-service/src/main/java/.../shared/ImportExportLimits.java` (new) | Centralised numeric caps for every E9.x orchestrator. |
| `services/importexport-service/src/main/java/.../shared/DnaBucketPrefixes.java` (new) | Closed-set DNA bucket prefix list + `isForbidden(prefix)` helper. |
| `services/importexport-service/src/main/java/.../shared/ForbiddenPayloadKeys.java` (new) | Closed-set forbidden payload key list + opaque aggregate-id regex check. |
| `services/importexport-service/src/main/java/.../transfer/TransferWorkflowKind.java` (new) | Closed-set enum (11 values) + `wire()` / `fromWire(wire)` round-trip. |
| `services/importexport-service/src/main/java/.../transfer/TransferStatus.java` (new) | Closed-set enum (10 values) + `isTerminal()` helper. |
| `services/importexport-service/src/main/java/.../transfer/TransferRunValidator.java` (new) | Pure orchestrator: `validate(TransferRequest)` → `TransferValidationOutcome.ok/failed`. Refuses null workflow kind, blank runId, missing idempotency key, any forbidden payload key (DNA → `TRANSFER_DNA_BUCKET_FORBIDDEN`; PII → `TRANSFER_PII_LEAK_DETECTED`), `containsSecret=true` → `TRANSFER_SECRETS_IN_PAYLOAD`, blank tenant → `TRANSFER_TENANT_MISMATCH`. |
| `services/importexport-service/src/test/java/.../transfer/TransferRunValidatorTest.java` (new, 10 tests) | Pins: valid → ok; null kind → fail; DNA payload → `TRANSFER_DNA_BUCKET_FORBIDDEN`; PII payload → `TRANSFER_PII_LEAK_DETECTED`; secret → `TRANSFER_SECRETS_IN_PAYLOAD`; blank tenant → `TRANSFER_TENANT_MISMATCH`; enum wire round-trip; terminal status check; limits match contract. |
| `.kiro/specs/genealogy-platform/tasks.md` (E9.1 checkbox) | Flipped `- [ ] E9.1 …` → `- [x] E9.1 …` after evidence file committed. |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** — every closed-set vocabulary anchored to the YAML contract.
- **ADR-E0.5-01 (Java 21 toolchain): ACCEPTED** — `importexport-service/build.gradle.kts` applies the convention plugins.
- **ADR-E0.5-07 (Temporal is the only scheduler): ACCEPTED** — `temporalIsOnlyScheduler=true` + `customJobStateDatabaseForbidden=true` + `customRetryDatabaseForbidden=true`. The orchestrator only validates inputs; the actual Temporal worker wires in E9.x.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket keys blocked end-to-end.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-temporal-transfer-framework.mjs` | PASS | 200+ OK lines, exit 0 |
| `node --test scripts/__tests__/lint-temporal-transfer-framework.test.mjs` | PASS | 4/4 tests pass |
| `pnpm check:importexport` (all 6 linters + all 6 unit tests) | PASS | 14/14 tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks executed, exit 0 |
| `pnpm check:boundary` | PASS | scanned 11 service(s); no cross-service db/domain imports |
| `pnpm lint:ownership` | PASS | 37 declared paths, all per-directory OWNERS present |
| `pnpm check:gradle:lock` | PASS | every lockAll subproject has gradle.lockfile |
| `diff -q contracts/importexport/temporal-transfer-framework-policy.yaml platform/helm/.../temporal-transfer-framework-policy.yaml` | PASS | byte-equal (14820 bytes) |

## Security and privacy

- Tenant isolation: PASS — `tenantBoundaryOnEveryRepository=true` + `tenantPseudoId` mandatory on every request.
- Authorization/ABAC: N/A at the framework layer (per-orchestrator ABAC overlay lives in E9.5 public-API / E9.6 webhook).
- Secret/PII/DNA leakage: PASS — `dnaBucketAccess=FORBIDDEN` + DNA bucket shield + secret bytes MUST equal 0 + forbidden payload patterns (24 entries incl. `rawDnaSequence`, `rawFastq`, `rawVcf`, `rawEmail`, `rawPhone`, `biometricTemplate`, `productionPii`).
- Dependency/config scan: N/A — no new dependencies.

## Acceptance criteria

- [x] Closed-set vocabularies pinned + linter enforced (12 sets)
- [x] Sandbox egress allowlist pinned + linter enforced (7 entries)
- [x] 3 state matrices (transferRunStateMatrix / transferCheckpointStateMatrix / transferActivityStateMatrix) reachable from initial; terminals have empty transition lists
- [x] Boolean guard rails pinned to exact values (27)
- [x] Numeric bounds pinned to exact values (24)
- [x] Reconciled invariant enforced (activity startToClose ≥ 10 × heartbeat)
- [x] Outbox envelope fields declared for every outbox event
- [x] Audit hooks declared for every required event (18)
- [x] DNA bucket shield enforced (3 prefixes)
- [x] Cross-service references opaque (transfer never joins)
- [x] Chart mirror byte-equal

## Residual risks

- Actual Temporal worker + namespace isolation + outbox relay
  wiring lands in later sub-epic (E9.x / E11.x). The orchestrator
  today is the contract-side guardrail only.
- `TransferRequest` constructor enforces the closed-set contract;
  the runtime worker will need to deserialise Avro payloads through
  the same contract-check step before calling the orchestrator.