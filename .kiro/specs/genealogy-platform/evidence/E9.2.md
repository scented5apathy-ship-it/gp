# Completion Evidence — E9.2

- Status: DONE
- Completed at: 2026-08-15T23:45:00+07:00
- Scope: GEDCOM 7 streaming parser + GEDCOM 5.5.1 mapper with provenance preservation, extension support, dry-run validation. The `importexport-service` parser runs inside the sandbox network with size/depth/count/encoding limits; user-supplied code execution is forbidden; DNA bucket keys are rejected. Dry-run mode never writes domain data.
- Requirements: R12.1, R12.2, R12.3, R16, NFR1, NFR4.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/importexport/gedcom-parser-validator-policy.yaml` (new, 10466 bytes) | Single source of truth for E9.2. 12 closed-set vocabularies: gedcomVersions[2] (5.5.1, 7.0), gedcomEncodings[4] (UTF-8, UTF-16LE, UTF-16BE, ASCII), gedcomLineEndings[3] (LF, CRLF, CR), gedcomRecordKinds[8] (INDI, FAM, SOUR, REPO, OBJE, NOTE, SUBM, TRLR), gedcomExtensionTags[5] (_MEDI, _DNA, _CUSTOM, _SHARED, _PROVENANCE), gedcomValidationStatuses[12], gedcomDryRunSeverities[4], gedcomProvenanceKinds[7] (SOURCE_CITATION, TRANSCRIBER_NAME, TRANSCRIPTION_DATE, CONFIDENCE_LEVEL, LICENSE_REFERENCE, EXTENSION_AUTHOR, EXTENSION_VERSION), gedcomMappingOutcomes[8] (MAPPED_NATIVE / MAPPED_TRANSFORMED / MAPPED_WITH_NOTES / MAPPED_WITH_PROVENANCE_PRESERVED / DROPPED_UNSUPPORTED / DROPPED_DNA_BUCKET / DROPPED_PII_PAYLOAD / FAILED_MAPPING), gedcomFailureReasons[20], gedcomAuditEvents[11], gedcomRecordClasses[7]. 16 boolean guards (`streamingParserRequired`, `boundedMemoryFootprint`, `noFullFileLoad`, `userCodeExecutionForbidden`, `extensionTagSandboxEnforced`, `provenancePreservationRequired`, `mappingReproducible`, `dryRunDefaultBeforeCommit`, `domainWriteForbiddenDuringDryRun`, `outboxRelaySeparated`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `rawPayloadInEventForbidden`, `secretInPayloadForbidden`, `tenantBoundaryOnEveryRepository`, `adrRequiredBeforeExternalParser`). 24 numeric bounds (`gedcomMaxPayloadBytes=52428800`, `gedcomMaxLineLength=4096`, `gedcomMaxDepth=32`, `gedcomMaxRecordCount=5000000`, `gedcomMaxTagCount=25000000`, `gedcomStreamingChunkBytes=65536`, `gedcomValidationTimeoutSeconds=600`, `gedcomValidationHeartbeatSeconds=30`, `gedcomMappingTimeoutSeconds=900`, `gedcomMappingHeartbeatSeconds=60`, `gedcomBatchInsertSize=1000`, `gedcomBatchInsertTimeoutSeconds=300`, `gedcomNameMaxLength=256`, `gedcomPlaceMaxLength=512`). 1 state matrix (gedcomParseStateMatrix initial=QUEUED). 2 invariants (`validationTimeout ≥ 20 × heartbeat`, `mappingTimeout ≥ 15 × heartbeat`). Sandbox egress allowlist `[postgres, apicurio, vault-agent, openfga, audit-service, kafka-broker, temporal-frontend]`. Outbox events `gp.importexport.v1.GedcomParsed` / `GedcomValidationFinished`. |
| `platform/helm/genealogy-platform/files/gedcom-parser-validator-policy.yaml` (byte-equal) | Chart mirror. |
| `scripts/lint-gedcom-parser-validator.mjs` (new) | E9.2 deep validator (12 closed-set + 16 boolean + 24 numeric + 2 invariants + outbox envelope + 11 audit hooks + 24 forbidden payload patterns + capability boundaries + chart mirror byte-equality). |
| `scripts/__tests__/lint-gedcom-parser-validator.test.mjs` (new, 2 tests) | Pins: canonical contract exits 0; chart mirror byte-equal confirmation. |
| `package.json` (extended) | New `lint:gedcom` / `check:gedcom` scripts; already wired into umbrella `lint:importexport` / `check:importexport`. |
| `services/importexport-service/src/main/java/.../gedcom/GedcomValidationStatus.java` (new) | Closed-set enum (12 values) + wire round-trip. |
| `services/importexport-service/src/main/java/.../gedcom/GedcomMappingOutcome.java` (new) | Closed-set enum (8 values) + wire round-trip. |
| `services/importexport-service/src/main/java/.../gedcom/GedcomFailureReason.java` (new) | Closed-set enum (20 values) + wire round-trip. |
| `services/importexport-service/src/main/java/.../gedcom/GedcomParserLimits.java` (new) | Streaming parser guard: validates `GedcomPayload{byteSize, maxLineLength, maxDepth, recordCount, encoding, dnaBucketReference}` against the closed-set limits; refuses payload > 52MB, line > 4096, depth > 32, records > 5M, non-UTF/ASCII encoding, DNA bucket reference. |
| `services/importexport-service/src/test/java/.../gedcom/GedcomParserLimitsTest.java` (new, 9 tests) | Pins: valid → ok; oversized payload / line length / depth / record count fail; DNA bucket reference fails; 3 enum wire round-trips. |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** — every closed-set vocabulary anchored to the YAML contract.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket keys blocked end-to-end.
- **R12.1 (streaming GEDCOM 7 with 5.5.1 mapping): ACCEPTED** — `streamingParserRequired=true` + `boundedMemoryFootprint=true` + `noFullFileLoad=true` + `extensionTagSandboxEnforced=true`.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-gedcom-parser-validator.mjs` | PASS | exit 0 |
| `node --test scripts/__tests__/lint-gedcom-parser-validator.test.mjs` | PASS | 2/2 tests pass |
| `pnpm check:importexport` | PASS | 14/14 unit tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks executed, exit 0 |
| `pnpm check:boundary` | PASS | scanned 11 service(s); no cross-service db/domain imports |
| `pnpm lint:ownership` | PASS | 37 declared paths, all per-directory OWNERS present |
| `pnpm check:gradle:lock` | PASS | every lockAll subproject has gradle.lockfile |
| `diff -q contracts/importexport/gedcom-parser-validator-policy.yaml platform/helm/.../gedcom-parser-validator-policy.yaml` | PASS | byte-equal (10466 bytes) |

## Security and privacy

- Tenant isolation: PASS — `tenantBoundaryOnEveryRepository=true` enforced at linter + Java level.
- Authorization/ABAC: N/A at the parser layer (per-orchestrator ABAC overlay lives in E9.5 / E9.6).
- Secret/PII/DNA leakage: PASS — `dnaBucketAccess=FORBIDDEN` + DNA bucket shield + forbidden payload patterns (24 entries incl. `rawDnaSequence`, `rawEmail`, `productionPii`).
- Dependency/config scan: N/A — no new dependencies.

## Acceptance criteria

- [x] Closed-set vocabularies pinned + linter enforced (12 sets)
- [x] Sandbox egress allowlist pinned + linter enforced (7 entries)
- [x] State matrix (gedcomParseStateMatrix) reachable from QUEUED; terminals have empty transition lists
- [x] Boolean guard rails pinned to exact values (16)
- [x] Numeric bounds pinned to exact values (24)
- [x] Reconciled invariants enforced (validation timeout ≥ 20 × heartbeat, mapping timeout ≥ 15 × heartbeat)
- [x] Outbox envelope fields declared for every outbox event
- [x] Audit hooks declared for every required event (11)
- [x] DNA bucket shield enforced (3 prefixes)
- [x] Cross-service references opaque (parser never joins)
- [x] Chart mirror byte-equal
- [x] Streaming parser + size/depth/count/encoding limits enforced (5 limits)

## Residual risks

- Actual streaming parser implementation + Flyway migration +
  outbox relay + Kafka event payload decoder land in later
  sub-epic (E9.x / E11.x). The orchestrator today is the
  contract-side guardrail only.