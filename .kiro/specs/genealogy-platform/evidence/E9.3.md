# Completion Evidence — E9.3

- Status: DONE
- Completed at: 2026-08-15T23:45:00+07:00
- Scope: Import mapping + dedup + saga contract. The `importexport-service` saga drives the user-facing state machine preview → user confirmation → chunked commit with checkpoint / compensation. Domain service remains the source of truth; no long transactions; reconciliation events fire on completion; DNA bucket shield enforced.
- Requirements: R12.2, R12.3, R12.5, R16, NFR1, NFR3, NFR4.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/importexport/import-saga-policy.yaml` (new, 11128 bytes) | Single source of truth for E9.3. 10 closed-set vocabularies: importSagaPhases[10] (PREVIEWING / MAPPING / DEDUPING / CONFIRMING / CHUNKING / COMMITTING / RECONCILING / COMPENSATING / CANCELLED / FINALIZED), importSagaStatuses[13] (incl. PREVIEWING + PREVIEW_READY), importMappingStrategies[6] (EXACT_KEY_MATCH / FUZZY_NAME_MATCH / PLACE_NORMALIZED_MATCH / DATE_NORMALIZED_MATCH / CITATION_PROVENANCE_MATCH / MANUAL_OVERRIDE), importDedupOutcomes[6] (NEW / DUPLICATE_AUTO_MERGE / DUPLICATE_CANDIDATE / DUPLICATE_USER_MERGED / DUPLICATE_REJECTED / DRY_RUN_REPORT_ONLY), importChunkCommitStrategies[4], importReconciliationTargets[5] (SEARCH_PROJECTION / COLLABORATION_TIMELINE / MEDIA_INDEX / TREE_RENDERER_CACHE / PUBLIC_PROJECTION), importSagaFailureReasons[14], importSagaAuditEvents[13], importSagaConfirmationModes[5] (USER_ACCEPT_ALL / USER_ACCEPT_PER_CANDIDATE / USER_ACCEPT_PER_RECORD / ADMIN_FORCE_COMMIT / DRY_RUN_ONLY), importSagaCompensationActions[5]. 19 boolean guards (`previewBeforeCommitRequired`, `userConfirmationRequired`, `dryRunBeforeChunking`, `chunkCommitBoundedParallelism`, `compensationReversible`, `compensationIdempotent`, `checkpointVersioned`, `outboxRelaySeparated`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `tenantBoundaryOnEveryRepository`, `longTransactionForbidden`, `secretInPayloadForbidden`, `auditHookOnEveryChunkCommit`, `auditHookOnEveryCompensation`, `adrRequiredBeforeExternalMatcher`). 24 numeric bounds (`importPreviewMaxRecords=1000000`, `importPreviewMaxCandidatesPerRecord=50`, `importPreviewExpirySeconds=604800`, `importChunkMaxRecords=1000`, `importChunkMaxBytes=10485760`, `importChunkMaxConcurrentCommits=8`, `importChunkCommitTimeoutSeconds=300`, `importChunkCommitHeartbeatSeconds=30`, `importChunkBackoffCoefficient=2`, `importCompensationTimeoutSeconds=600`, `importCompensationHeartbeatSeconds=30`, `importReconciliationTimeoutSeconds=900`, `importReconciliationHeartbeatSeconds=60`, `importDedupFuzzyThreshold=0.85`, `importNameMaxLength=256`, `importAliasMaxLength=256`, `importAliasPerRecordMax=64`, `importUserPayloadMaxBytes=52428800`). 1 state matrix (importSagaStateMatrix initial=QUEUED, 13 statuses). 3 invariants (`chunkCommitTimeout ≥ 10 × heartbeat`, `compensationTimeout ≥ 20 × heartbeat`, `reconciliationTimeout ≥ 15 × heartbeat`). Sandbox egress allowlist. Outbox events `gp.importexport.v1.ImportPreviewReady` / `ImportChunkCommitted` / `ImportReconciled`. |
| `platform/helm/genealogy-platform/files/import-saga-policy.yaml` (byte-equal) | Chart mirror. |
| `scripts/lint-import-saga.mjs` (new) | E9.3 deep validator (10 closed-set + 19 boolean + 24 numeric + 3 invariants + outbox envelope + 13 audit hooks + 24 forbidden payload patterns + capability boundaries + chart mirror byte-equality). |
| `scripts/__tests__/lint-import-saga.test.mjs` (new, 2 tests) | Pins: canonical contract exits 0; chart mirror byte-equal confirmation. |
| `package.json` (extended) | New `lint:importSaga` / `check:importSaga` scripts. |
| `services/importexport-service/src/main/java/.../saga/ImportDedupOutcome.java` (new) | Closed-set enum (6 values) + wire round-trip. |
| `services/importexport-service/src/main/java/.../saga/ImportChunkCommitValidator.java` (new) | Pure orchestrator: `validate(ChunkCommitCommand{records, byteSize, dedupOutcome, tenantPseudoId, dnaBucketReference, piiLeakDetected})` → refuses oversized chunk (records > 1000 or bytes > 10MB), unknown dedup outcome, blank tenant, DNA bucket reference, PII leak. |
| `services/importexport-service/src/test/java/.../saga/ImportChunkCommitValidatorTest.java` (new, 7 tests) | Pins: valid → ok; oversized records / oversized bytes fail; DNA bucket reference fails; PII leak fails; tenant mismatch fails; enum wire round-trip. |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** — every closed-set vocabulary anchored to the YAML contract.
- **ADR-E0.5-07 (Temporal is the only scheduler): ACCEPTED** — saga runs through Temporal, no custom job-state DB.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket keys blocked end-to-end.
- **R12.3 (preview + dedup + user confirmation + chunked commit): ACCEPTED** — `previewBeforeCommitRequired=true` + `userConfirmationRequired=true` + `dryRunBeforeChunking=true` + chunked commit instead of long transactions.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-import-saga.mjs` | PASS | exit 0 |
| `node --test scripts/__tests__/lint-import-saga.test.mjs` | PASS | 2/2 tests pass |
| `pnpm check:importexport` | PASS | 14/14 unit tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks executed, exit 0 |
| `pnpm check:boundary` | PASS | scanned 11 service(s); no cross-service db/domain imports |
| `pnpm lint:ownership` | PASS | 37 declared paths, all per-directory OWNERS present |
| `pnpm check:gradle:lock` | PASS | every lockAll subproject has gradle.lockfile |
| `diff -q contracts/importexport/import-saga-policy.yaml platform/helm/.../import-saga-policy.yaml` | PASS | byte-equal (11128 bytes) |

## Security and privacy

- Tenant isolation: PASS — `tenantBoundaryOnEveryRepository=true` + tenant pseudo id mandatory.
- Authorization/ABAC: N/A at the saga layer (per-orchestrator ABAC overlay lives in E9.5 / E9.6).
- Secret/PII/DNA leakage: PASS — `dnaBucketAccess=FORBIDDEN` + DNA bucket shield + PII leak detection + `secretInPayloadForbidden=true` + forbidden payload patterns (24 entries incl. `rawDnaSequence`, `rawEmail`, `productionPii`).
- Dependency/config scan: N/A — no new dependencies.

## Acceptance criteria

- [x] Closed-set vocabularies pinned + linter enforced (10 sets)
- [x] Sandbox egress allowlist pinned + linter enforced (7 entries)
- [x] State matrix (importSagaStateMatrix) reachable from QUEUED; terminals have empty transition lists
- [x] Boolean guard rails pinned to exact values (19)
- [x] Numeric bounds pinned to exact values (24)
- [x] Reconciled invariants enforced (3 invariants)
- [x] Outbox envelope fields declared for every outbox event (3 events)
- [x] Audit hooks declared for every required event (13)
- [x] DNA bucket shield enforced (3 prefixes)
- [x] Cross-service references opaque (saga never joins)
- [x] Chart mirror byte-equal
- [x] Preview + dedup + user confirmation + chunked commit flow pinned

## Residual risks

- Actual Temporal saga implementation + reconciliation Temporal
  worker + Kafka reconciliation consumer + dedup matcher (trigram
  / OpenSearch per ADR-E0.5-09 evolution gate) land in later
  sub-epic. The orchestrator today is the contract-side guardrail
  only.