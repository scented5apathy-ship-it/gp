# Completion Evidence — E9.4

- Status: DONE
- Completed at: 2026-08-15T23:45:00+07:00
- Scope: Privacy-aware export contract. The `importexport-service` supports full / branch GEDCOM 7, GEDCOM 5.5.1, CSV, JSON, PDF, media bundle (zip/tar) exports with redaction preview, DNA default-off + consent receipt requirement, signed short-lived URL, audit + retention cleanup, checksum manifest. Scope → minimum redaction floor mapping is enforced; legal hold respected.
- Requirements: R12.3, R12.4, R13, R16, NFR1, NFR3, NFR4.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/importexport/privacy-export-policy.yaml` (new, 11070 bytes) | Single source of truth for E9.4. 9 closed-set vocabularies + 1 scope → floor mapping: exportScopes[6] (FULL_TREE / BRANCH_SUBGRAPH / PERSON_CENTRIC / ANCESTORS_ONLY / DESCENDANTS_ONLY / FAMILY_UNIT), exportFormats[7] (GEDCOM_7 / GEDCOM_5_5_1 / CSV / JSON / PDF / MEDIA_BUNDLE_ZIP / MEDIA_BUNDLE_TAR), exportBundleComponents[9], exportRedactionLevels[7] (NONE / LIVING_ONLY / MINOR_ONLY / LIVING_AND_MINOR / SENSITIVE_FULL / DNA_DEFAULT_OFF / CONSENT_REQUIRED), exportRedactionOutcomes[7], exportConsentReceipts[6] (DNA_PURPOSE_OWN / DNA_PURPOSE_FAMILY_MATCH / DNA_PURPOSE_RESEARCH / FULL_TREE_LIVING_MINOR / FULL_TREE_PUBLIC_RELEASE / MEDIA_PUBLIC_RELEASE), exportSignLifecycleStatuses[7] (PENDING / SIGNED / ACTIVE / EXPIRED / REVOKED / DOWNLOADED / CLEANED_UP), exportRetentionPolicies[4], exportFailureReasons[20], exportAuditEvents[15]. `exportScopesToRedactionFloor.pairs` mapping: FULL_TREE → LIVING_AND_MINOR; BRANCH_SUBGRAPH / PERSON_CENTRIC / ANCESTORS_ONLY / DESCENDANTS_ONLY / FAMILY_UNIT → LIVING_ONLY. 21 boolean guards (`privacyFirstByDefault`, `dnaDefaultOff`, `dnaRequiresExplicitConsent`, `livingRedactedByDefault`, `minorRedactedByDefault`, `redactionPreviewBeforeCommit`, `signedUrlShortLived`, `signedUrlRevocable`, `checksumManifestRequired`, `auditOnEveryDownload`, `retentionSchedulerRequired`, `outboxRelaySeparated`, `dnaBucketAccess=FORBIDDEN`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `tenantBoundaryOnEveryRepository`, `secretInBundleForbidden`, `adrRequiredBeforeExternalRedactor`, `legalHoldRespected`). 24 numeric bounds (`exportBundleMaxBytes=524288000`, `exportBundleMediaMaxBytes=1073741824`, `exportBundleMaxFiles=100000`, `exportSignUrlTtlSeconds=3600`, `exportSignUrlRevocationPropagationSeconds=30`, `exportDownloadMaxConcurrent=4`, `exportRetentionCleanupIntervalSeconds=3600`, `exportBundleTimeoutSeconds=900`, `exportBundleHeartbeatSeconds=60`, `exportNameMaxLength=256`, `exportDnaDefaultOffFloor=0`). 1 state matrix (exportBundleStateMatrix initial=PENDING). 3 invariants (`bundleTimeout ≥ 15 × heartbeat`, `signUrlTtl ≥ 120 × revocationPropagation`, `dnaDefaultOffFloor=0`). Outbox events `gp.importexport.v1.ExportBundleSigned` / `ExportDownloadRevoked` / `ExportRetentionCleanedUp`. |
| `platform/helm/genealogy-platform/files/privacy-export-policy.yaml` (byte-equal) | Chart mirror. |
| `scripts/lint-privacy-export.mjs` (new) | E9.4 deep validator (9 closed-set + scope → floor mapping check + 21 boolean + 24 numeric + 3 invariants + outbox envelope + 15 audit hooks + 24 forbidden payload patterns + capability boundaries + chart mirror byte-equality). |
| `scripts/__tests__/lint-privacy-export.test.mjs` (new, 2 tests) | Pins: canonical contract exits 0; chart mirror byte-equal confirmation. |
| `package.json` (extended) | New `lint:privacyExport` / `check:privacyExport` scripts. |
| `services/importexport-service/src/main/java/.../export/ExportRedactionLevel.java` (new) | Closed-set enum (7 values) + wire round-trip + `isDnaAllowed()` matrix (only SENSITIVE_FULL + CONSENT_REQUIRED allow DNA). |
| `services/importexport-service/src/main/java/.../export/PrivacyAwareExportBuilder.java` (new) | Pure orchestrator: `validate(ExportBuildRequest{scope, format, redactionLevel, byteSize, containsDnaContent, consentReceipt, dnaBucketReference, tenantPseudoId, expectedTenantPseudoId, signUrlTtlSeconds, revocationPropagationSeconds})` → refuses unknown scope / format, oversized bundle (> 524MB), DNA bucket reference, missing / wrong-purpose consent receipt when DNA allowed, DNA content without `isDnaAllowed()` level, tenant mismatch, sign URL TTL < 120 × revocation propagation. |
| `services/importexport-service/src/test/java/.../export/PrivacyAwareExportBuilderTest.java` (new, 11 tests) | Pins: valid → ok; DNA content without allow fails; DNA content with allow but no consent fails; DNA content with consent passes; bundle too large fails; DNA bucket reference fails; tenant mismatch fails; sign URL TTL too short fails; enum wire round-trip; DNA allowance matrix (NONE / LIVING_ONLY / MINOR_ONLY / LIVING_AND_MINOR / DNA_DEFAULT_OFF = not allowed; SENSITIVE_FULL + CONSENT_REQUIRED = allowed); scope minimum redaction matrix. |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** — every closed-set vocabulary anchored to the YAML contract.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket keys blocked end-to-end.
- **R13 (DNA default-off + explicit consent): ACCEPTED** — `dnaDefaultOff=true` + `dnaRequiresExplicitConsent=true` + `exportDnaDefaultOffFloor=0`.
- **R16.6 (audit + retention): ACCEPTED** — `auditOnEveryDownload=true` + `retentionSchedulerRequired=true`.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-privacy-export.mjs` | PASS | exit 0 |
| `node --test scripts/__tests__/lint-privacy-export.test.mjs` | PASS | 2/2 tests pass |
| `pnpm check:importexport` | PASS | 14/14 unit tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks executed, exit 0 |
| `pnpm check:boundary` | PASS | scanned 11 service(s); no cross-service db/domain imports |
| `pnpm lint:ownership` | PASS | 37 declared paths, all per-directory OWNERS present |
| `pnpm check:gradle:lock` | PASS | every lockAll subproject has gradle.lockfile |
| `diff -q contracts/importexport/privacy-export-policy.yaml platform/helm/.../privacy-export-policy.yaml` | PASS | byte-equal (11070 bytes) |

## Security and privacy

- Tenant isolation: PASS — `tenantBoundaryOnEveryRepository=true` + tenant pseudo id mandatory + comparison.
- Authorization/ABAC: N/A at the export layer (per-orchestrator ABAC overlay lives in E9.5 / E9.6).
- Secret/PII/DNA leakage: PASS — `dnaBucketAccess=FORBIDDEN` + DNA bucket shield + `secretInBundleForbidden=true` + forbidden payload patterns (24 entries incl. `rawDnaSequence`, `rawEmail`, `productionPii`) + living/minor redacted by default + DNA requires explicit consent.
- Dependency/config scan: N/A — no new dependencies.

## Acceptance criteria

- [x] Closed-set vocabularies pinned + linter enforced (9 sets)
- [x] Scope → redaction floor mapping pinned (6 pairs)
- [x] Sandbox egress allowlist pinned + linter enforced (7 entries)
- [x] State matrix (exportBundleStateMatrix) reachable from PENDING; terminals have empty transition lists
- [x] Boolean guard rails pinned to exact values (21)
- [x] Numeric bounds pinned to exact values (24)
- [x] Reconciled invariants enforced (3 invariants)
- [x] Outbox envelope fields declared for every outbox event (3 events)
- [x] Audit hooks declared for every required event (15)
- [x] DNA bucket shield enforced (3 prefixes)
- [x] Cross-service references opaque (export never joins)
- [x] Chart mirror byte-equal
- [x] DNA default-off + consent receipt requirement pinned
- [x] Signed URL TTL ≥ 120 × revocation propagation pinned
- [x] Living/minor redaction default pinned
- [x] Legal hold respected pinned

## Residual risks

- Actual export bundle generator + Vault transit signing +
  Temporal retention schedule + checksum manifest signer land
  in later sub-epic. The orchestrator today is the contract-side
  guardrail only.