# Completion Evidence — E9.5

- Status: DONE
- Completed at: 2026-08-15T23:45:00+07:00
- Scope: Public API on Kong contract. The `importexport-service` exposes 8 public resources (PUBLIC_PERSON, PUBLIC_EVENT, PUBLIC_PLACE, PUBLIC_SOURCE, PUBLIC_CITATION, PUBLIC_MEDIA, PUBLIC_TREE, PUBLIC_ALBUM) through Kong edge routing. Kong handles routing / auth / rate limit / correlation id; the service does domain authorization through OpenFGA + ABAC overlay. OAuth scopes strictly map to OpenFGA tuples; idempotency key mandatory for writes + token redeem; quota metrics exported; abuse signal detection wired; public projection only.
- Requirements: R12.5, R16, NFR1, NFR4, NFR8.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/importexport/public-api-policy.yaml` (new, 10125 bytes) | Single source of truth for E9.5. 10 closed-set vocabularies: publicApiResources[8], publicApiMethods[3] (GET, HEAD, OPTIONS), publicApiScopes[7] (public.read.basic / public.read.living / public.read.media / public.read.tree / public.read.album / public.write.token / admin.read.abuse), publicApiIdempotencyKeys[4] (REQUIRED_FOR_WRITE / OPTIONAL_FOR_READ / REQUIRED_FOR_TOKEN_REDEEM / OPTIONAL_FOR_HEALTH), publicApiQuotaBuckets[5] (PER_IP_PER_MINUTE / PER_IP_PER_HOUR / PER_OAUTH_CLIENT_PER_MINUTE / PER_OAUTH_CLIENT_PER_HOUR / PER_TOKEN_PER_HOUR), publicApiRateLimitOutcomes[6], publicApiVersionStates[5] (CURRENT / PREVIOUS / DEPRECATED / SUNSET / RETIRED), publicApiFailureReasons[20], publicApiAuditEvents[12], publicApiQuotaMetrics[6] (REQUESTS_PER_MINUTE / REQUESTS_PER_HOUR / BYTES_PER_MINUTE / ERRORS_PER_MINUTE / TOKEN_REDEMPTIONS_PER_HOUR / ABUSE_SIGNALS_PER_HOUR). 21 boolean guards (`kongEdgeRoutingRequired`, `kongEdgeAuthRequired`, `kongEdgeRateLimitRequired`, `kongEdgeCorrelationIdRequired`, `openapiContractRequired`, `idempotencyKeyRequiredForWrites`, `idempotencyKeyReplaySafe`, `oauthScopeStrictMapping`, `scopeMustMapToOpenFga`, `domainAuthorizationInService`, `tenantBoundaryOnEveryRepository`, `outboxRelaySeparated`, `dnaBucketAccess=FORBIDDEN`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `publicProjectionOnly`, `abuseSignalDetectionRequired`, `contractCompatibilityChecked`, `quotaMetricsExported`, `adrRequiredBeforeExternalIdp`). 22 numeric bounds (`publicApiMaxPayloadBytes=1048576`, `publicApiMaxHeaderBytes=8192`, `publicApiMaxQueryBytes=4096`, `publicApiIdempotencyKeyLength=128`, `publicApiIdempotencyKeyTtlSeconds=86400`, `publicApiVersionSunsetSeconds=31536000`, `publicApiVersionRetiredSeconds=63072000`, `publicApiPerIpRequestsPerMinute=60`, `publicApiPerIpRequestsPerHour=1000`, `publicApiPerClientRequestsPerMinute=120`, `publicApiPerClientRequestsPerHour=5000`, `publicApiPerTokenRequestsPerHour=500`, `publicApiTokensPerClientPerHour=100`, `publicApiTokenTtlSeconds=86400`, `publicApiTokenRevocationPropagationSeconds=30`). 1 state matrix (publicApiTokenStateMatrix initial=ISSUED). 2 invariants (`perIpPerHour ≥ 16 × perIpPerMinute`, `perClientPerHour ≥ 41 × perClientPerMinute`). Sandbox egress allowlist. Outbox events `gp.importexport.v1.PublicApiRequestAllowed` / `PublicApiQuotaBreached`. |
| `platform/helm/genealogy-platform/files/public-api-policy.yaml` (byte-equal) | Chart mirror. |
| `scripts/lint-public-api.mjs` (new) | E9.5 deep validator (10 closed-set + 21 boolean + 22 numeric + 2 invariants + outbox envelope + 12 audit hooks + 24 forbidden payload patterns + capability boundaries + chart mirror byte-equality). |
| `scripts/__tests__/lint-public-api.test.mjs` (new, 2 tests) | Pins: canonical contract exits 0; chart mirror byte-equal confirmation. |
| `package.json` (extended) | New `lint:publicApi` / `check:publicApi` scripts. |
| `services/importexport-service/src/main/java/.../publicapi/PublicApiScope.java` (new) | Closed-set enum (7 scopes) + wire round-trip with `wire` lowercased. |
| `services/importexport-service/src/main/java/.../publicapi/PublicApiRequestGuard.java` (new) | Pure orchestrator: `authorize(PublicApiRequest{resource, method, scope, perIpPerMinute, perIpPerHour, perClientPerMinute, perClientPerHour, dnaBucketReference, abuseSignal, idempotencyKeyReuseConflict, tenantPseudoId, expectedTenantPseudoId})` → refuses null / non-GET-HEAD-OPTIONS method, scope not covering resource, per-IP-per-minute > 60, per-IP-per-hour > 1000, per-client-per-minute > 120, per-client-per-hour > 5000, tenant mismatch, DNA bucket reference, abuse signal, idempotency key reuse conflict. `scopeCoversResource()` matrix: basic covers all; living scopes for PUBLIC_PERSON; media scope for PUBLIC_MEDIA; tree scope for PUBLIC_TREE; album scope for PUBLIC_ALBUM. |
| `services/importexport-service/src/test/java/.../publicapi/PublicApiRequestGuardTest.java` (new, 9 tests) | Pins: valid → ok; null method fails; DNA bucket reference fails; abuse signal fails; rate limit exceeded fails; tenant mismatch fails; scope insufficient fails (PUBLIC_READ_TREE for PUBLIC_MEDIA); enum wire round-trip; idempotency key reuse conflict fails. |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** — every closed-set vocabulary anchored to the YAML contract.
- **ADR-E0.5-02 (Kong edge / runtime): ACCEPTED** — Kong handles routing / auth / rate limit / correlation id; domain authorization stays in service.
- **ADR-E0.5-06 (OpenFGA + ABAC overlay): ACCEPTED** — `scopeMustMapToOpenFga=true` + `domainAuthorizationInService=true`.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket keys blocked end-to-end.
- **R12.5 (OAuth scopes + idempotency key + OpenAPI): ACCEPTED** — `oauthScopeStrictMapping=true` + `idempotencyKeyRequiredForWrites=true` + `openapiContractRequired=true`.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-public-api.mjs` | PASS | exit 0 |
| `node --test scripts/__tests__/lint-public-api.test.mjs` | PASS | 2/2 tests pass |
| `pnpm check:importexport` | PASS | 14/14 unit tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks executed, exit 0 |
| `pnpm check:boundary` | PASS | scanned 11 service(s); no cross-service db/domain imports |
| `pnpm lint:ownership` | PASS | 37 declared paths, all per-directory OWNERS present |
| `pnpm check:gradle:lock` | PASS | every lockAll subproject has gradle.lockfile |
| `diff -q contracts/importexport/public-api-policy.yaml platform/helm/.../public-api-policy.yaml` | PASS | byte-equal (10125 bytes) |

## Security and privacy

- Tenant isolation: PASS — `tenantBoundaryOnEveryRepository=true` + tenant pseudo id mandatory + comparison.
- Authorization/ABAC: PASS — `domainAuthorizationInService=true` + `scopeMustMapToOpenFga=true` + scope → resource mapping enforced in orchestrator.
- Secret/PII/DNA leakage: PASS — `dnaBucketAccess=FORBIDDEN` + DNA bucket shield + `publicProjectionOnly=true` + forbidden payload patterns (24 entries incl. `rawDnaSequence`, `rawEmail`, `productionPii`).
- Dependency/config scan: N/A — no new dependencies.

## Acceptance criteria

- [x] Closed-set vocabularies pinned + linter enforced (10 sets)
- [x] Sandbox egress allowlist pinned + linter enforced (7 entries)
- [x] State matrix (publicApiTokenStateMatrix) reachable from ISSUED; terminals have empty transition lists
- [x] Boolean guard rails pinned to exact values (21)
- [x] Numeric bounds pinned to exact values (22)
- [x] Reconciled invariants enforced (perIpPerHour ≥ 16 × perIpPerMinute, perClientPerHour ≥ 41 × perClientPerMinute)
- [x] Outbox envelope fields declared for every outbox event (2 events)
- [x] Audit hooks declared for every required event (12)
- [x] DNA bucket shield enforced (3 prefixes)
- [x] Cross-service references opaque (public API never joins)
- [x] Chart mirror byte-equal
- [x] Kong edge / auth / rate limit / correlation id pinned
- [x] OAuth scope → OpenFGA + ABAC overlay mapping pinned
- [x] Idempotency key mandatory for writes + token redeem pinned
- [x] Quota metrics exported pinned
- [x] Abuse signal detection pinned

## Residual risks

- Actual OpenAPI spec + Kong route integration + Keycloak scope
  registry + per-IP/per-client rate limit Redis bucket + abuse
  signal ML model land in later sub-epic (E9.x / E11.x). The
  orchestrator today is the contract-side guardrail only.