# Completion Evidence — E9.6

- Status: DONE
- Completed at: 2026-08-15T23:45:00+07:00
- Scope: Webhooks contract. The `importexport-service` webhook dispatcher supports subscription authorization, signed payload (HMAC-SHA-256 / HMAC-SHA-512 / ED25519), secret rotation (ACTIVE → ROTATING → RETIRED / REVOKED), retry with exponential backoff + jitter, dead-letter with audited replay, event minimization (OPAQUE_REFERENCE / FULL / REDACTED / METADATA_ONLY), disable / revoke on tenant revocation + repeated abuse, id/secret/payload limits, DNA bucket shield + PII leak detection.
- Requirements: R12.6, R16, NFR1, NFR3, NFR4.

## Changes

| File / group | Why |
| --- | --- |
| `contracts/importexport/webhook-policy.yaml` (new, 11142 bytes) | Single source of truth for E9.6. 10 closed-set vocabularies: webhookSubscriptionKinds[8] (TREE_EVENTS / MEDIA_EVENTS / ALBUM_EVENTS / COLLABORATION_EVENTS / RESEARCH_EVENTS / SEARCH_ALERTS / IMPORT_FINALIZED / EXPORT_FINALIZED), webhookDeliveryStatuses[8] (QUEUED / DISPATCHING / SIGNED / DELIVERED / RETRY_SCHEDULED / DEAD_LETTERED / REVOKED / DISABLED), webhookSecretStates[4] (ACTIVE / ROTATING / RETIRED / REVOKED), webhookSignatureAlgorithms[3] (HMAC_SHA_256 / HMAC_SHA_512 / ED25519), webhookRetryPolicies[3] (EXPONENTIAL_BACKOFF / EXPONENTIAL_BACKOFF_JITTER / LINEAR_BACKOFF), webhookDeadLetterOutcomes[5] (PARKED / AUDITED_REPLAY_QUEUED / AUDITED_REPLAY_FINISHED / AUDITED_REPLAY_FAILED / DISCARDED_BY_TENANT), webhookFailureReasons[20], webhookAuditEvents[13], webhookEventMinimizationModes[4], webhookDisabledReasons[8] (TENANT_REVOKED / USER_REVOKED / CONSENT_REVOKED / TARGET_REPEATED_FAILURE / RATE_LIMIT_REPEATED_BREACH / SECRET_ROTATION_FAILED / ABUSE_SIGNAL_DETECTED / ADMIN_ACTION). 20 boolean guards (`subscriptionAuthorizationRequired`, `signedPayloadMandatory`, `signedPayloadVerifiableInReceiver`, `eventMinimizationByDefault`, `secretRotationRequired`, `secretRotationAudited`, `retryIdempotent`, `deadLetterAudited`, `replayRequiresAudit`, `tenantBoundaryOnEveryRepository`, `outboxRelaySeparated`, `dnaBucketAccess=FORBIDDEN`, `crossServiceReferencesAreOpaque`, `crossServiceReferencesRequirePublisherResolution`, `revokeOnTenantRevocation`, `disableOnRepeatedAbuse`, `secretInPlainPayloadForbidden`, `adrRequiredBeforeExternalDispatcher`). 22 numeric bounds (`webhookMaxPayloadBytes=1048576`, `webhookMaxHeaderBytes=8192`, `webhookMaxDeliveryAttempts=8`, `webhookInitialBackoffSeconds=5`, `webhookMaxBackoffSeconds=3600`, `webhookBackoffCoefficient=2`, `webhookJitterSeconds=30`, `webhookTargetResponseTimeoutSeconds=30`, `webhookTargetConnectionTimeoutSeconds=10`, `webhookSecretRotationGraceSeconds=604800`, `webhookReplayMaxAgeSeconds=604800`, `webhookDispatcherHeartbeatSeconds=30`, `webhookDispatcherTimeoutSeconds=600`, `webhookSubscriptionIdLength=64`, `webhookSecretLength=64`, `webhookSignatureLength=128`, `webhookMaxSubscribersPerTenant=100`, `webhookMaxEndpointsPerSubscription=10`). 2 state matrices (webhookDeliveryStateMatrix initial=QUEUED + webhookSecretStateMatrix initial=ACTIVE). 3 invariants (`maxBackoff ≥ 720 × initial`, `dispatcherTimeout ≥ 20 × heartbeat`, `replayGrace ≥ 1 × replayMaxAge`). Sandbox egress allowlist. Outbox events `gp.importexport.v1.WebhookDeliveryQueued` / `WebhookDeliveryDeadLettered` / `WebhookSubscriptionRevoked`. |
| `platform/helm/genealogy-platform/files/webhook-policy.yaml` (byte-equal) | Chart mirror. |
| `scripts/lint-webhook.mjs` (new) | E9.6 deep validator (10 closed-set + 20 boolean + 22 numeric + 3 invariants + outbox envelope + 13 audit hooks + 24 forbidden payload patterns + capability boundaries + chart mirror byte-equality). |
| `scripts/__tests__/lint-webhook.test.mjs` (new, 2 tests) | Pins: canonical contract exits 0; chart mirror byte-equal confirmation. |
| `package.json` (extended) | New `lint:webhook` / `check:webhook` scripts. |
| `services/importexport-service/src/main/java/.../webhook/WebhookRetryPolicy.java` (new) | Closed-set enum (3 values) + wire round-trip. |
| `services/importexport-service/src/main/java/.../webhook/WebhookDispatcherGuard.java` (new) | Pure orchestrator: `dispatch(WebhookDeliveryRequest{targetUrl, attempts, initialBackoffSeconds, maxBackoffSeconds, retryPolicy, signatureAlgorithm, signed, dnaBucketReference, piiLeakDetected, tenantRevoked, idempotencyKeyReuseConflict, tenantPseudoId, expectedTenantPseudoId})` → refuses null URL, non-https scheme, attempts > 8, maxBackoff < 720 × initial, null retry policy / signature algorithm, unsigned payload, DNA bucket reference, PII leak, tenant mismatch, tenant revoked, idempotency key reuse conflict. |
| `services/importexport-service/src/test/java/.../webhook/WebhookDispatcherGuardTest.java` (new, 9 tests) | Pins: valid → ok; non-https fails; attempts exceeded fails; maxBackoff too small fails; not signed fails; DNA bucket reference fails; tenant revoked fails; tenant mismatch fails; enum wire round-trip. |

## Decisions

- **ADR-E0.5-08 (Avro + Apicurio + BACKWARD evolution): ACCEPTED** — every closed-set vocabulary anchored to the YAML contract.
- **ADR-E0.5-15 (DNA bucket shield): ACCEPTED** — DNA bucket keys blocked end-to-end.
- **R12.6 (signed payload + retry + dead-letter + replay with audit): ACCEPTED** — `signedPayloadMandatory=true` + `retryIdempotent=true` + `deadLetterAudited=true` + `replayRequiresAudit=true` + `secretRotationRequired=true` + `secretRotationAudited=true`.

## Validation

| Command/check | Result | Evidence |
| ------------- | ------ | -------- |
| `node scripts/lint-webhook.mjs` | PASS | exit 0 |
| `node --test scripts/__tests__/lint-webhook.test.mjs` | PASS | 2/2 tests pass |
| `pnpm check:importexport` | PASS | 14/14 unit tests pass |
| `JAVA_HOME=… pnpm check:java` | PASS | 86 tasks executed, exit 0 |
| `pnpm check:boundary` | PASS | scanned 11 service(s); no cross-service db/domain imports |
| `pnpm lint:ownership` | PASS | 37 declared paths, all per-directory OWNERS present |
| `pnpm check:gradle:lock` | PASS | every lockAll subproject has gradle.lockfile |
| `diff -q contracts/importexport/webhook-policy.yaml platform/helm/.../webhook-policy.yaml` | PASS | byte-equal (11142 bytes) |

## Security and privacy

- Tenant isolation: PASS — `tenantBoundaryOnEveryRepository=true` + tenant pseudo id mandatory + comparison + `revokeOnTenantRevocation=true` auto-revoke.
- Authorization/ABAC: N/A at the webhook layer (per-subscription authorization is part of the subscription flow + OpenFGA integration in the runtime worker).
- Secret/PII/DNA leakage: PASS — `dnaBucketAccess=FORBIDDEN` + DNA bucket shield + `secretInPlainPayloadForbidden=true` + signed payload mandatory + `eventMinimizationByDefault=true` + forbidden payload patterns (24 entries incl. `rawDnaSequence`, `rawEmail`, `productionPii`).
- Dependency/config scan: N/A — no new dependencies.

## Acceptance criteria

- [x] Closed-set vocabularies pinned + linter enforced (10 sets)
- [x] Sandbox egress allowlist pinned + linter enforced (7 entries)
- [x] 2 state matrices (webhookDeliveryStateMatrix + webhookSecretStateMatrix) reachable from initial; terminals have empty transition lists
- [x] Boolean guard rails pinned to exact values (20)
- [x] Numeric bounds pinned to exact values (22)
- [x] Reconciled invariants enforced (maxBackoff ≥ 720 × initial, dispatcherTimeout ≥ 20 × heartbeat, replayGrace ≥ 1 × replayMaxAge)
- [x] Outbox envelope fields declared for every outbox event (3 events)
- [x] Audit hooks declared for every required event (13)
- [x] DNA bucket shield enforced (3 prefixes)
- [x] Cross-service references opaque (webhook never joins)
- [x] Chart mirror byte-equal
- [x] Signed payload + signed URL + secret rotation pinned
- [x] Retry + dead-letter + audited replay pinned
- [x] Event minimization pinned
- [x] Disable / revoke on tenant revocation + repeated abuse pinned

## Residual risks

- Actual Temporal dispatcher + Vault transit signing + Kafka DLQ
  topic + signed secret rotation flow + event minimization
  redaction transformer land in later sub-epic (E9.x / E11.x).
  The orchestrator today is the contract-side guardrail only.