# .kiro/specs/genealogy-platform/evidence

Completion-evidence files. Per `AGENTS.md` §4 a PR cannot merge
without a `<TASK_ID>.md` evidence file committed here describing
what was changed, what was tested and how it maps back to the spec.

Each evidence file is referenced by the matching checkbox in
`tasks.md` and serves as the audit trail for that epic/subtask.