# apps/web/public/icons

Application icons referenced by `apps/web/public/manifest.webmanifest`
and the install prompt. Per `design.md` §10 the PWA must be
installable on every supported viewport (mobile, tablet, desktop)
and pass the platform's accessibility bar.

Owner: web-app team. Reviewer: Design.