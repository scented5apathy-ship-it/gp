# apps/web/src/app/health

Kubernetes probe route. Returns the liveness/readiness response
required by the deployment manifests in `platform/helm/templates/web/`
(added in later epics).

Owner: web-app team. Reviewer: SRE.