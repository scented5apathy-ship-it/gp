# contracts/openapi/bff/v1

Current OpenAPI spec for `apps/web-bff/`. Per
`ownership-catalog.md` §5.1 this version is pinned for **12 months**
of overlap after a deprecation announcement.

Schemas:

- `session.yaml` — authenticated session bootstrap, locale
  resolution and tenant context surface.

Owner: web-bff team. Reviewers: web-app team, contract-first
platform team.