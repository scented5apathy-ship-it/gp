# gradle/wrapper

Gradle wrapper artefacts pinned to a single version per
ADR-E0.5-01 (`architecture-decisions.md` §16 #1). Per
`AGENTS.md` §2 pinning any other version requires an ADR
supersession.

- `gradle-wrapper.jar`
- `gradle-wrapper.properties`

Owner: platform-primary.