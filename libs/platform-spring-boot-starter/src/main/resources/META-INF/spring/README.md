# libs/platform-spring-boot-starter/.../resources/META-INF/spring

Spring Boot autoconfiguration metadata for the platform starter.

- `org.springframework.boot.autoconfigure.AutoConfiguration.imports`
  — list of `@AutoConfiguration` classes to load (`AuditAutoConfiguration`,
  `OpenFeatureAutoConfiguration`, `TrustedContextAutoConfiguration`,
  `PlatformSpringBootAutoConfiguration`).

Owner: platform-secondary.