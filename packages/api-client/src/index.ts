/**
 * Placeholder API client. Real implementation lands in E5.x once the
 * OpenAPI/Protobuf contract tooling is wired (E1.3). The empty export
 * keeps the workspace build green for `pnpm -r typecheck`.
 */
export const apiClientVersion = "0.1.0";
