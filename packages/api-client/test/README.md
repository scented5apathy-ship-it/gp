# packages/api-client/test

Tests for the typed REST client runtime helpers.

- `runtime.test.ts` — problem-mapping, pagination cursor handling,
  idempotency key propagation, error envelope unmasking.

Owner: web-app team.