# packages/eslint-config/src

Flat-config entrypoints.

- `index.ts` — base config (TypeScript strict + import + a11y +
  security plugins). Other configs in the repo extend this file.

Owner: web-app team.