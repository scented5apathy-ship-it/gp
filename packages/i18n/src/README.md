# packages/i18n/src

i18n loader and helpers.

- `index.ts` — `loadMessages(locale)`, ICU formatter factory and
  plural rule helpers. Wraps `Intl.PluralRules` and locale-specific
  CLDR data.

Owner: web-app team. Reviewers: i18n.