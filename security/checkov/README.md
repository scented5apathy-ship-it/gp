# security/checkov

Checkov policies for the platform. Reserved for the platform's
custom Checkov rules layered on top of the OSS ruleset (per
`AGENTS.md` §4 and `.github/workflows/security.yml`).

Currently empty — populated in later epics as platform-specific
policies (e.g. tenant-isolation, signed-URL issuance, Keycloak
realm hardening) are codified.

Owner: Security Engineering team. Reviewers: platform-primary, SRE.