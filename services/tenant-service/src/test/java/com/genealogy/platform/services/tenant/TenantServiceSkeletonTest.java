package com.genealogy.platform.services.tenant;

import org.junit.jupiter.api.Test;

class TenantServiceSkeletonTest {

    @Test
    void placeholder_so_ci_can_run_unit_stage() {
        // E1.1 deliberately ships a single-passing test so that
        // `:services:tenant-service:test` exercises the Gradle
        // configuration (toolchain, lockfile, ArchUnit boundary check).
        // Real coverage arrives with E3.x.
    }
}